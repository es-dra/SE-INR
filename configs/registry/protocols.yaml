protocols:
  benchmark_discrete:
    status: active
    entrypoint: entrypoints/eval_full.py
    datasets: [Set5, Set14, BSD100, Urban100]
    scales: [2, 3, 4, 6, 8, 12, 16, 24, 30]
    id_scales: [2, 3, 4]
    ood_scales: [6, 8, 12, 16, 24, 30]
    data_protocol: HR onsite bicubic downsampling via sr-implicit-downsampled
    checkpoint_rule: epoch-best.pth

  auxiliary_quality_consistency:
    status: active
    entrypoint: scripts/analysis/evaluate_seed1_aux_metrics.py
    current_artifact: artifacts/derived/diagnostics/seed1_aux_metrics_all8
    final_candidate_artifact: artifacts/derived/diagnostics/sc_inr_final_aux_metrics_seed1
    caveat: Complete auxiliary evidence is still seed1-only; final-candidate SC-INR is included in the seed1 auxiliary artifact but not as multi-seed auxiliary evidence.

  qualitative_candidates:
    status: retired_candidate_pool
    entrypoint: scripts/viz/prepare_qualitative_figure.py
    current_artifact: artifacts/derived/paper_figures/qualitative_selected_seed1
    caveat: Auto-delta crop pools are not active evidence. Only user-confirmed selected figures can be cited.
