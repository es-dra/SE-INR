test_dataset:
  dataset:
    name: image-folder
    args:
      root_path: /workspace/SE-INR/Data/Set14/HR
  wrapper:
    name: sr-implicit-downsampled
    args:
      scale_min: 2
      scale_max: 2
  batch_size: 1

data_norm:
  inp: {sub: [0.5], div: [0.5]}
  gt: {sub: [0.5], div: [0.5]}

eval_type: benchmark-2
eval_bsize: 50000
