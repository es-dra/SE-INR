# 2026-05-12

## SC-INR-EQ benchmark 与 6.1-6.6 机制 suite

完成范围：

- 补齐 `SC-INR-EQ` seed1 full benchmark。
- 将 6.1-6.6 机制实验集中到一个 artifact：
  `artifacts/derived/analysis/mechanism_suite_2026-05-12/`。
- 接入 `SC-INR-EQ` 到评估/分析入口和 registry。
- 用真实监督 subagent 做协议审查；采纳其关于命名闭环、负控 caveat、
  SC-INR-EQ exploratory 边界和 artifact 收敛的意见。

改动文件或产物：

- 代码/配置：
  - `entrypoints/eval_full.py`
  - `scripts/analysis/analyze_results.py`
  - `scripts/analysis/analyze_sampling_response.py`
  - `scripts/analysis/evaluate_seed1_aux_metrics.py`
  - `scripts/analysis/smoke_check_sc_inr_eq.py`
  - `scripts/analysis/run_mechanism_suite.py`
  - `configs/registry/models.yaml`
- 文档/ledger：
  - `docs/project/current_state.md`
  - `paper/ARTIFACTS_ALLOWED.md`
  - `paper/claims_evidence_matrix.md`
- 新产物：
  - `artifacts/raw_results/seed1/benchmark_sc_inr_eq.json`
  - `artifacts/derived/analysis/mechanism_suite_2026-05-12/`

运行过的命令/测试/分析及结果：

- `python -m py_compile entrypoints/eval_full.py scripts/analysis/analyze_sampling_response.py scripts/analysis/evaluate_seed1_aux_metrics.py scripts/analysis/run_mechanism_suite.py scripts/analysis/smoke_check_sc_inr_eq.py scripts/analysis/analyze_results.py`
  - 通过。
- YAML parse for `configs/registry/models.yaml`
  - 通过。
- `CUDA_DEVICE_ORDER=PCI_BUS_ID CUDA_VISIBLE_DEVICES=0 python scripts/analysis/smoke_check_sc_inr_eq.py --checkpoint save/sc-inr-eq/epoch-best.pth --device cuda:0`
  - 通过；forward/backward finite；scale cells finite；`response_cell_delta=4.315952e-01`。
- `CUDA_DEVICE_ORDER=PCI_BUS_ID CUDA_VISIBLE_DEVICES=0 python entrypoints/eval_full.py --device 0 --save_root save --models SC-INR-EQ --output artifacts/raw_results/seed1/benchmark_sc_inr_eq.json`
  - 36/36 finite，无 missing/None。
- 6.1/6.4：
  - `analyze_sampling_response.py --mode both ... --out artifacts/derived/analysis/mechanism_suite_2026-05-12/sampling_response`
  - 生成 `response_distribution.csv` 162 行、`cell_response_curve.csv` 66 行。
- 6.3：
  - `evaluate_seed1_aux_metrics.py --skip_quality --skip_visuals ... --out artifacts/derived/analysis/mechanism_suite_2026-05-12/sampling_consistency`
  - 生成 `consistency_summary.csv` 36 行，无 NaN。
- 6.2/6.5/6.6：
  - `run_mechanism_suite.py --device cuda:0 --out artifacts/derived/analysis/mechanism_suite_2026-05-12`
  - 生成 `scale_gain_curve.csv` 18 行、`cell_extrapolation_diagnostic.csv` 50 行、
    `cell_intervention_summary.csv` 20 行和 cell intervention patch figures。

证据支持的结论：

- `SC-INR-EQ` seed1 full benchmark：
  - ID `31.1513`
  - OOD `22.7779`
  - ALL `25.5691`
- `SC-INR-EQ` vs `LTE-EQ`：
  - ID `+0.0841 dB`
  - OOD `+0.0749 dB`
  - ALL `+0.0780 dB`
- `SC-INR-EQ` vs final `SC-INR`：
  - ID `+0.0326 dB`
  - OOD `+0.0014 dB`
  - ALL `+0.0118 dB`
- 6.3 consistency 不能作为主证据：
  - 平均 consistency PSNR 排序中 `LTE-PhaseZ`、`LTE-NoCellPhase`、
    `SC-INR-NoSinc` 都高于完整 `SC-INR`/`SC-INR-EQ`。
  - 这再次证明 cell-independent 或 no-active-response 模型可以刷高 same-LR
    consistency。
- 6.6 cell intervention：
  - `LTE` 对错误 cell 的平均 RMSE-Y vs true cell 更高；
  - `LTE-NoCellPhase` 和 `LTE-PhaseZ` 为 0；
  - `SC-INR`/`SC-INR-EQ` 为非零但较小，符合 analytic finite response 的解释。

未解决风险：

- `SC-INR-EQ` 仍是 single-seed exploratory extension，不能作为主方法。
- 仍缺更严格的 Rot-E/group action 属性测试，尤其是通道顺序、旋转响应、`eff_x/eff_y`
  与 axis-aligned cell sinc 积分语义。
- 6.1-6.6 当前是小样本机制 suite，其中 6.6 使用固定 Urban100 img004 中心 crop；
  可用于诊断和候选图，不是平均视觉质量证据。
- 6.3 consistency 的负控 caveat 很强，不能用它单独证明 sampling correctness。

下一步：

- 若要把 6.1-6.6 纳入论文，应先做 figure selection protocol 和 claim gate：
  main figure 只放 scale gain、response/cell intervention 中最稳的一部分；
  consistency 放 appendix 或 diagnostic caveat。
- 如果继续推进 `SC-INR-EQ`，先补 Rot-E 属性测试，再决定是否多 seed；不要仅因单 seed
  小幅正 delta 直接扩展为主线。

专家评审：

可靠结论：`SC-INR-EQ` benchmark 完整且数值有限，单 seed 相对 `LTE-EQ` 有小幅正增益；
机制 suite 已覆盖用户要求的 6.1-6.6，且产物集中、可追溯。`SC-INR-EQ` 相对 final
`SC-INR` 基本打平这一判断有直接 raw benchmark 支持。

证据不足：`SC-INR-EQ` 的数学正确性仍未由完整属性测试证明；6.1-6.6 目前是机制诊断，
不是因果证明。same-LR consistency 的负控结果阻止把它写成主证据。

下一步是否合理：先整理机制图和补属性测试比立即扩大 `SC-INR-EQ` 多 seed 更合理；若论文
时间紧，应把 `SC-INR-EQ` 放到 exploratory/appendix，主线仍围绕 final `SC-INR` 的 3-seed
结果与 NoSinc/response caveat。

需要防止过度宣称：不能写 strict scale equivariance、whole-network scale equivariance、
SC-INR-EQ 已成为主方法、或 consistency alone 证明 sampling correctness。

## 项目健康治理与 canonical benchmark 收敛

完成范围：

- 建立 canonical benchmark 活跃入口：`artifacts/derived/benchmarks/`。
- 建立辅助证据索引：`artifacts/derived/evidence/README_zh.md`。
- 将模型命名、benchmark 主表、seed1 context、NoSinc、SC-INR-EQ 和机制 suite 的引用收敛到
  `paper/ARTIFACTS_ALLOWED.md`、`paper/claims_evidence_matrix.md` 和
  `docs/project/current_state.md`。
- 删除或移出 active tree：
  - 删除旧 overview 派生产物：`artifacts/derived/analysis/overview/`
  - 删除旧 planning table：`artifacts/derived/paper_tables/current_evidence_2026-05-10/`
  - 删除未确认自动 qualitative candidate pool：`artifacts/derived/paper_candidates/qualitative_phiz_candidates/`
  - 清理历史 smoke/debug 文件，仅保留 `artifacts/smoke/README_zh.md`
  - 将 `artifacts/derived/analysis/sinc_theory_checks_2026-05-09/` 移到
    `artifacts/legacy/derived_analysis/sinc_theory_checks_2026-05-09/`

改动文件或产物：

- 新增：
  - `scripts/analysis/build_canonical_benchmarks.py`
  - `artifacts/derived/benchmarks/benchmark_all_models_long.csv`
  - `artifacts/derived/benchmarks/benchmark_by_seed_model.csv`
  - `artifacts/derived/benchmarks/paper_main_3seed.csv`
  - `artifacts/derived/benchmarks/paper_main_3seed_summary.csv`
  - `artifacts/derived/benchmarks/paper_main_3seed_paired_delta.csv`
  - `artifacts/derived/benchmarks/paper_context_seed1.csv`
  - `artifacts/derived/benchmarks/README_zh.md`
  - `artifacts/derived/evidence/README_zh.md`
  - `artifacts/smoke/README_zh.md`
- 修改：
  - `entrypoints/eval_full.py`
  - `scripts/analysis/analyze_results.py`
  - `scripts/analysis/run_mechanism_suite.py`
  - `paper/ARTIFACTS_ALLOWED.md`
  - `paper/claims_evidence_matrix.md`
  - `docs/project/current_state.md`
  - `README.md`
  - `MANIFEST.md`
  - `configs/registry/protocols.yaml`
  - `experiments/phiz/experiment_card.md`
  - `docs/project/SC-INR_Exp.md`
  - `docs/project/project_story.md`
  - `paper/draft/main.tex`

运行过的命令/测试/分析及结果：

- `python scripts/analysis/build_canonical_benchmarks.py`
  - 生成 canonical benchmark 表；`benchmark_all_models_long.csv` 为 720 行。
- `python -m py_compile entrypoints/eval_full.py scripts/analysis/analyze_results.py scripts/analysis/build_canonical_benchmarks.py scripts/analysis/run_mechanism_suite.py`
  - 通过。
- YAML/JSON/CSV sanity check：
  - `configs/registry/models.yaml`、`configs/registry/protocols.yaml` 可解析。
  - `artifacts/derived/paper_tables/current_evidence_2026-05-11/provenance.json` 可解析。
  - canonical long table 每行均有 `source_file/raw_key/canonical_model` 且 `finite=true`。
- 数值一致性检查：
  - final `SC-INR` 3-seed summary 与 2026-05-11 canonical 结果一致到论文精度。
  - paired delta 检查通过：vs LIIF ID/OOD/ALL 为 `+0.043728/+0.032760/+0.036416`；
    vs LTE ID/OOD/ALL 为 `-0.004753/+0.050421/+0.032030`。
- 引用检查：
  - active docs 中不再引用旧 `analysis/overview`、旧 qualitative candidate pool、
    `current_evidence_2026-05-10`、旧 `results/analysis` 或旧
    `results/benchmark_seed1_with_signed_phiz`。

证据支持的结论：

- 当前 benchmark 主入口已经从分散 dated artifact 收敛为 `artifacts/derived/benchmarks/`。
- seed1 raw key `SC-INR` 与 final `SC-INR` 的误读风险已通过 `(source_file, raw_key)`
  显式映射缓解。
- 旧 overview、旧 planning table、未确认自动候选池和 smoke/debug 输出已不再污染 active tree。
- `SC-INR-EQ` 仍保留在 seed1 context/exploratory 层级，未进入主表或主 claim。

未解决风险：

- `scripts/analysis/analyze_sampling_response.py` 和 `evaluate_seed1_aux_metrics.py` 仍有各自的
  model path/alias 表；本轮先通过 canonical benchmark 入口消除最高风险的 benchmark 误读，
  尚未完全抽取统一 registry loader。
- `SC-INR-EQ` 的 Rot-E 属性测试仍未补齐，不能写 strict equivariance 或数学接入已完全验证。
- `paper/draft/main.tex` 仍是内部草稿，后续需要按 canonical benchmark 表重新生成正式 LaTeX 表格。

下一步：

- 若继续治理代码健康，应抽出共享 model registry loader，减少 analysis scripts 中重复 alias 表。
- 做 paper-facing figure/table selection gate：只从 `artifacts/derived/benchmarks/` 和
  `artifacts/derived/evidence/README_zh.md` 进入正文/appendix。
- 为 `SC-INR-EQ` 补 Rot-E/channel-order/cell-response 属性测试后，再判断是否值得进一步多 seed。

专家评审：

可靠结论：本轮确实减少了 active artifact 面和模型命名误读风险；canonical benchmark 表保留
raw provenance 且数值通过一致性检查，适合作为后续论文表格入口。

证据不足：这不是新的科学结果；它是项目健康和证据治理节点。`SC-INR-EQ` 的科学正确性仍只到
exploratory benchmark + smoke/mechanism diagnostic，不能因整理后入口更清楚而升级 claim。

下一步是否合理：先做 table/figure selection gate 比继续新增机制实验更合理。若继续代码清理，
应集中在统一 registry loader 和旧 alias 表删减，而不是再创建新的平行脚本。

需要防止过度宣称：不能把 canonical 表的整洁性当作科学进展；不能把 deleted/archived artifact
解释成证据消失，只是它们不再是 active 入口。

## 项目健康治理续检：共享命名 helper 与兼容层断链清理

完成范围：

- 按 L3 项目健康节点继续治理 active tree，目标是降低 `SC-INR` raw key 复用、旧路径断链和
  dated artifact 混杂造成的误读风险。
- 使用两个真实 subagent 的动态分工结果：
  - 代码别名/路径审查：指出最高风险是 source-aware `SC-INR` 语义，不应全局把 raw
    `SC-INR` 映射为某一模型；建议先收敛重复 `MODEL_PATHS`/alias helper，不碰
    `sc_inr_eq.py`。
  - artifact/docs 活跃面审查：指出旧 qualitative candidate、old overview、current evidence
    planning table 和断链 symlink 会继续污染 active 入口；建议补 legacy/compat README。

改动文件或产物：

- 新增 `scripts/analysis/model_registry.py`，从 `configs/registry/models.yaml` 读取 paper-facing
  alias、seed1 checkpoint 路径和 source-aware raw result key 规范化 helper。
- 更新 `scripts/analysis/analyze_sampling_response.py` 与
  `scripts/analysis/evaluate_seed1_aux_metrics.py`，改为使用共享 `MODEL_PATHS`/`STYLE`。
- 更新 `scripts/analysis/analyze_results.py`，改为使用共享
  `canonicalize_result_key(raw_key, source_path, raw_keys)`，删除本地重复 alias/legacy-path 判断。
- 更新 `artifacts/README_zh.md`、`artifacts/derived/README_zh.md`、
  `artifacts/results/README_zh.md`、`artifacts/results/analysis/README_zh.md`、
  `artifacts/derived/analysis/benchmark_progress_2026-05-09/README_zh.md`、
  `artifacts/derived/analysis/seed1_aux_metrics_all8/paper/README_zh.md` 和
  `reviewer_advice_analysis.md`，将旧 `results/analysis`、旧 paper candidates、旧 paper tables
  指向替换为 canonical `artifacts/derived/benchmarks/`、`artifacts/derived/evidence/` 和
  selected qualitative 入口。
- 新增 `artifacts/derived/analysis/benchmark_progress_2026-05-10/README_zh.md`，明确 NoSinc
  seed1 消融的输入、派生表和禁止用途。
- 新增 `artifacts/legacy/README_zh.md`。
- 删除 broken compatibility symlink：旧 overview 表图、旧 qualitative candidate pool、
  smoke/debug analysis symlink；未删除 checkpoint、raw result 或正式 benchmark。
- 删除 `__pycache__`、`.pytest_cache`、`.ipynb_checkpoints` 等生成缓存。

运行过的命令/测试/分析及结果：

- `python -m py_compile entrypoints/eval_full.py scripts/analysis/analyze_results.py scripts/analysis/analyze_sampling_response.py scripts/analysis/evaluate_seed1_aux_metrics.py scripts/analysis/model_registry.py scripts/analysis/build_canonical_benchmarks.py scripts/analysis/run_mechanism_suite.py scripts/viz/prepare_qualitative_figure.py scripts/paper/prepare_seed1_aux_paper_artifacts.py`
  - 通过。
- source-aware normalization synthetic check：
  - seed1 `benchmark_signed_phiz.json` 中 raw `SC-INR` -> `SC-INR-NoPhi`；
  - seed1 raw `SC-INR+PhiZ` -> final `SC-INR`；
  - seed2 `benchmark_sc_inr.json` raw `SC-INR` -> final `SC-INR`；
  - seed2 core raw `SC-INR-Adaptive` -> `SC-INR-NoPhi`。
- `python scripts/analysis/build_canonical_benchmarks.py`
  - 重新生成 canonical benchmark 表。
  - `benchmark_all_models_long.csv` 为 720 行，PSNR 全部 finite。
  - `paper_main_3seed_summary.csv` 中 final `SC-INR` OOD 为 `22.757810 ± 0.031629`，与既有结论一致。
- `python scripts/analysis/analyze_results.py --help` 和 `analyze_results.load_json(...)` canonical
  key smoke check 通过。
- `find artifacts results -xtype l -print`
  - 无 broken symlink。
- active doc targeted path check 通过；旧 active 路径搜索未再命中旧 overview、旧 candidate pool、
  旧 current evidence 或已删除 smoke/debug symlink。

证据支持的结论：

- 当前项目健康度比上一个节点更好：benchmark 主入口、辅助证据入口和兼容层说明更清楚；
  `SC-INR` 模型名复用风险已经集中到共享 helper 与 canonical benchmark long table 中处理。
- 删除动作主要发生在 broken symlink、过时 planning/候选/overview 派生产物和生成缓存；没有删除
  checkpoint、formal raw result 或当前 canonical benchmark。
- NoSinc、SC-INR-EQ、mechanism suite 仍在辅助/诊断/探索层级，没有被整理工作误升格为主 claim。

未解决风险：

- `entrypoints/eval_full.py`、`eval_continuous.py`、`eval_fce.py`、`eval_phase_intervention.py`
  以及部分 paper/viz 脚本仍有局部 alias 逻辑；这轮只收敛了最高风险的 analysis helper 和 active docs。
- `build_canonical_benchmarks.py` 仍保留显式 source mapping，这是可读性和审计性的刻意设计，不应简单
  抽象掉；但后续可以让它复用 registry metadata。
- dated analysis 目录仍较多，当前通过 evidence README 和 ARTIFACTS_ALLOWED 约束入口；后续若论文表图冻结，
  可以继续把未被引用的 dated 目录删除或移入 legacy。

下一步：

- 下一轮健康优化优先做“入口削减”，不是新增分析：检查所有 active docs 是否只通过
  `artifacts/derived/benchmarks/`、`artifacts/derived/evidence/README_zh.md` 和
  `paper/ARTIFACTS_ALLOWED.md` 进入证据。
- 逐步把 remaining alias logic 收敛到 registry/helper，但每次只改一类入口，并保持 source-aware
  raw key 语义测试。
- 若继续动 `eval_full.py`，要先冻结兼容目标：是否仍允许 `results/seeds/...` legacy output 被自动规范化。

专家评审：

可靠结论：这次推进是结构健康改善，不是科学结论升级；验证覆盖了脚本语法、命名 helper、canonical
benchmark 重建、断链和关键 active path。

证据不足：没有对所有旧 entrypoint 做行为回归，也没有运行长评估；因此不能说项目已经完全无历史包袱。

下一步是否合理：先继续收敛入口和 alias helper 是合理的；不建议现在再新增机制实验或新模型分支。

需要防止过度宣称：不要把“无 broken symlink”和“canonical 表可重建”说成论文已经工程完备；它们只说明
当前证据面更清晰、后续 agent 不容易误读。

## 项目健康治理续检 2：eval 入口 alias 收敛与辅助指标索引

完成范围：

- 按 L3 项目健康节点继续处理剩余 alias/legacy result key 风险，但保持低风险边界：不改 checkpoint discovery、不改模型实现、不改变 raw formal results。
- 使用两个真实 subagent：
  - 代码入口审查指出 `eval_full.py` 的本地 `MODEL_ALIASES` / `LEGACY_RESULT_ALIASES` 和 resume 结果 key 判断是最高风险重复点；建议只收敛 result-key resume normalization 与 CLI alias，暂不动 checkpoint path/FCE/phase intervention。
  - artifact/docs 审查指出 dated analysis 中 `benchmark_progress_*`、aux metrics、response diagnostics、mechanism suite 仍被正式引用，应保留；但 `seed1_aux_metrics_all8/` 顶层缺少 README，容易被误读为全目录 paper-ready evidence。

改动文件或产物：

- `entrypoints/eval_full.py`
  - 改用 `scripts.analysis.model_registry.MODEL_ALIASES` 解析 `--models`。
  - 新增 `normalize_existing_results(...)`，用共享 `canonicalize_result_key(...)` 规范化旧 JSON 的 result keys。
  - 保留 `--legacy_sc_inr_no_phi_input` 作为手动强制旧 `SC-INR -> SC-INR-NoPhi` 的兼容开关。
  - 未改 `--save_root` 与目录发现逻辑。
- `entrypoints/eval_continuous.py`
  - 改用共享 `MODEL_ALIASES` 解析 CLI 模型名。
  - 未改固定 `save/.../epoch-best.pth` 路径表。
- `configs/registry/models.yaml`
  - 为 `SC-INR-NoSinc` 补充 legacy raw key `SC-INR-w/o-sinc`。
- `artifacts/derived/analysis/seed1_aux_metrics_all8/README_zh.md`
  - 新增顶层索引，明确顶层 CSV 是辅助审计/再分析数据，论文入口应走 `paper/` 或 evidence index。

运行过的命令/测试/分析及结果：

- `python -m py_compile scripts/analysis/model_registry.py entrypoints/eval_full.py entrypoints/eval_continuous.py entrypoints/eval_fce.py entrypoints/eval_phase_intervention.py scripts/paper/prepare_seed1_aux_paper_artifacts.py scripts/viz/prepare_qualitative_figure.py`
  - 通过。
- shared alias check：
  - `SC-INR -> SC-INR`
  - `SC-INR-Adaptive -> SC-INR-NoPhi`
  - `SC-INR+PhiZ -> SC-INR`
  - `SC-INR-w/o-sinc -> SC-INR-NoSinc`
- `eval_full.normalize_existing_results(...)` synthetic check：
  - seed1 `SC-INR` + `SC-INR+PhiZ` 正确拆为 `SC-INR-NoPhi` 与 final `SC-INR`。
  - seed2 clean `benchmark_sc_inr.json` 中 `SC-INR` 保持 final `SC-INR`。
  - `--legacy_sc_inr_no_phi_input` 可强制旧 `SC-INR -> SC-INR-NoPhi`。
  - `SC-INR-w/o-sinc` 规范化为 `SC-INR-NoSinc`。
- `python entrypoints/eval_full.py --help`、`python entrypoints/eval_continuous.py --help`
  - 均可正常加载。
- `python scripts/analysis/build_canonical_benchmarks.py`
  - canonical benchmark 可重建；`benchmark_all_models_long.csv` 仍为 720 行，PSNR 全部 finite。
  - final `SC-INR` OOD 为 `22.757810 ± 0.031629`，未改变结论。
- `find artifacts results -xtype l -print`
  - 无 broken symlink。
- targeted active path check 通过；旧 overview、旧 candidate pool、旧 current evidence、已删除 smoke/debug symlink 未回流到 active docs。

证据支持的结论：

- `eval_full.py` 中最危险的 result-key 语义重复已收敛到共享 helper；后续 resume 旧结果时更不容易把 `SC-INR` 误读为错误模型。
- `eval_continuous.py` 的 CLI alias 与 registry 保持一致，但其 checkpoint 路径逻辑仍按旧入口保留，避免改变实验行为。
- `seed1_aux_metrics_all8` 顶层和 `paper/` 子目录的用途边界更清楚。

未解决风险：

- `eval_fce.py` 仍保留带 FCE model type 的本地模型表；这是实验协议的一部分，暂不应机械迁移到通用 registry。
- `eval_phase_intervention.py` 仍硬编码 LTE/LTE-NoCellPhase 干预对象；这是干预实验设计的一部分，不属于普通 alias 去重。
- `scripts/paper/prepare_seed1_aux_paper_artifacts.py` 仍有 `SC-INR -> SC-INR-NoPhi` 的本地映射，因为它消费 legacy aux CSV；若迁移必须先冻结输入 CSV 的 raw key 语义。
- dated analysis 目录仍较多，但当前均被 evidence/claim/allowed artifacts 约束用途；不建议无差别删除。

下一步：

- 若继续健康治理，优先做 paper-facing 表图冻结与引用检查，而不是再扩大脚本重构。
- 若要进一步清 alias，应先给 `scripts/paper/prepare_seed1_aux_paper_artifacts.py` 增加输入语义测试，再决定是否迁移到 source-aware helper。

专家评审：

可靠结论：本轮是工程健康改进，验证覆盖了语法、CLI 加载、alias 语义、resume key normalization、benchmark 重建和断链检查。

证据不足：没有实际启动长评估，因此不能证明所有入口运行时行为完全等价；不过本轮没有改 checkpoint discovery 和数据协议，风险受控。

下一步是否合理：继续做引用冻结和 paper-facing 表图选择比继续抽象所有历史脚本更有价值；对 FCE/phase intervention 这类协议型入口应保持谨慎。

需要防止过度宣称：不能把 alias 去重说成科学进展，也不能因此升级 `SC-INR-EQ`、NoSinc 或 auxiliary consistency 的 claim 等级。

## 发表前项目收敛审查：active surface 应大幅缩小

完成范围：

- 按 L4 发表前项目健康节点进行只读深度审查，目标不是继续小修补，而是判断哪些代码、结果和文档已经成为主线噪声。
- 使用两个真实 subagent：
  - 论文证据链审查：从发表和 claim 支撑角度给出 artifact triage。
  - 代码入口维护性审查：从正式入口、历史脚本和模型文件暴露面角度给出 active/legacy/delete 建议。

核心判断：

- 当前项目已经有主模型、主 benchmark 和主 claim ledger；继续把大量 dated analysis、机制探索、smoke gate、legacy figures 和 exploratory EQ 路线放在 active tree，会降低项目健康度。
- 真正 active 的最小主线应是：
  - 训练入口：`entrypoints/train.py`
  - 离散 benchmark：`entrypoints/eval_full.py`
  - canonical benchmark 聚合：`scripts/analysis/build_canonical_benchmarks.py`
  - 模型/结果命名消歧：`scripts/analysis/model_registry.py`、`configs/registry/models.yaml`
  - 协议 registry：`configs/registry/protocols.yaml`
  - 定性图生成：`scripts/viz/prepare_qualitative_figure.py`
  - 主 benchmark artifact：`artifacts/derived/benchmarks/`
  - 证据白名单与 claim ledger：`paper/ARTIFACTS_ALLOWED.md`、`paper/claims_evidence_matrix.md`
  - 当前状态恢复入口：`docs/project/current_state.md`
- 其他内容应按用途收缩，而不是继续并列在 active 面。

建议的 aggressive triage：

- `keep-active/main`：
  - `artifacts/derived/benchmarks/`
  - `paper/ARTIFACTS_ALLOWED.md`
  - `paper/claims_evidence_matrix.md`
  - `docs/project/current_state.md`
  - `configs/registry/models.yaml`、`configs/registry/protocols.yaml`
  - `entrypoints/train.py`、`entrypoints/eval_full.py`
  - `scripts/analysis/build_canonical_benchmarks.py`、`scripts/analysis/model_registry.py`
  - `scripts/viz/prepare_qualitative_figure.py`
- `keep-active/appendix-or-diagnostic`，但应移到更清晰的 `artifacts/derived/diagnostics/` 或通过 evidence index 进入：
  - `seed1_aux_metrics_all8/`
  - `sc_inr_final_aux_metrics_seed1/`
  - `sc_inr_nosinc_aux_metrics_seed1/`
  - `response_omega_diagnostics_2026-05-10/`
  - 用户确认定性图 `paper_figures/qualitative_selected_seed1/`
- `archive-from-active`：
  - `benchmark_progress_2026-05-09/10/11`：数值已被 `artifacts/derived/benchmarks/` 吸收，继续留在 active analysis 会让后续读者绕过 canonical table。应移到 `artifacts/legacy/derived_analysis/benchmark_provenance/`，只作为 provenance。
  - `mechanism_suite_2026-05-12/`：包含 SC-INR-EQ 和 6.1-6.6 exploratory 机制结果；如果主论文不是 SC-INR-EQ，应只保留 README/少量 CSV 为 appendix diagnostic，完整 suite 进 legacy。
  - `entrypoints/analyze_phase_consistency.py`、`eval_fce.py`、`eval_phase_intervention.py`、`plot_continuous_scale.py`
  - `scripts/analysis/analyze_sampling_response.py`、`run_mechanism_suite.py`、`verify_sinc_observation.py`
  - `scripts/analysis/analyze_results.py`
  - `scripts/analysis/smoke_check_sc_inr_eq.py`、`smoke_check_signed_omega.py`
- `delete-or-legacy`：
  - 旧 overview、自动 qualitative candidate pool、旧 planning table、legacy figures、一次性 launcher。
  - 若没有审计价值，默认删除；若保留，必须离开 active tree 且在 legacy README 中注明禁止用途。

证据支持的结论：

- `artifacts/derived/benchmarks/` 已经足以承载主 benchmark 和 raw-key provenance；dated benchmark progress 目录继续 active 是重复且有误导风险。
- `SC-INR-EQ` 是探索性扩展，不应和 final SC-INR 主线同级暴露；否则会造成“新主方法”误读。
- 机制/consistency 诊断有论文价值，但必须降级为 appendix/diagnostic，不应以大量目录形式和主结果并列。
- 代码入口数量应该收缩到一条训练入口、一个 benchmark 入口、一个 canonical aggregation 入口、一个定性图入口和 registry；其余脚本不应作为第一层 active tools。

未执行删除原因：

- 本轮用户要求是深入分析，尚未明确授权本轮直接移动/删除大量 active 文件。
- 大规模清理会影响白名单、claim ledger、README、MANIFEST 和可能的 paper draft 路径，应该作为单独执行节点完成，并在执行前冻结目标结构。

下一步建议：

- 直接启动“发表前 active surface 收敛执行节点”：
  1. 先建立目标结构：`artifacts/derived/benchmarks/`、`artifacts/derived/diagnostics/`、`artifacts/derived/paper_figures/`、`artifacts/legacy/derived_analysis/`。
  2. 移动 dated benchmark provenance 和 exploratory mechanism suite 到 legacy/diagnostics。
  3. 移动或删除 legacy/diagnostic scripts，正式入口只保留最小集合。
  4. 更新 `README.md`、`MANIFEST.md`、`ARTIFACTS_ALLOWED.md`、`claims_evidence_matrix.md`、`current_state.md`。
  5. 运行断链检查、py_compile、canonical benchmark 重建。

专家评审：

可靠结论：项目已经过了“多保留以免丢证据”的阶段。现在最大的健康风险是 active 面过宽、诊断和主线混放、历史脚本暗示仍有多个主指标。

证据不足：具体删除前仍需逐条确认 paper draft 和 allowed artifacts 的路径迁移，避免把仍在正文引用的 selected figure 或 appendix table 误删。

下一步是否合理：应从现在开始执行 aggressive cleanup，而不是继续新增机制实验或继续补小型兼容 helper。

需要防止过度宣称：清理项目结构不会增加科学贡献；它的价值是让已有贡献更清晰、更可复现、更不容易被误读。

---

完成范围：

- 执行“发表前 active surface 收敛执行节点”的第一轮实质清理。
- 将 active `artifacts/derived/` 收敛为四类：
  - `artifacts/derived/benchmarks/`
  - `artifacts/derived/diagnostics/`
  - `artifacts/derived/evidence/`
  - `artifacts/derived/paper_figures/qualitative_selected_seed1/`
- 将 dated benchmark progress、完整 mechanism suite 和 sinc theory checks 从 active analysis 面降级到
  `artifacts/legacy/derived_analysis/`；删除空的 `artifacts/derived/analysis/`。
- 移动/降级脚本入口：
  - `scripts/analysis/smoke_check_sc_inr_eq.py`
  - `scripts/analysis/smoke_check_signed_omega.py`
  - `scripts/analysis/verify_sinc_observation.py`
  到 `scripts/legacy/diagnostics/`。
- 删除 root-level 训练/评估 compatibility symlink：
  `train.py`、`test.py`、`eval_full.py`、`eval_continuous.py`、`eval_fce.py`、
  `eval_phase_intervention.py`、`analyze_phase_consistency.py`、`plot_continuous_scale.py`。
  仍保留源码/数据/产物兼容 symlink：`models`、`datasets`、`utils.py`、`save`、
  `save-seeds`、`results`、`logs`、`Data`。
- 删除旧 compatibility 污染：
  - `scripts/` root 兼容 symlink；
  - `artifacts/results/analysis` 下旧 analysis/figures symlink；
  - 断链的 `artifacts/results/figures`、`artifacts/results/index.json`、root `figs`。
- 删除无 active 引用且不再作为审计主链的旧 legacy 目录：
  `artifacts/legacy/analysis/`、`artifacts/legacy/figures/`、
  `artifacts/legacy/results_index/`。
- 修正 import 和路径：
  - `scripts/analysis/analyze_results.py`
  - `scripts/analysis/evaluate_seed1_aux_metrics.py`
  - `scripts/analysis/analyze_sampling_response.py`
  改为 package import `scripts.analysis.model_registry`。
  - 更新 README、MANIFEST、protocol registry、claim ledger、artifact whitelist、
    current_state、experiment cards 和 legacy README 中的 active/diagnostic/legacy 路径。

subagent 使用：

- 真实 subagent `Feynman` 负责证据链/引用审查，指出 legacy benchmark 和 mechanism suite 不应作为当前主入口。
- 真实 subagent `Nietzsche` 负责代码入口/清理风险审查，指出 smoke/verify 脚本可低风险迁移，
  `run_mechanism_suite.py` / `analyze_sampling_response.py` / `analyze_results.py` 依赖链暂不应拆。
- 主流程采纳了上述主要意见；对 root entrypoint symlink 的删除比 `Nietzsche` 建议更激进，
  原因是本轮用户明确要求彻底收敛 active surface，且删除前后均完成了引用和 CLI 测试。

运行过的验证：

- `python -m py_compile` 覆盖 `entrypoints/*.py`、`scripts/analysis/*.py`、
  `scripts/paper/*.py`、`scripts/viz/*.py`、`scripts/legacy/diagnostics/*.py`。
- CLI help smoke：
  - `python entrypoints/train.py --help`
  - `python entrypoints/eval_full.py --help`
  - `python entrypoints/plot_continuous_scale.py --help`
  - `python scripts/analysis/run_mechanism_suite.py --help`
  - `python scripts/viz/prepare_qualitative_figure.py --help`
  - `python scripts/legacy/diagnostics/verify_sinc_observation.py --help`
- package import smoke：
  - `scripts.analysis.run_mechanism_suite`
  - `scripts.analysis.analyze_results`
  - `scripts.analysis.evaluate_seed1_aux_metrics`
  - `scripts.analysis.analyze_sampling_response`
- canonical benchmark 重建：
  - `python scripts/analysis/build_canonical_benchmarks.py`
  - `benchmark_all_models_long.csv` 行数为 `720`
  - `SC-INR` OOD mean/std 保持 `22.757810 / 0.031629`
- 路径和结构检查：
  - active docs / registries / experiment cards 反引号路径存在性检查通过；
  - `rg` 确认 active 区无 `artifacts/derived/analysis` 残留；
  - `find . -xtype l` 无断链；
  - 清理后无 `__pycache__`。
- JSON 检查：
  - legacy mechanism suite run config 和 benchmark provenance JSON 通过 `python -m json.tool`。

证据支持的结论：

- 当前 active artifact 面已经基本符合“三分法”：
  模型/配置入口、benchmark 主表、辅助证据/diagnostics 分区清晰。
- 主 benchmark 仍由 `artifacts/derived/benchmarks/` 承载；legacy benchmark provenance
  只作为 audit/support，不能替代 canonical benchmark。
- `SC-INR-EQ` 在 claim ledger 和 whitelist 中继续被限制为 exploratory Rot-E integration，
  不是主方法。

未解决风险：

- `scripts/analysis/run_mechanism_suite.py` 仍依赖 `analyze_sampling_response.py` 和
  `analyze_results.py` 的 helper；下一轮若继续收敛代码入口，应先拆 helper 再移动脚本。
- `entrypoints/eval_fce.py`、`eval_phase_intervention.py`、`analyze_phase_consistency.py`
  等历史诊断入口仍在 `entrypoints/`；本轮只删除 root symlink，没有移动这些文件。
- `docs/archive/` 仍保留历史叙述和旧命令；这是 archive 层，不应影响 active 入口，但后续可缩减为更短索引。

下一步：

- 第二轮健康优化应集中在代码入口层：判断哪些 `entrypoints/` 历史诊断脚本应移入
  `scripts/legacy/diagnostics/`，并拆掉仍被 `run_mechanism_suite.py` 复用的 helper 依赖。
- 继续保持每批改动后即时测试：引用搜索、py_compile、CLI help、断链检查和 canonical benchmark 重建。

专家评审：

可靠结论：本轮是结构健康推进，不是科学 claim 升级。它确实降低了 active 入口混乱、
旧路径误读和 legacy 结果伪装成当前证据的风险。

证据不足：尚未完整审查 paper draft 的所有 LaTeX include/figure/table 路径；若下一步进入投稿排版，
需要单独跑 LaTeX 路径/编译检查。

下一步是否合理：继续清理代码入口是合理的，但必须避免把仍承担复现实验或 helper 依赖的脚本机械移动。

需要防止过度宣称：项目健康改善不能被写成方法贡献；论文中仍应只依据 benchmark、diagnostics
和 claim ledger 说话。

## 项目改名与第二轮清理后的状态汇报

完成范围：

- 项目目录已从 `Equivariant-ASISR` 更名为 `SC-ASISR`；当前工作根目录为
  `/workspace/SE-INR/SC-ASISR`。
- 根目录说明文档已经改为中文：
  - `README.md`
  - `MANIFEST.md`
- 删除根目录旧叙事 symlink：
  - `PHASE0_1_REPORT.md`
  - `PROJECT_STORY.md`
- 删除旧 archive 文档：
  - `docs/archive/phase0_1_report.md`
  - `docs/archive/project_story_legacy_2026-05-09.md`
- checkpoint 命名已收敛为 canonical 目录：
  - final `SC-INR` 使用 `artifacts/checkpoints/seed*/sc-inr`
  - `SC-INR-NoPhi` 使用 `sc-inr-nophi`
  - `SC-INR-NoPhi-Signed` 使用 `sc-inr-nophi-signed`
  - `SC-INR-FixedOmega` 使用 `sc-inr-fixed-omega`
  - `LTE-NoCellPhase` 使用 `lte-nocellphase`
  - `LTE-PhaseZ` 使用 `lte-phasez`
  旧目录名只保留为 symlink/provenance。
- `entrypoints/eval_full.py` 已改为从 `configs/registry/models.yaml` 派生 checkpoint
  目录 alias，减少本地硬编码模型名映射。
- 删除/降级了一批旧入口与旧结果面：
  - 旧 `docs/archive/`
  - 旧 `scripts/legacy/`
  - 旧 `artifacts/results/analysis`
  - 旧 continuous/diagnostics raw result
  - 旧 root/compat 分析入口与若干历史诊断入口
  - 旧 smoke/debug/自动 candidate 产物
- 目前 active artifact 面收敛为：
  - `artifacts/checkpoints/`
  - `artifacts/raw_results/`
  - `artifacts/derived/benchmarks/`
  - `artifacts/derived/diagnostics/`
  - `artifacts/derived/evidence/`
  - `artifacts/derived/paper_figures/`
  - `artifacts/legacy/` 中少量 provenance/audit 材料。

本次状态检查用到的真实 subagent：

- `Hilbert`：只读审查活跃文档/入口引用完整性。指出 `eval_continuous.py`、`MANIFEST.md`、
  `current_state.md`、`project_story.md`、`SC-INR_Exp.md`、`models.yaml` 和部分分析脚本默认
  输出仍有旧入口或 legacy 入口风险。
- `Mill`：只读审查保留治理。指出 `save_test`/`artifacts/scratch/save_test`、`__pycache__`、
  LaTeX 构建副产物、`models.yaml` legacy benchmark 指向、`artifacts/results/` 兼容层等仍需处理。

已运行验证：

- `find . -xtype l -printf ...`
  - 无 broken symlink。
- `PYTHONDONTWRITEBYTECODE=1 python -m py_compile entrypoints/*.py scripts/analysis/*.py scripts/paper/*.py scripts/viz/*.py`
  - 通过。
- `python scripts/analysis/build_canonical_benchmarks.py`
  - 通过，canonical benchmark 表可重建。
- 精准旧路径/旧入口搜索：
  - 未发现 `Equivariant-ASISR`、`/workspace/SE-INR/Equivariant-ASISR`、`docs/archive`、
    `scripts/legacy`、`artifacts/smoke`、`artifacts/results/analysis` 等在 active docs 中残留。

证据支持的结论：

- 当前项目已经从“旧结果、旧入口、旧命名并列”转向以 `SC-ASISR` 为根、以 registry 和 canonical
  benchmark 表为中心的结构。
- `SC-INR` / `SC-INR-NoPhi` / `SC-INR-EQ` 的层级边界更清楚：
  final `SC-INR` 是主方法；`SC-INR-NoPhi` 是旧主线/消融；`SC-INR-EQ` 是 exploratory Rot-E
  integration，不是主方法。
- 旧 `results/phase_intervention.json`、`results/continuous` 等不再是当前 canonical 证据入口；
  IDE 若仍显示这些路径，应视为旧标签页或兼容/历史文件，不应继续引用。

未解决风险：

- `entrypoints/eval_continuous.py` 仍是旧式 continuous 入口，默认输出仍指向 `results/continuous`
  和 `results/figures`；下一步应修改为 canonical diagnostics 输出，或退役该入口。
- `configs/registry/models.yaml` 中部分 result/provenance 字段仍需从 legacy 目录改指向
  `artifacts/derived/benchmarks/`，legacy 只保留为 `provenance_*`。
- `docs/project/current_state.md`、`project_story.md`、`SC-INR_Exp.md` 仍有少量 legacy provenance
  直接引用，应改为先指向 canonical benchmark 和 artifact whitelist。
- `artifacts/results/` 与根目录 `results -> artifacts/results` 仍是旧 raw JSON 兼容层；可保留但不应扩展。
- `save_test -> artifacts/scratch/save_test` 与空 scratch 目录、`__pycache__`、LaTeX 构建副产物仍可继续清理。

下一步：

- 先修正 subagent 指出的残留旧入口：
  1. `models.yaml` result/provenance 字段；
  2. active docs 中直接指向 legacy provenance 的地方；
  3. `eval_continuous.py` 的默认输出和 checkpoint 路径；
  4. 删除空 scratch、生成缓存和不必要构建副产物。
- 每一批改动后继续跑窄验证：py_compile、CLI help、断链检查、旧引用搜索、canonical benchmark 重建。

专家评审：

可靠结论：项目结构健康已经明显改善，主 benchmark 和模型命名的核心误读风险已被 registry/canonical
table 缓解；当前剩余问题主要是旧 continuous 入口、legacy provenance 引用和兼容层尾巴。

证据不足：尚未完成所有残留修正，也没有重新跑长评估；因此不能说项目健康治理已经结束。

下一步是否合理：继续做“残留引用与旧入口退役”是合理的；不建议现在新增模型或新增机制实验。

需要防止过度宣称：这轮工作是工程/证据治理，不是新的科学贡献；论文 claim 仍只能由已冻结 benchmark
和诊断证据支撑。

## checkpoint 命名与 benchmark JSON 入口收敛

完成范围：

- 处理用户指出的两个核心健康问题：
  1. `save`/`artifacts/checkpoints` 下新老命名模型共存；
  2. `results/benchmark*.json` 文件名和内容 raw key 仍是旧命名，且 `SC-INR-EQ` 没有进入便捷 benchmark 入口。
- 删除旧 checkpoint alias symlink，不删除任何实体 checkpoint：
  - `seed1/lte-no-cell`
  - `seed1/lte-feature-phase`
  - `seed1/sc-inr-fixed`
  - `seed1/sc-inr-adaptive`
  - `seed1/sc-inr-adaptive-signed`
  - `seed1/sc-inr-phiz`
  - `seed2/sc-inr-adaptive`
  - `seed3/sc-inr-adaptive`
- 将新命名训练配置从 symlink 迁移为实体 YAML 文件，并删除旧文件名：
  - `train-lte-nocellphase.yaml`
  - `train-lte-phasez.yaml`
  - `train-sc-inr-fixed-omega.yaml`
  - `train-sc-inr-nophi.yaml`
  - `train-sc-inr-nophi-signed.yaml`
  - `train-sc-inr.yaml`
- 清理 `configs/registry/models.yaml`：
  - 删除 `legacy_checkpoint_dir_*`
  - 删除 `legacy_train_config`
  - `SC-INR.result_multiseed` 改为 `artifacts/derived/benchmarks/paper_main_3seed_summary.csv`
  - 阶段记录保留为 `provenance_multiseed`
- 更新 `entrypoints/eval_full.py`，模型发现只使用 canonical checkpoint dir，不再扫描 legacy checkpoint dir。
- 更新 `scripts/analysis/build_canonical_benchmarks.py`，新增两个 canonical JSON：
  - `artifacts/derived/benchmarks/benchmark_all_models_canonical.json`
  - `artifacts/derived/benchmarks/benchmark_paper_main_3seed.json`
- 将 `results/` 从旧 raw JSON 兼容层改为 canonical benchmark JSON 便捷层：
  - 新增 `results/benchmark_all_models_canonical.json`
  - 新增 `results/benchmark_paper_main_3seed.json`
  - 删除 `results/benchmark.json`
  - 删除 `results/benchmark_seed1_with_signed_phiz.json`
  - 删除 `results/seeds/benchmark_seed2.json`
  - 删除 `results/seeds/benchmark_seed3.json`
  - 确认 `results/phase_intervention.json` 不存在。
- 删除空 scratch 留份：
  - `save_test`
  - `artifacts/scratch/save_test`

subagent 使用：

- `Wegener` 审查 checkpoint/save 命名迁移，确认旧 checkpoint 名都是 symlink，新命名都是实体目录；
  建议删除旧 symlink 时同步 registry、`eval_full.py` 和文档。
- `Galileo` 审查 benchmark 文件组织，确认 raw JSON key 历史混杂，不应直接改写 raw；
  建议新增 canonical 派生 JSON，并让 `results/` 不再暴露旧 raw 入口。
- 主流程采纳两者意见：raw JSON 仍保留在 `artifacts/raw_results/` 作 provenance，当前可读入口改为 canonical JSON/CSV。

运行过的命令/测试/分析及结果：

- `find artifacts/checkpoints/seed1 artifacts/checkpoints/seed2 artifacts/checkpoints/seed3 configs/train-div2k -maxdepth 1 -type l`
  - 无输出；checkpoint 和 train config 旧 symlink 已清空。
- registry 路径检查：
  - `checkpoint_dir*`、`train_config`、`result_*` 均存在。
  - 不再存在 `legacy_checkpoint_dir_*` 或 `legacy_train_config`。
- `python entrypoints/eval_full.py --help`
  - 通过。
- `discover_models('artifacts/checkpoints/seed1')` 和 `discover_models('save')`
  - 均发现 12 个 canonical seed1 模型，`SC-INR` 指向 `sc-inr/epoch-best.pth`。
- `python -m py_compile entrypoints/*.py scripts/analysis/*.py scripts/paper/*.py scripts/viz/*.py`
  - 通过。
- `python scripts/analysis/build_canonical_benchmarks.py`
  - 通过，canonical benchmark 表和 JSON 可重建。
- `python -m json.tool` 检查两个新增 canonical JSON
  - 通过。
- 内容检查：
  - `benchmark_all_models_canonical.json` 顶层模型包括 `SC-INR-EQ`。
  - `SC-INR-EQ` seed1 summary 为 ID `31.151333`、OOD `22.777929`、ALL `25.569064`。
  - `benchmark_paper_main_3seed.json` 只含 `LIIF`、`LTE`、`SC-INR`。
  - `paper_context_seed1.csv` 仍包含 `SC-INR-EQ` context row。
- 断链检查：
  - `find . -xtype l` 无输出。
- 旧引用扫描：
  - active docs/scripts 不再引用 `results/benchmark.json`、`results/benchmark_seed1_with_signed_phiz.json`、
    `results/seeds/benchmark_seed*.json` 或 `phase_intervention.json` 作为入口；只有
    `artifacts/results/README_zh.md` 明确说明这些旧入口已删除。
- 清理 `__pycache__` 后未残留生成缓存。

证据支持的结论：

- checkpoint/save 层现在只保留新命名实体目录；旧命名只存在于 raw-key/provenance 文本中，不再作为可打开的模型目录。
- benchmark 读者现在可以从 `results/benchmark_all_models_canonical.json` 直接看到 canonical 模型名和
  `SC-INR-EQ`，不需要打开含旧 raw key 的 `benchmark_signed_phiz.json`。
- 论文主模型和全模型/context 已分离：主三 seed JSON 只含 LIIF/LTE/SC-INR，全模型 JSON 含
  NoPhi、NoSinc、LTE diagnostics、EQ 和 `SC-INR-EQ`。

未解决风险：

- raw JSON 文件名和 raw key 仍是历史状态，这是保留 provenance 的有意选择；不能直接从 raw JSON 写论文表。
- git status 中新命名配置显示为 type-change，是因为原先新命名文件是 symlink，现在替换为实体 YAML；
  这是预期的命名迁移结果。
- 旧 raw filenames 仍可能在用户 IDE 历史标签页中显示；真正入口已从 `results/` 删除。

下一步：

- 后续论文表格只读 `artifacts/derived/benchmarks/` 或 `results/benchmark_paper_main_3seed.json`。
- 如继续健康治理，可处理 `eval_continuous.py` 的旧式输出路径；本轮未改该入口的行为。

专家评审：

可靠结论：本轮完成的是命名和证据入口收敛，且有路径、编译、JSON、benchmark 重建和模型发现验证。
这直接解决了用户指出的 save 新老模型共存、benchmark 文件名/内容误导、`SC-INR-EQ` 没有进入便捷入口的问题。

证据不足：这不是新的 benchmark 实验；新增 JSON 是从既有 raw results 派生，不改变科学结果。

下一步是否合理：优先把论文和后续分析切到 canonical JSON/CSV 是合理的；不建议回头改写 raw JSON key。

需要防止过度宣称：不能把 `SC-INR-EQ` 因进入全模型 JSON 就说成主方法；它仍只属于 seed1 exploratory/context。
