# 2026-05-11

## SC-INR-EQ 运行复核与 final SC-INR 多 seed benchmark 补齐

完成范围：

- 复核正在运行的 exploratory `SC-INR-EQ`：
  - 源码层面确认 `coef(z)`、`omega(z)`、`phi(z)` 均由 encoder feature 生成；
  - `cell/scale` 只在 `_basis_and_response` 中进入 analytic sinc response；
  - 未引入 `LTE-EQ` 的 learned `phase(cell)`。
- 补齐 final-candidate `SC-INR` seed2/seed3 full benchmark，并合并 seed1/2/3
  形成 3-seed 汇总。
- 更新 claim/evidence 入口、项目状态、模型 registry、论文草稿和表格规划产物，使后续论文写作不再沿用
  “final SC-INR 只有 seed1 preliminary” 的过期状态。

改动文件或产物：

- 新增/更新分析产物：
  - `artifacts/raw_results/seed2/benchmark_sc_inr.json`
  - `artifacts/raw_results/seed3/benchmark_sc_inr.json`
  - `artifacts/derived/analysis/benchmark_progress_2026-05-11/`
  - `artifacts/derived/paper_tables/current_evidence_2026-05-11/`
- 更新项目/论文状态：
  - `paper/ARTIFACTS_ALLOWED.md`
  - `paper/claims_evidence_matrix.md`
  - `paper/model_taxonomy.md`
  - `paper/draft/main.tex`
  - `paper/draft/README_zh.md`
  - `docs/project/current_state.md`
  - `docs/project/project_story.md`
  - `docs/project/SC-INR_Exp.md`
  - `configs/registry/models.yaml`
  - `configs/registry/protocols.yaml`
  - `memory/task_ledger.md`

运行过的命令/测试/分析及结果：

- `SC-INR-EQ` smoke gate：
  - `CUDA_DEVICE_ORDER=PCI_BUS_ID CUDA_VISIBLE_DEVICES=2 python scripts/analysis/smoke_check_sc_inr_eq.py --device cuda:0`
  - 结果：omega init max abs error `7.450581e-09`；forward/backward finite；
    scale cells x2/x4/x8/x16/x30 finite；`response_cell_delta=3.166991e-01`；
    smoke passed。
- `SC-INR-EQ` 运行状态：
  - tmux: `scinr_eq_seed1_20260510`
  - latest checked log: `epoch 326/1000, train: loss=0.0311, val: psnr=30.2522`
  - 当前无 NaN/inf/traceback；训练可继续。
- final `SC-INR` seed2/seed3 benchmark 覆盖检查：
  - `artifacts/raw_results/seed2/benchmark_sc_inr.json`: 36/36 finite，无 missing/None。
  - `artifacts/raw_results/seed3/benchmark_sc_inr.json`: 36/36 finite，无 missing/None。
- paper draft 编译：
  - 在 `paper/draft` 下运行 `pdflatex`、`bibtex`、`pdflatex`、`pdflatex`。
  - 最终 `rg -n "Warning|Error|Undefined|Rerun" main.log` 未发现实际 warning/error。

证据支持的结论：

- final `SC-INR` 3-seed benchmark 已完成：
  - ID `31.0738 ± 0.0582`
  - OOD `22.7578 ± 0.0316`
  - ALL `25.5298 ± 0.0403`
- paired delta vs `LTE`：
  - ID `-0.0048 ± 0.0836`
  - OOD `+0.0504 ± 0.0417`
  - ALL `+0.0320 ± 0.0555`
- paired delta vs `SC-INR-NoPhi`：
  - ID `-0.0007 ± 0.0752`
  - OOD `-0.0003 ± 0.0407`
  - ALL `-0.0004 ± 0.0519`
- 因此，final `SC-INR` 保留了相对 LTE 的 OOD/ALL 均值优势，但 feature-conditioned
  phase 相对 `SC-INR-NoPhi` 在 benchmark PSNR 上基本打平，不能写成稳定增益项。

未解决风险和下一步：

- `SC-INR-EQ` 目前只能说训练正常、工程 smoke 通过、源码 contract 没有明显违反；不能说旋转等变语义或 analytic footprint 积分已经完全正确。
- `SC-INR-EQ` 需要补属性测试：
  - cell 变化不得改变 `coef/omega/phi`，只改变 response；
  - `phase_map` 对不同 cell 不变；
  - 旋转输入时 transform 通道按预期循环置换；
  - analytic sinc response 与直接 cell 平均积分对齐。
- 论文主线下一步应收敛表格和解释，而不是继续扩新分支；尤其要把 final `SC-INR`
  相对 `SC-INR-NoPhi` 的中性结果写清楚。

专家评审：

可靠结论：final `SC-INR` 的 3-seed benchmark 覆盖完整，raw key/provenance 已单独记录，和旧 ambiguous `SC-INR` 结果隔离；相对 LTE 的 OOD/ALL 正均值、相对 `SC-INR-NoPhi` 基本打平这两个判断有直接表格支持。`SC-INR-EQ` 当前可继续跑，没有看到需要停训的工程错误。

证据不足：`SC-INR-EQ` 的数学正确性还没有被属性测试证明；训练 loss/PSNR 正常不能证明旋转符号、通道 flatten 顺序、`eff_x/eff_y` 和 axis-aligned cell sinc 积分语义正确。final `SC-INR` 的 auxiliary/qualitative 仍主要是 seed1，不能把三 seed PSNR 扩展为所有指标结论。

下一步是否合理：优先完成论文表格、claim ledger 和 footprint correctness 诊断是合理的；继续扩大 descriptor、metric 或新模型分支前应先有明确 gate。需要防止过度宣称：不要写 strict scale equivariance，不要写 feature phase 稳定提升 NoPhi，不要把 selected qualitative examples 写成平均视觉质量证明。

## SC-INR 展示口径修正与 SC-INR-EQ 日志恢复

完成范围：

- 根据用户修正，调整 final `SC-INR` 论文展示口径：
  - `SC-INR` vs `SC-INR-NoPhi` 只展示 seed1 结构增量；
  - seed2/seed3 和三 seed 主表不展示这两个内部变体差异；
  - final `SC-INR` 三 seed主展示改为相对 `LIIF` 和 `LTE` 的差距。
- 修复 `save/sc-inr-eq/log.txt` 只从 epoch 318 开始的问题。

改动文件或产物：

- 新增 paper-facing 表：
  - `artifacts/derived/analysis/benchmark_progress_2026-05-11/final_sc_inr_vs_liif_lte_paper.csv`
- 更新：
  - `artifacts/derived/analysis/benchmark_progress_2026-05-11/README_zh.md`
  - `artifacts/derived/paper_tables/current_evidence_2026-05-11/current_evidence_table.csv`
  - `artifacts/derived/paper_tables/current_evidence_2026-05-11/README_zh.md`
  - `paper/draft/main.tex`
  - `paper/draft/main.pdf`
  - `paper/ARTIFACTS_ALLOWED.md`
  - `paper/claims_evidence_matrix.md`
  - `paper/model_taxonomy.md`
  - `docs/project/current_state.md`
  - `docs/project/project_story.md`
  - `docs/project/SC-INR_Exp.md`
  - `configs/registry/models.yaml`
  - `memory/task_ledger.md`
- 日志备份：
  - `artifacts/checkpoints/seed1/sc-inr-eq/log.txt.truncated_backup_20260511_034100.bak`

运行过的命令/测试/分析及结果：

- 从 `artifacts/checkpoints/seed1/sc-inr-eq/stdout.log` 提取 header 和 epoch 摘要，
  恢复 `artifacts/checkpoints/seed1/sc-inr-eq/log.txt`。
- 验证修复后 `save/sc-inr-eq/log.txt`：
  - epoch 数 `334`
  - first `1`
  - last `334`
  - missing `0`
  - duplicates `0`
  - 最新追加到 `epoch 334/1000, train: loss=0.0307, val: psnr=29.8404`
- 重新编译 `paper/draft/main.tex`，最终 `main.log` 未见实际 LaTeX error/undefined reference；
  `rg` 只匹配到 rerunfilecheck 包自身说明行。

证据支持的结论：

- `SC-INR` 三 seed paper-facing 表现在是：
  - ID `31.0738 ± 0.0582`，vs LIIF `+0.0437`，vs LTE `-0.0048`
  - OOD `22.7578 ± 0.0316`，vs LIIF `+0.0328`，vs LTE `+0.0504`
  - ALL `25.5298 ± 0.0403`，vs LIIF `+0.0364`，vs LTE `+0.0320`
- `SC-INR` vs `SC-INR-NoPhi` 的展示位置应是 seed1：
  - OOD `+0.0295`
  - ALL `+0.0421`

未解决风险和下一步：

- `stdout.log` 仍包含大量 `tqdm` 进度输出，不适合人工阅读；当前已恢复 `log.txt`，
  后续监控应优先看 `log.txt`。
- 若后续再次发现 `log.txt` 被截断，应优先检查是否有人为编辑或外部工具覆盖；
  训练脚本自身的 `utils.log` 是 append 写法。

专家评审：

可靠结论：展示口径已经按用户要求修正，三 seed 主表现在聚焦 LIIF/LTE；`SC-INR-EQ`
日志已从完整 stdout 恢复，epoch 1-334 连续无缺失，训练仍在追加。

证据不足：`SC-INR-EQ` 的“准确无误”仍只能在工程合同层面说未发现错误；Rot-E
通道置换和 analytic sinc 在旋转坐标下的严格语义还需要属性测试。这里的谨慎不是说模型错了，
而是区分“按 LTE-EQ 经验已处理主要项”和“已经由测试证明所有数学约定无误”。

下一步是否合理：继续训练 `SC-INR-EQ` 是合理的；后续应补最小属性测试，而不是在训练正常时
频繁改模型。论文侧应按 LIIF/LTE 主比较收敛表格。
