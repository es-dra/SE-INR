# 2026-05-10

## SC-INR-NoSinc benchmark 补评与记录更新

### 完成范围

- 核查 `artifacts/checkpoints/seed1/sc-inr-nosinc/log.txt`，训练已完成到 `epoch 1000/1000`。
- 核查 checkpoint：`epoch-best.pth`、`epoch-last.pth` 均存在；本次 benchmark 使用 `epoch-best.pth`。
- 使用正式入口补跑 `SC-INR-NoSinc` seed1 完整 benchmark，不覆盖既有结果。
- 生成新的 dated derived summary，并更新 NoSinc experiment card、model taxonomy、registry 和 claim ledger。
- 按主流程内模拟独立视角审查实验解释：实验设计视角认为该消融是合理的机制对照；证据审计视角反对把单 seed PSNR 差异直接写成“sinc 唯一因果证明”。

### 运行命令与产物

- GPU/进程核查：
  - `nvidia-smi`
  - `pgrep -af "train.py|eval_full.py|eval_continuous.py|evaluate_seed1_aux_metrics|python"`
- NoSinc benchmark：
  - `CUDA_DEVICE_ORDER=PCI_BUS_ID CUDA_VISIBLE_DEVICES=2 python entrypoints/eval_full.py --device 0 --save_root artifacts/checkpoints/seed1 --models SC-INR-NoSinc --output artifacts/raw_results/seed1/benchmark_sc_inr_nosinc.json`
- 派生汇总：
  - `artifacts/derived/analysis/benchmark_progress_2026-05-10/seed1_signed_phiz_nosinc_summary.csv`
  - `artifacts/derived/analysis/benchmark_progress_2026-05-10/seed1_nosinc_per_scale_delta.csv`
  - `artifacts/derived/analysis/benchmark_progress_2026-05-10/seed1_signed_phiz_nosinc_provenance.json`
- 校验：
  - `configs/registry/models.yaml` 可由 `yaml.safe_load` 解析。
  - `benchmark_sc_inr_nosinc.json` 覆盖 4 个 dataset × 9 个 scale，共 36 项，无 `None`。

### 结果摘要

- `SC-INR-NoSinc` seed1 benchmark：
  - ID PSNR：`30.9790`
  - OOD PSNR：`22.7299`
  - All PSNR：`25.4796`
- 相对 `LTE`：
  - ID：`-0.0659 dB`
  - OOD：`+0.0382 dB`
  - All：`+0.0035 dB`
- 相对完整 `SC-INR`：
  - ID：`-0.1397 dB`
  - OOD：`-0.0467 dB`
  - All：`-0.0777 dB`
- 相对 `SC-INR-NoPhi`：
  - ID：`-0.0724 dB`
  - OOD：`-0.0172 dB`
  - All：`-0.0356 dB`

### 模型差异记录

- 完整 `SC-INR` 使用 registry name `sc_inr_signed_phiz`，结构为 signed bounded $\omega(z)$ + feature-conditioned $\phi(z)$ + analytic sinc response。
- `SC-INR-NoSinc` 使用 registry name `sc_inr_nosinc`，继承 `SCINRSignedPhiZ`，因此保留 signed bounded $\omega(z)$ 和 feature-conditioned $\phi(z)$。
- 唯一机制调整是 `use_sinc_response=False`：代码中将 Fourier response 权重从
  $W=\operatorname{sinc}(\omega_h c_h/2)\operatorname{sinc}(\omega_w c_w/2)$
  替换为 $W=1$。
- local ensemble、coordinate/cell 单位、encoder、coef、omega、phase、MLP 和 residual upinput 路径保持不变。

### 证据支持的结论

- seed1 benchmark 显示：移除 sinc 后，最终候选相对完整 `SC-INR` 明显变弱，尤其 ID 和 All 平均下降更明显。
- NoSinc 的 OOD 平均仍略高于 `LTE`，说明 signed $\omega(z)$ + $\phi(z)$ 本身也有贡献，不能把全部收益归因到 sinc。
- 当前更稳妥的论文表述是：NoSinc 消融提示 analytic observation response 对最终候选有贡献；但需要 auxiliary consistency 和多 seed 才能形成更强机制证据。

### 未解决风险与下一步

- `SC-INR-NoSinc` 还缺 same-LR consistency、PSNR-Y/SSIM-Y、texture/highpass RMSE；这些指标比 PSNR 更直接检验 sampling-consistency 机制。
- `SC-INR` final candidate 仍缺 seed2/seed3，不能把 seed1 NoSinc 差异写成最终统计结论。
- 后续建议优先运行 `SC-INR-NoSinc` auxiliary metrics，直接和 `LTE`、`SC-INR-NoPhi`、`SC-INR-NoPhi-Signed`、`SC-INR` 比较；若 NoSinc consistency 显著退化，机制证据会更完整。

### 专家评审

- 可靠结论：NoSinc 训练完成、完整 benchmark 已补齐且 36 项无缺失；实现差异确认为只关闭 analytic sinc response，未改 signed $\omega(z)$ 和 $\phi(z)$。
- 证据不足：当前只有 seed1 PSNR benchmark，不能证明 sinc 是唯一原因，也不能证明 multi-seed 统计稳定。
- 下一步是否合理：补 NoSinc auxiliary consistency 是最直接的机制验证；final `SC-INR` seed2/seed3 仍应作为后续统计闭环。
- 需要防止过度宣称：不要写 strict scale equivariance；不要把 NoSinc 单 seed 下滑写成严格因果证明；不要把 OOD 小幅差异脱离 variance 和 protocol 解释。

## SC-INR-NoSinc auxiliary metrics 补齐与机制解释修正

### 完成范围

- 复核仓库状态、GPU/后台进程状态、final `SC-INR` seed1 auxiliary 目录和
  `scripts/analysis/evaluate_seed1_aux_metrics.py`。
- 确认 `artifacts/derived/analysis/sc_inr_final_aux_metrics_seed1/` 覆盖
  `LTE`、`SC-INR-NoPhi`、`SC-INR-NoPhi-Signed`、`SC-INR`，不含
  `SC-INR-NoSinc`；协议为 BSD100/Urban100 前 10 张、x4/x8/x16/x30、
  x8/x16/x30 -> x4。
- 使用同一正式脚本补跑 `SC-INR-NoSinc` auxiliary metrics，输出到新目录，
  未覆盖既有 final aux 目录。
- 更新 NoSinc auxiliary README、model registry、model taxonomy、claim ledger、
  NoSinc experiment card 和技术记录。
- 采用真实只读 subagent 复核协议覆盖、脚本复用风险和最强反对意见；结论是
  `--out` 会覆盖同名文件，必须使用新目录；same-LR consistency 只是 decoder
  self-consistency，不等价于严格 equivariance 或正确 footprint observation。

### 运行命令与产物

- GPU/进程核查：
  - `nvidia-smi`
  - `ps -eo pid,ppid,stat,etime,pcpu,pmem,args | rg -i 'python|train|benchmark|eval|torch|cuda'`
- NoSinc auxiliary metrics：
  - `CUDA_DEVICE_ORDER=PCI_BUS_ID CUDA_VISIBLE_DEVICES=2 python scripts/analysis/evaluate_seed1_aux_metrics.py --models LTE,SC-INR-NoPhi,SC-INR-NoPhi-Signed,SC-INR,SC-INR-NoSinc --datasets bsd100,urban100 --scales 4,8,16,30 --consistency_pairs '8,4;16,4;30,4' --max_images 10 --device cuda:0 --eval_bsize 50000 --out artifacts/derived/analysis/sc_inr_nosinc_aux_metrics_seed1 --skip_visuals`
- 新产物：
  - `artifacts/derived/analysis/sc_inr_nosinc_aux_metrics_seed1/quality_metrics.csv`
  - `artifacts/derived/analysis/sc_inr_nosinc_aux_metrics_seed1/quality_summary.csv`
  - `artifacts/derived/analysis/sc_inr_nosinc_aux_metrics_seed1/consistency_metrics.csv`
  - `artifacts/derived/analysis/sc_inr_nosinc_aux_metrics_seed1/consistency_summary.csv`
  - `artifacts/derived/analysis/sc_inr_nosinc_aux_metrics_seed1/README_zh.md`
  - `artifacts/derived/analysis/sc_inr_nosinc_aux_metrics_seed1/figures/*.png`
- 更新文件：
  - `configs/registry/models.yaml`
  - `paper/ARTIFACTS_ALLOWED.md`
  - `paper/model_taxonomy.md`
  - `paper/claims_evidence_matrix.md`
  - `experiments/nosinc/experiment_card.md`
  - `docs/project/SC-INR_Exp.md`
- 校验：
  - `quality_metrics.csv` 为 480 条数据行，`consistency_metrics.csv` 为 300 条数据行。
  - 模型、dataset、scale、consistency pair 覆盖均符合冻结协议。
  - 共享模型 summary 与 `sc_inr_final_aux_metrics_seed1` 完全一致，最大差异为 0。
  - `configs/registry/models.yaml` 可由 `yaml.safe_load` 解析。
  - 文档中未再检出 NoSinc auxiliary pending 的旧表述。

### 结果摘要

- OOD PSNR-Y（BSD100/Urban100 前 10 张，x8/x16/x30 平均）：
  - BSD100：`SC-INR-NoSinc` 22.8495，`SC-INR` 22.8571，delta `-0.0076 dB`。
  - Urban100：`SC-INR-NoSinc` 20.6072，`SC-INR` 20.5864，delta `+0.0208 dB`。
  - All：`SC-INR-NoSinc` 21.7284，`SC-INR` 21.7218，delta `+0.0066 dB`。
- OOD texture RMSE-Y（越低越好，All average）：
  - `SC-INR-NoSinc` 0.152998，`SC-INR` 0.153208，delta `-0.000210`。
- Same-LR consistency PSNR-Y（x8/x16/x30 -> x4 平均）：
  - BSD100：`SC-INR-NoSinc` 69.0870，`SC-INR` 52.1421，delta `+16.9448 dB`。
  - Urban100：`SC-INR-NoSinc` 64.6241，`SC-INR` 47.1128，delta `+17.5112 dB`。
  - All：`SC-INR-NoSinc` 66.8555，`SC-INR` 49.6275，delta `+17.2280 dB`。

### 证据支持的结论

- NoSinc auxiliary metrics 不支持“移除 sinc 会破坏 same-LR self-consistency”。
  结果相反：NoSinc 的 self-consistency 大幅高于完整 `SC-INR`。
- 更合理的解释是：关闭 sinc 后，decoder 几乎失去 cell-dependent analytic
  observation response，更接近 cell-independent point-function；当前 same-LR
  consistency 指标会奖励这种自一致性，但没有验证 footprint observation 是否正确。
- full benchmark 仍提示 sinc 对最终候选 PSNR 有贡献：NoSinc 相对完整 `SC-INR`
  的 seed1 benchmark OOD/All 分别低 `0.0467/0.0777 dB`。
- 因此 claim ledger 已调整为：sinc 支持主要来自 full benchmark/fidelity 证据；
  current same-LR self-consistency 不能作为 sinc 改善 consistency 的证据。

### 未解决风险与下一步

- 当前 auxiliary 覆盖仍只是 BSD100/Urban100 前 10 张、seed1，不能替代 full benchmark
  或 multi-seed 结论。
- same-LR consistency 指标暴露出判别限制；下一步需要 response/omega diagnostics，
  或更直接区分 point self-consistency 与 footprint observation correctness 的诊断。
- final `SC-INR` 仍缺 seed2/seed3，不能把 seed1 PhiZ 和 NoSinc 结果写成最终统计结论。
- qualitative figure 仍应只使用用户确认的 selected examples，不能扩展为平均视觉质量证明。

### 专家评审

- 可靠结论：NoSinc auxiliary 指标已按冻结协议完成；共享模型复算与旧 final aux
  结果完全一致；NoSinc 的 same-LR self-consistency 在当前指标下显著高于完整
  `SC-INR`。
- 证据不足：该结果不能证明 NoSinc 的 observation model 更正确，也不能推翻 sinc
  在 full benchmark 上对 PSNR 的贡献；它主要说明现有 consistency 指标有偏好
  cell-independent decoder 的风险。
- 下一步是否合理：优先补 response/omega diagnostics 和 final `SC-INR` 多 seed；
  在论文中把 NoSinc aux 写成机制诊断 caveat，而不是正向主证据。
- 需要防止过度宣称：不要说 NoSinc 破坏 sampling consistency；不要说高
  same-LR consistency 等价于更好的 scale sampling consistency；不要说 sinc
  已被证明必要或唯一因果。

## response/omega diagnostics 补充与 token/执行效率修正

### 完成范围

- 回应用户关于 token 高效利用的问题：本轮继续采用精准读取、复用已有脚本、
  长结果落盘、对话只汇报关键数值的策略。
- 扩展 `scripts/analysis/analyze_sampling_response.py`，新增：
  - `active_response_*`，区分模型实际使用的 response 与按 omega 计算出的 hypothetical
    analytic sinc response。
  - `analytic_attenuation_*` 和 `active_attenuation_*`。
  - 多图像 cell-only 曲线汇总。
  - `run_config.json` 和 summary CSV 输出。
- 首次运行时发现 `torch.quantile` 对大 tensor 的 CPU 开销过大，主动中断未完成进程；
  将统计采样上限从 200k 降到 20k，并对常数 tensor 跳过 quantile，然后重跑。
- 完成 response/omega 机制诊断并更新：
  - `paper/claims_evidence_matrix.md`
  - `paper/ARTIFACTS_ALLOWED.md`
  - `experiments/nosinc/experiment_card.md`
  - `docs/project/SC-INR_Exp.md`

### 运行命令与产物

- GPU/进程核查：
  - `nvidia-smi --query-gpu=index,name,memory.used,utilization.gpu --format=csv,noheader`
  - `ps -eo pid,ppid,stat,etime,pcpu,pmem,args | rg -i 'entrypoints/train|eval_full|evaluate_seed1_aux_metrics|analyze_sampling_response|torchrun'`
- 最终诊断命令：
  - `CUDA_DEVICE_ORDER=PCI_BUS_ID CUDA_VISIBLE_DEVICES=2 python scripts/analysis/analyze_sampling_response.py --mode both --models SC-INR-NoPhi,SC-INR-NoPhi-Signed,SC-INR,SC-INR-NoSinc --datasets bsd100,urban100 --max_images 5 --lr_scale 4 --scales 4,8,16,30 --cell_models LTE,SC-INR-NoPhi,SC-INR-NoPhi-Signed,SC-INR,SC-INR-NoSinc --cell_datasets bsd100,urban100 --cell_image '' --cell_max_images 5 --cell_lr_scale 4 --cell_ref_scale 4 --cell_scales 4,6,8,12,16,24,30 --max_queries 4096 --eval_bsize 50000 --device cuda:0 --out artifacts/derived/analysis/response_omega_diagnostics_2026-05-10`
- 新产物：
  - `artifacts/derived/analysis/response_omega_diagnostics_2026-05-10/README_zh.md`
  - `artifacts/derived/analysis/response_omega_diagnostics_2026-05-10/response_distribution.csv`
  - `artifacts/derived/analysis/response_omega_diagnostics_2026-05-10/response_distribution_summary.csv`
  - `artifacts/derived/analysis/response_omega_diagnostics_2026-05-10/cell_response_curve.csv`
  - `artifacts/derived/analysis/response_omega_diagnostics_2026-05-10/cell_response_curve_summary.csv`
  - `artifacts/derived/analysis/response_omega_diagnostics_2026-05-10/cell_response_curve_by_dataset.csv`
  - `artifacts/derived/analysis/response_omega_diagnostics_2026-05-10/figures/*.png`
- 校验：
  - `python -m py_compile scripts/analysis/analyze_sampling_response.py`
  - `response_distribution.csv` 160 条数据行。
  - `response_distribution_summary.csv` 16 条数据行。
  - `cell_response_curve.csv` 350 条数据行。
  - `cell_response_curve_summary.csv` 35 条数据行。
  - `cell_response_curve_by_dataset.csv` 70 条数据行。
  - 任务结束后未发现仍在运行的 analysis/eval/train 进程。

### 结果摘要

- Active attenuation mean：
  - `SC-INR`：x4 `0.040495`，x8 `0.010570`，x16 `0.002672`，x30 `0.000762`。
  - `SC-INR-NoPhi`：x4 `0.037106`，x8 `0.009583`，x16 `0.002415`，x30 `0.000688`。
  - `SC-INR-NoPhi-Signed`：x4 `0.036853`，x8 `0.009494`，x16 `0.002392`，x30 `0.000681`。
  - `SC-INR-NoSinc`：x4/x8/x16/x30 均为 `0.000000`。
- Cell-only RMSE-Y vs x4 cell：
  - `LTE`：x8 `0.005267`，x16 `0.008329`，x30 `0.010233`，x8/x16/x30 avg `0.007943`。
  - `SC-INR`：x8 `0.002854`，x16 `0.003395`，x30 `0.003515`，avg `0.003255`。
  - `SC-INR-NoPhi`：avg `0.003308`。
  - `SC-INR-NoPhi-Signed`：avg `0.003055`。
  - `SC-INR-NoSinc`：所有测试尺度均为 `0.000000`。
- Omega 观察：
  - `SC-INR` signed omega negative fraction `0.3951`。
  - `SC-INR-NoSinc` signed omega negative fraction `0.3960`。
  - `SC-INR-NoSinc` near-bound fraction `0.0135`，高于 `SC-INR` 的 `0.000084`，但当前只作为待监控优化差异。

### 证据支持的结论

- `SC-INR-NoSinc` 的高 same-LR self-consistency 可由 zero active cell response
  解释：它实际使用 `W=1`，所以同一坐标下改变 cell 不改变输出。
- 完整 `SC-INR` 不是 cell-independent；它保留非零 analytic attenuation 和非零
  cell-only output sensitivity。
- `SC-INR` 的 cell-only sensitivity 明显低于 LTE，符合“移除 learned
  cell-conditioned phase，改为解析 response”的机制预期。
- 该诊断加强了 claim ledger 中的 caveat：same-LR consistency 不是 standalone
  proof，必须结合 fidelity、response/omega 和 full benchmark。

### 未解决风险与下一步

- 当前 response/omega 诊断是小样本机制诊断，只覆盖两数据集前 5 张和最多 4096
  query points，不能替代 benchmark。
- active attenuation 在高倍率 x16/x30 数值很小，说明 sinc response 对 OOD 高倍
  的直接幅度影响可能弱；其 PSNR 贡献可能通过训练动态、x4/ID regularization、
  或与 phase/omega 的交互体现，需要更谨慎解释。
- `SC-INR-NoSinc` near-bound fraction 较高，后续可检查 omega 分布图或训练日志，
  但不能把它直接写成失败原因。
- 下一步若继续机制闭环，应优先设计 footprint correctness 指标，而不是继续堆叠
  same-LR self-consistency。

### 专家评审

- 可靠结论：NoSinc 的 zero active response 和 zero cell-only RMSE 是实现机制的
  直接结果；它解释了 NoSinc self-consistency 虚高。
- 证据不足：该诊断不能证明 sinc 是唯一因果，也不能单独解释 full benchmark 的
  PSNR 差异；尤其高倍率 active attenuation 很小，需要避免过度机制化叙事。
- 下一步是否合理：继续补 final `SC-INR` 多 seed 和更直接的 footprint correctness
  指标是合理的；不建议把 same-LR consistency 继续作为 sinc 消融的主要正证据。
- 需要防止过度宣称：不要把 cell-only sensitivity 小写成严格 scale equivariance；
  不要把 NoSinc zero sensitivity 写成更好；不要把小样本 omega near-bound 差异写成
  已证实失败机制。

## 工作流优化、SC-INR seed2/seed3 启动与论文候选产物推进

### 完成范围

- 新增短恢复入口 `docs/project/current_state.md`，用于后续对话优先恢复当前事实、
  claim 边界、当前缺口和下一步 gate，减少重复读取长日志/长文档。
- 新增长任务账本 `memory/task_ledger.md`，记录 seed2/seed3 训练的目标、允许范围、
  禁止事项、资源预算、命令、PID、日志和暂停条件。
- 更新 `docs/project/project_story.md`，修正旧的 2026-05-09 状态：final `SC-INR`
  auxiliary、NoSinc benchmark/auxiliary、response diagnostics 已完成；seed2/seed3
  已启动但未完成。
- 在物理 GPU 2/3 上启动 final `SC-INR` seed2/seed3 训练。
- 后台训练期间，重新导出用户确认的两张 qualitative selected figures，并生成当前
  evidence table planning artifact。
- 更新 `paper/ARTIFACTS_ALLOWED.md` 和 `paper/claims_evidence_matrix.md` 中的
  qualitative 路径与局部指标。

### 运行命令与产物

- GPU/进程核查：
  - `nvidia-smi`
  - `ps -eo pid,ppid,stat,etime,pcpu,pmem,args | rg -i 'python|train|eval|torchrun|cuda'`
- seed2 训练：
  - tmux session: `scinr_seed2_20260510`
  - PID: `64956`
  - command: `CUDA_DEVICE_ORDER=PCI_BUS_ID CUDA_VISIBLE_DEVICES=2 python entrypoints/train.py --config configs/train-div2k/train-sc-inr.yaml --name sc-inr --saveFolder artifacts/checkpoints/seed2 --seed 2`
  - output: `artifacts/checkpoints/seed2/sc-inr/`
- seed3 训练：
  - tmux session: `scinr_seed3_20260510`
  - PID: `64959`
  - command: `CUDA_DEVICE_ORDER=PCI_BUS_ID CUDA_VISIBLE_DEVICES=3 python entrypoints/train.py --config configs/train-div2k/train-sc-inr.yaml --name sc-inr --saveFolder artifacts/checkpoints/seed3 --seed 3`
  - output: `artifacts/checkpoints/seed3/sc-inr/`
- qualitative selected figures：
  - `artifacts/derived/paper_figures/qualitative_selected_seed1/urban100_img004_x8_selected_gt_bicubic_lte_nophi_scinr.png`
  - `artifacts/derived/paper_figures/qualitative_selected_seed1/urban100_img012_x8_selected_gt_bicubic_lte_nophi_scinr.png`
  - 对应 `.pdf`、`_metrics.csv`、`_config.json` 和 `README_zh.md`
- evidence table planning：
  - `artifacts/derived/paper_tables/current_evidence_2026-05-10/current_evidence_table.csv`
  - `artifacts/derived/paper_tables/current_evidence_2026-05-10/README_zh.md`
  - `artifacts/derived/paper_tables/current_evidence_2026-05-10/provenance.json`

### 训练状态

- seed2/seed3 均已进入正式训练，已写出 `config.yaml`、`log.txt` 和 `stdout.log`。
- 最近检查：
  - seed2 到 `epoch 6/1000`：train loss `0.0338`，val PSNR `29.5979`，ETA 约 `12.0h`。
  - seed3 到 `epoch 6/1000`：train loss `0.0338`，val PSNR `29.2746`，ETA 约 `12.1h`。
  - GPU 2/3 memory 约 `2790 MiB`，utilization 约 `95%/95%`。
  - 两个输出目录均已产生 `epoch-ini.pth`、`epoch-last.pth`、`epoch-best.pth`。

### qualitative 结果摘要

- `urban100_img004_x8` crop `(144,912,96)`：
  - Bicubic `16.2966`
  - LTE `19.0226`
  - `SC-INR-NoPhi` `20.8467`
  - `SC-INR` `21.4880`
- `urban100_img012_x8` crop `(432,864,96)`：
  - Bicubic `18.8876`
  - LTE `20.4458`
  - `SC-INR-NoPhi` `19.6163`
  - `SC-INR` `23.2229`

### 证据支持的结论

- seed2/seed3 训练已经以 canonical `sc-inr` 名称和独立 seed 目录启动，未覆盖
  seed1 或旧 `sc-inr-adaptive` 目录。
- qualitative selected figures 已用当前命名重新导出，解决旧候选图中
  `SC-INR`/`SC-INR+PhiZ` 标签歧义。
- evidence table planning artifact 将当前多 seed core、seed1 final、NoSinc、
  auxiliary/diagnostic 和 selected qualitative 放到同一短表中，便于后续写 LaTeX
  时避免混淆 cite_ok、preliminary、diagnostic、candidate。

### 未解决风险与下一步

- seed2/seed3 训练仍在运行，不能提前写 multi-seed final `SC-INR` 结论。
- 两个训练预计还需约 12 小时，应继续监控 NaN、异常退出、GPU 占用和 checkpoint
  写入。
- qualitative 仍是 selected examples，不能写 average visual quality proof。
- evidence table planning 是汇总入口，不替代源 CSV；正式表格仍要回到 canonical
  artifacts 核对。
- seed2/seed3 完成后，下一步应运行 full benchmark 并生成 final `SC-INR`
  multi-seed summary，然后再更新 claim ledger。

### 专家评审

- 可靠结论：工作流恢复入口和长任务账本已落盘；seed2/seed3 训练已有效进入 epoch；
  qualitative selected figures 已按当前命名重新生成并有局部 PSNR 支持。
- 证据不足：seed2/seed3 目前只是 early training 状态，不能判断最终性能；qualitative
  仍不能替代平均指标。
- 下一步是否合理：在训练后台运行时推进 paper table planning 是合理的低风险并行工作；
  训练完成后必须优先 benchmark，再做论文主表更新。
- 需要防止过度宣称：不要把 running seed 写成 completed multi-seed；不要把 selected
  crops 写成整体视觉结论；不要把 planning table 当成新实验结果。

## 全局工作流规则补充修正

### 完成范围

- 用户指出上一轮只在项目内新增了恢复入口和任务账本，没有真正补充全局工作流。
- 已将该反馈抽象为跨项目规则，并写入 `/home/admin/.codex/AGENTS.md`。
- 同步更新 `memory/task_ledger.md` 的训练检查点。

### 写入的全局规则

- 当用户明确要求“优化工作流”、或指出当前行动只产生了项目内产物而没有改变工作流时，
  应视为对全局工作流更新的授权信号；若规则具有跨项目价值，应直接更新
  `$CODEX_HOME/AGENTS.md` 或对应全局规则文件，而不是只在项目内新建恢复文档、
  task ledger 或实验说明。
- 工作流优化必须形成闭环：最终回复应明确区分“已写入全局工作流的跨项目规则”和
  “仅写入项目目录的项目事实/产物”；如果没有写入全局规则，必须说明原因。
- 对 L2-L4 科研节点，完成后应维护一个短 context capsule 或等价恢复入口，内容只
  包含当前研究问题、冻结命名/协议、可信结论、禁止过度宣称、canonical artifacts、
  正在运行的任务和下一步 gate。
- context capsule 是项目事实层，不能被当作全局工作流优化本身；若一次任务暴露出
  可迁移的上下文管理规则，还必须同步到全局工作流。

### 训练状态补充

- seed2 latest: `epoch 23/1000, train: loss=0.0331, val: psnr=29.8818`。
- seed3 latest: `epoch 24/1000, train: loss=0.0331, val: psnr=29.9114`。
- `save-seeds/seed{2,3}/sc-inr/log.txt` 经 symlink 指向
  `artifacts/checkpoints/seed{2,3}/sc-inr/log.txt`。

### 专家评审

- 可靠结论：此次修正确实写入了全局工作流，而不是只写项目记忆；seed2/seed3
  训练仍正常推进。
- 证据不足：这只是工作流规则修正，不产生新的科研结果；不能改变当前论文证据强度。
- 下一步是否合理：继续监控 seed2/seed3，完成后 benchmark；后续若再发现可迁移
  流程缺口，应按新规则同步到全局工作流。
- 需要防止过度宣称：不要把项目内 context capsule 说成全局工作流优化；不要把训练
  running 状态写成已完成 multi-seed。

## 全局工作流硬触发点补充

### 完成范围

- 根据用户要求，将此前反思中提出的“硬触发点”抽象为跨项目科研协作规则。
- 已写入 `/home/admin/.codex/AGENTS.md`，没有只停留在项目内恢复入口或当前对话。
- 项目目录仅追加本条记忆；未修改模型代码、训练配置、实验结果或论文可引用 artifact。

### 写入的全局规则

- L2-L4 科研节点开始前必须给出最小 Node Contract，冻结科学问题、主/辅指标、
  成功/失败标准、禁止事后调整项、预期 artifact 和 subagent 需求判断。
- 影响论文表述、协议、主指标、新指标、模型解释或阶段推进的节点，最终回复和项目记忆
  必须写明 subagent 使用状态：真实 subagent、主流程内模拟独立视角或未启动，并说明理由。
- 关键决策前必须执行短角色 gate：工程师、科研工作者、审稿人、导师和学生视角；
  任一关键角色失败时，不推进强结论。
- 新指标、诊断字段和分析协议若要进入 claim、表格、主分析或后续实验基础，必须先通过
  Metric Validity Gate：positive control、negative control 和关键不变量/属性测试。
- 非简单科研节点结束时必须给出科学进展分类，区分推进主问题、补齐证据、暴露 caveat、
  negative result 或仅完成工程/记录维护。

### 运行命令与产物

- 读取与定位：
  - `nl -ba /home/admin/.codex/AGENTS.md | sed -n '1,130p'`
  - `tail -80 Equivariant-ASISR/memory/daily/2026-05-10.md`
  - `git -C Equivariant-ASISR status --short`
- 修改文件：
  - `/home/admin/.codex/AGENTS.md`
  - `Equivariant-ASISR/memory/daily/2026-05-10.md`
- 未运行训练、benchmark 或分析脚本；本次不产生新的科研数值。

### 证据支持的结论

- 此次补充是跨项目工作流规则，不属于 SC-INR 项目事实或实验结论。
- 硬触发点针对当前暴露的执行缺口：subagent 使用状态不够显式、角色切换偏后置、
  新指标缺少 validity gate、以及容易把执行完成误认为科学推进。

### 未解决风险与下一步

- 规则写入本身不能保证后续自动严格执行；下一次 L2-L4 科研节点应实际使用 Node
  Contract、subagent 状态说明、角色 gate 和科学进展分类来验证规则有效性。
- 当前 SC-INR 论文证据强度没有因本次工作流更新而改变；seed2/seed3 训练和后续
  benchmark 仍需按冻结协议完成。

### 专家评审

- 可靠结论：硬触发点已写入全局工作流，且明确区分为跨项目规则；项目内仅记录本次
  规则更新的追溯信息。
- 证据不足：尚未在新的实验或论文节点中实测这些 hard gate 的执行效果。
- 下一步是否合理：下一次 final `SC-INR` multi-seed benchmark 节点应首先使用
  Node Contract，并在结果汇报中明确 subagent 使用状态和科学进展分类。
- 需要防止过度宣称：不要把工作流规则更新写成科研贡献；不要把更完整的审查流程
  等同于更强实验结果。

## 发表阶段进展审查

### Node Contract

- 科学问题：当前 SC-INR 项目是否已经进入发表末期。
- 主指标：final `SC-INR` 多 seed 证据是否闭环、claim ledger 与 artifact 白名单是否一致、
  论文草稿是否同步当前证据。
- 辅助指标：seed2/seed3 训练进度、NoSinc/response diagnostics caveat、表格/图像准备度。
- 成功/失败标准：给出阶段判断、阻塞项和下一步 gate；禁止把 running seed、单 seed
  preliminary 或 diagnostic 结果写成最终论文结论。
- subagent 状态：未启动真实 subagent；当前系统/工具规则下，用户未明确要求委托
  subagent，因此采用主流程内模拟独立视角。

### 完成范围

- 读取 `docs/project/current_state.md`、`paper/ARTIFACTS_ALLOWED.md`、
  `paper/claims_evidence_matrix.md`、`memory/task_ledger.md` 和论文草稿。
- 检查 seed2/seed3 final `SC-INR` 训练日志、进程和 GPU 状态。
- 对发表阶段做主流程内多角色 gate：工程师、科研工作者、审稿人、导师和学生视角。
- 更新 `memory/task_ledger.md` 的训练检查点。

### 运行命令与产物

- Canonical 状态与证据：
  - `sed -n '1,180p' Equivariant-ASISR/docs/project/current_state.md`
  - `sed -n '1,220p' Equivariant-ASISR/paper/ARTIFACTS_ALLOWED.md`
  - `sed -n '1,260p' Equivariant-ASISR/paper/claims_evidence_matrix.md`
  - `sed -n '1,220p' Equivariant-ASISR/memory/task_ledger.md`
- 训练状态：
  - `date -u '+%Y-%m-%d %H:%M:%S UTC'`
  - `tail -50 Equivariant-ASISR/save-seeds/seed2/sc-inr/log.txt`
  - `tail -50 Equivariant-ASISR/save-seeds/seed3/sc-inr/log.txt`
  - `ps -eo pid,ppid,stat,etime,pcpu,pmem,args | rg -i 'entrypoints/train|eval_full|torchrun|python'`
  - `nvidia-smi --query-gpu=index,name,memory.used,utilization.gpu --format=csv,noheader`
  - `rg -n 'epoch (79|80|100|500|1000)/1000|nan|inf|Traceback|error' ...`
- 论文草稿：
  - `find Equivariant-ASISR/paper -maxdepth 2 -type f | sort`
  - `rg -n '^\\section|^\\subsection|^\\begin\\{table|^\\begin\\{figure|includegraphics|caption' Equivariant-ASISR/paper/draft/main.tex`
  - `rg -n 'TODO|TBD|placeholder|待|FIXME|\\cite\\{\\}|\\?\\?' Equivariant-ASISR/paper/draft/main.tex Equivariant-ASISR/paper/draft/README_zh.md`
  - `sed -n '1,270p' Equivariant-ASISR/paper/draft/main.tex`
- 修改文件：
  - `Equivariant-ASISR/memory/task_ledger.md`
  - `Equivariant-ASISR/memory/daily/2026-05-10.md`

### 进展审查结论

- 工程师视角：训练与产物组织总体健康。seed2 到 `epoch 79/1000`，seed3 到
  `epoch 80/1000`，GPU 2/3 正常占用，`epoch-last.pth` 正在更新，当前日志未匹配到
  `nan`/`inf`、`Traceback` 或 `error`。
- 科研工作者视角：核心研究对象已收敛为 decoder-side scale sampling consistency；
  但 final `SC-INR` 仍缺 seed2/seed3 完成后的 full benchmark，不能写成最终多 seed
  结论。
- 审稿人视角：NoSinc 和 response diagnostics 已经暴露 same-LR consistency 的指标偏差；
  这是重要 caveat，必须在论文中正面处理，不能只展示有利 consistency 表。
- 导师视角：项目已经进入“实验收敛末段”，不是早期探索；当前不应扩新消融，优先完成
  final `SC-INR` multi-seed benchmark、主表收敛和文本同步。
- 学生视角：当前故事可以讲清楚为“cell/scale 作为 analytic observation response，
  不宣称 whole-network strict equivariance”；但论文草稿仍滞后，Limitations 还写着
  final auxiliary 和 w/o-sinc ablation 待完成，需同步到最新证据。

### 阶段判断

- 距离发表已经进入后半程/实验收敛末段，但尚未进入真正的投稿末期。
- 仍阻塞投稿主结论的项目：final `SC-INR` seed2/seed3 训练完成、full benchmark、
  final multi-seed summary、claim ledger/artifact 白名单晋级、论文主表和 limitations
  同步。
- 论文草稿已可编译且结构存在，但目前是内部最小草稿；references 仍很薄，正文未吸收
  NoSinc auxiliary、response diagnostics 和最新 caveat，不应视为投稿接近完成。

### 科学进展分类

- 本节点主要是“暴露 caveat + 阶段审查 + 记录维护”，不是新的实验结果。
- 对主问题的推进来自明确了下一步 gate：完成 final `SC-INR` multi-seed 后，才能判断
  是否从实验收敛末段进入投稿末期。

### 未解决风险与下一步

- seed2/seed3 仍只完成约 8% epoch，训练还需继续监控，完成前不能更新 final
  multi-seed claim。
- draft 中旧表述需要后续集中修订，尤其 abstract、limitations 和 consistency 解释。
- footprint correctness 指标仍缺；若新增该指标，必须先通过 Metric Validity Gate，
  不能直接进入主证据。

### 专家评审

- 可靠结论：已有证据和文档足以说明项目不再是早期探索；final 多 seed 是当前最关键
  阻塞项；论文草稿存在但明显滞后于 artifact/claim ledger。
- 证据不足：seed2/seed3 未完成，无法判断 final `SC-INR` 是否在多 seed 下稳健优于
  LTE 或 `SC-INR-NoPhi`。
- 下一步是否合理：继续等待并监控 seed2/seed3，完成后立即运行 full benchmark，
  再进行 evidence audit 和论文文本同步，是当前最合理路径。
- 需要防止过度宣称：不要说已经进入投稿末期；不要把 seed1 final candidate 写成最终
  winner；不要把 same-LR consistency 单独包装成 sinc 正证据。

## subagent 长期授权规则修正

### 完成范围

- 用户指出：此前已要求 Codex 根据任务难易自主创建 subagent，并且每次至少创建一个
  subagent 监督工作进展；当前规则和执行解释把这一长期偏好弱化为“显式要求时才用”。
- 已将该偏好重新抽象为跨项目规则并写入 `/home/admin/.codex/AGENTS.md`。
- 本轮实际启动一个真实只读监督 subagent 审查规则修正，确认应把“至少一个监督/审查型
  subagent”写成非简单科研任务的默认下限，并保留系统/工具规则边界。

### 写入的全局规则

- 对非简单科研任务，用户长期授权 Codex 按任务难度、科研风险和可并行性自主创建真实
  subagent，并默认至少设置一个监督/审查型 subagent 跟踪工作进展、检查证据链和提出
  最强反对意见。
- 监督/审查型 subagent 的默认职责包括：检查任务是否仍在回答原始科学问题，是否遗漏
  验证、对照或停止条件，是否出现指标漂移、过度宣称、执行型局部最优、过度工程或记录
  膨胀，以及哪些证据会改变当前结论。
- L2-L4 非简单科研任务默认至少使用一个监督/审查型 subagent；简单低风险、不可并行、
  当前系统/工具规则不允许，或创建 subagent 会阻塞关键路径时，才可豁免。
- 如果未启动真实 subagent，必须明确说明原因；如果无法使用真实 subagent，应标注为
  “主流程内模拟独立视角”，不得称为独立验证。
- 全局规则不能覆盖更高优先级的系统消息、工具权限、模型运行环境或平台能力限制；
  subagent 要求均以前述约束允许为前提。

### 运行命令与产物

- 读取规则：
  - `nl -ba /home/admin/.codex/AGENTS.md | sed -n '22,72p'`
  - `tail -80 Equivariant-ASISR/memory/daily/2026-05-10.md`
- 使用真实 subagent：
  - `Wegener` 只读审查 `/home/admin/.codex/AGENTS.md` 的 subagent 规则清晰度。
- 修改文件：
  - `/home/admin/.codex/AGENTS.md`
  - `Equivariant-ASISR/memory/daily/2026-05-10.md`
- 未修改模型代码、训练配置、实验结果或论文可引用 artifact。

### 证据支持的结论

- 之前的执行解释混淆了两层：用户长期偏好应写成全局默认策略；当前运行时工具规则是
  更高优先级的临时边界，不能被全局规则覆盖。
- 正确修正是：全局规则明确“非简单科研任务默认至少一个监督/审查型 subagent”，同时
  要求如果当前系统/工具不允许真实 subagent，必须明说并用主流程内模拟独立视角兜底。

### 未解决风险与下一步

- 后续非简单科研任务应真正把监督 subagent 放进 Node Contract，而不是只在最终汇报中
  解释是否使用。
- 如果未来运行环境再次出现更高优先级的 subagent 限制，需要明确指出冲突，而不是把
  用户偏好改写成长期规则。

### 专家评审

- 可靠结论：全局规则已经恢复并强化用户长期偏好；本轮有真实监督 subagent 审查规则
  修正，且保留了系统/工具规则优先级边界。
- 证据不足：该规则修正尚未在下一个完整科研节点中实际验证执行效果。
- 下一步是否合理：下一次 final `SC-INR` benchmark 或论文同步节点，应在 Node
  Contract 中默认安排至少一个监督/审查型 subagent。
- 需要防止过度宣称：不要说全局规则能覆盖系统或工具限制；不要把模拟独立视角说成真实
  subagent；不要把监督 subagent 变成开放式扩新实验的理由。

## Cline subagent 工作流调研与当前不足审查

### 完成范围

- 调研 Cline 官方文档和 GitHub 仓库中与 subagents、task management、new_task 和
  Kanban/worktree 相关的机制。
- 启动真实只读监督 subagent `Aquinas`，审查当前全局工作流中“灵活使用 subagent 且
  token 高效”的不足。
- 本轮不直接修改 `/home/admin/.codex/AGENTS.md`；调研结论先作为待确认的工作流改进
  建议保留。

### 运行命令与来源

- Web sources：
  - `https://docs.cline.bot/features/subagents`
  - `https://docs.cline.bot/core-workflows/task-management`
  - `https://docs.cline.bot/kanban/features`
  - `https://github.com/cline/cline/blob/main/src/core/prompts/system-prompt/tools/subagent.ts`
  - `https://github.com/cline/cline/blob/main/src/core/prompts/system-prompt/tools/new_task.ts`
- GitHub API / raw 定位：
  - `curl -L --max-time 30 https://api.github.com/repos/cline/cline/git/trees/main?recursive=1 | rg -i 'subagent|new_task|new-task|task-management'`
  - `curl -L --max-time 30 https://raw.githubusercontent.com/cline/cline/main/docs/core-workflows/task-management.mdx | sed -n '1,260p'`
- 监督 subagent：
  - `Aquinas` 只读审查 `/home/admin/.codex/AGENTS.md` 的 subagent、token 效率和 hard
    gate 规则。

### 证据支持的结论

- Cline subagents 的核心不是“多 agent 越多越好”，而是隔离上下文：subagent 拥有独立
  prompt、context window 和 token budget，做只读研究，向主 agent 返回浓缩报告。
- Cline 官方文档明确 subagents 适合宽范围代码库探索、cross-cutting concern 和
  pre-edit research；小而集中的任务使用 subagent 会增加不必要开销。
- GitHub 源码中的 `use_subagents` 工具描述允许最多 5 个并行 subagents，并明确一个
  subagent 也有效；目的包括避免主 agent context window 被大量文件阅读消耗。
- Cline 的 `new_task` 与 subagent 不同：它是用摘要预加载新任务上下文的任务切换机制，
  适合长任务续接或上下文清理，不等同于并行监督。

### 当前工作流不足

- 当前全局规则已要求非简单科研任务默认监督 subagent，但还缺少“subagent 边际收益必须
  大于上下文切换和 token 成本”的明确约束。
- 缺少 subagent 输入包最小化规则：应只给 Node Contract、canonical 文件路径、待审问题
  和禁止事项，而不是整段聊天历史。
- 缺少 subagent 输出预算：应限制为关键发现、阻塞风险、最强反对意见和会改变决策的证据，
  不复制长日志或长背景。
- 缺少整合约束：主流程只吸收分歧、阻塞和会改变决策的证据，不把 subagent 全文并入
  最终回复或项目记忆。
- 角色 gate 和真实 subagent 的边界仍需更清楚：五个角色不应机械对应五个真实 subagent。

### 未解决风险与下一步

- 如果要把本次调研升格为全局规则，建议新增“subagent token budget / input packet /
  synthesis contract”小节，而不是扩大现有 hard gate 模板。
- 需要防止误迁移 Cline Kanban 的多 worktree/autonomous pipeline：科研项目中方向改变、
  claim 升级、指标改变仍需 gate 或用户决策。

### 专家评审

- 可靠结论：Cline 的 subagent 机制对当前工作流最有价值的启发是上下文隔离、只读研究、
  focused prompt、独立 token budget 和浓缩回传，而不是盲目增加 agent 数量。
- 证据不足：本轮未修改全局规则，也未在新的科研节点中实测 token 节省效果。
- 下一步是否合理：若用户确认，应将“subagent 输入包最小化、输出预算、整合约束、边际
  收益大于 token 成本”写入全局工作流。
- 需要防止过度宣称：不要把运行 subagent 等同于证据增强；不要把 Cline 的任务卡/流水日志
  迁移成科研记录模板；不要用 subagent 扩展开放式实验分支。

## Cline subagent 优势融合、实测与修正

### Node Contract

- 目标：将 Cline subagents 中“上下文隔离、focused prompt、独立 token budget、浓缩回传”
  的优势融合进 Codex 全局科研工作流。
- 主指标：全局规则是否新增 Subagent Input Packet、Output Budget、Synthesis Contract、
  成本收益门槛和读取范围约束。
- 实测任务：用最小 input packet 让真实只读 subagent 审查 seed2/seed3 训练进展是否正常，
  再由主流程整合并检查新规则是否仍有缺口。
- 禁止事项：不把 Cline 的 Kanban/worktree 自动流水线迁移成科研自动推进；不把 subagent
  数量当成严谨性；不运行训练或 benchmark。

### 完成范围

- 启动真实监督 subagent `Franklin`，只读审查 `/home/admin/.codex/AGENTS.md` 中
  subagent/token 规则缺口。
- 向 `/home/admin/.codex/AGENTS.md` 新增 `subagent token 高效使用` 小节。
- 启动真实只读测试 subagent `Locke`，使用最小输入包审查 seed2/seed3 训练日志和
  task ledger。
- 主流程独立检查最新日志、错误关键字、进程/GPU 状态，并对 subagent 结果做 synthesis。
- 根据测试暴露的缺口修正规则：Input Packet 必须明确审查范围、允许/禁止工具和 live
  checks 分工；证据不足时 subagent 必须标注限制。
- 更新 `memory/task_ledger.md` 的训练检查点。

### 写入的全局规则

- 真实 subagent 启动前必须收到最小 Subagent Input Packet：节点目标、审查范围、需要读取
  的 canonical source、具体审查问题、允许/禁止工具和输出上限。
- subagent 默认只读 packet 指定 source；扩展读取范围必须说明触发原因。
- subagent 输出默认不超过 5-8 条 bullet，只报告会改变主流程决策的发现、阻塞风险、最强
  反对意见和建议下一步读取的文件。
- 主流程必须做 synthesis contract，将关键意见标记为采纳、不采纳或待验证，并说明如何
  影响实现、实验、claim 或下一步 gate。
- subagent 的边际收益必须大于上下文切换和 token 成本；不得为“显得严谨”启动固定数量、
  固定角色矩阵或开放式搜索型 subagent。
- 若 Input Packet 不足以回答某个审查问题，subagent 应标注证据限制并建议补充 source
  或命令，不得用不完整证据替代主流程检查。

### 实测结果

- 测试 input packet 只包含：seed2/seed3 日志、`memory/task_ledger.md`、3 个审查问题、
  只读约束和最多 6 条 bullet 输出限制。
- `Locke` 输出 6 条 bullet，未复制长日志，未扩展读取范围，满足 output budget。
- subagent 发现：seed2/seed3 日志连续推进，无 `nan`/`inf`、`Traceback` 或 `error`，
  未见暂停阻塞。
- 主流程补充 live checks：
  - `2026-05-10 06:51 UTC`
  - seed2 latest: `epoch 107/1000, train: loss=0.0320, val: psnr=29.7461`
  - seed3 latest: `epoch 107/1000, train: loss=0.0316, val: psnr=29.9454`
  - GPU 2/3 memory about `2790 MiB` each, utilization about `95%/95%`
- Synthesis：采纳 subagent 的日志侧结论；补充主流程进程/GPU 检查；无需要暂停、改配置或
  请求用户决策的阻塞。

### 测试暴露的问题与修正

- 问题：测试 subagent 的 packet 只给了日志和 task ledger，无法独立判断进程/GPU 状态；
  如果不标注边界，可能把日志侧健康误写成完整运行健康。
- 修正：全局规则已补充 Input Packet 必须明确 live checks 分工；当证据不足时，subagent
  必须标注证据限制并建议补充 source 或命令。

### 科学进展分类

- 本节点是“工作流优化 + 实测验证 + 记录维护”，不是新的科研实验结果。
- 对当前项目的实际状态结论仅限训练监控：seed2/seed3 正常推进，尚未完成，不能升级
  final `SC-INR` multi-seed claim。

### 专家评审

- 可靠结论：Cline 式 subagent 优势已转化为可执行全局规则；最小 input packet 和输出预算
  在一次真实训练监控任务中可用；测试发现的证据边界问题已修正。
- 证据不足：这只是一次小型监控测试，尚未覆盖复杂论文 evidence audit 或跨模块实现任务。
- 下一步是否合理：后续 final `SC-INR` benchmark/evidence audit 节点应继续使用新的
  Subagent Input Packet 和 Synthesis Contract，并观察是否真正降低 token 消耗。
- 需要防止过度宣称：不要把 subagent 审查写成实验验证；不要把日志健康写成训练最终成功；
  不要让 subagent 机制扩大开放式实验分支。

## 回归主线状态诊断

### Node Contract

- 科学问题：当前 SC-INR 项目主线状态是什么，下一步应做什么。
- 主指标：final `SC-INR` multi-seed 闭环进度、claim/artifact 是否仍一致、训练是否触发
  暂停条件。
- 辅助指标：论文草稿滞后程度、NoSinc/response caveat 是否已进入主线判断。
- 禁止事项：不扩新 descriptor、metric 或 model 分支；不把 running seed 或 seed1
  preliminary 升级为最终结论。

### subagent 使用与 synthesis

- 使用真实监督 subagent `Hooke`，输入包只包含当前状态 capsule、artifact 白名单、claim
  ledger 和 task ledger；live GPU/进程检查由主流程负责。
- `Hooke` 结论采纳：当前主线阻塞仍是 final `SC-INR` seed2/seed3 完成训练后 full
  benchmark；没有只读证据支持暂停路线或改方向。
- `Hooke` 边界采纳：它未做 live GPU/进程检查，因此运行健康由主流程补证。
- 不采纳项：无。
- 待验证项：final `SC-INR` 多 seed 性能，必须等训练完成和 full benchmark 后判断。

### 完成范围

- 读取 `docs/project/current_state.md`、`paper/ARTIFACTS_ALLOWED.md`、
  `paper/claims_evidence_matrix.md`、`memory/task_ledger.md`。
- 检查 seed2/seed3 最新日志、错误关键字、GPU/进程状态和 checkpoint 更新时间。
- 更新 `memory/task_ledger.md` 的最新检查点。

### 运行命令与产物

- Canonical 状态：
  - `sed -n '1,120p' Equivariant-ASISR/docs/project/current_state.md`
  - `sed -n '1,120p' Equivariant-ASISR/paper/ARTIFACTS_ALLOWED.md`
  - `sed -n '1,180p' Equivariant-ASISR/paper/claims_evidence_matrix.md`
  - `tail -80 Equivariant-ASISR/memory/task_ledger.md`
- Live checks：
  - `date -u '+%Y-%m-%d %H:%M:%S UTC'`
  - `tail -3 Equivariant-ASISR/save-seeds/seed2/sc-inr/log.txt`
  - `tail -3 Equivariant-ASISR/save-seeds/seed3/sc-inr/log.txt`
  - `rg -n -i 'nan|inf|traceback|error|exception|cuda out|out of memory' ...`
  - `nvidia-smi --query-gpu=index,name,memory.used,utilization.gpu --format=csv,noheader`
  - `ps -eo pid,ppid,stat,etime,pcpu,pmem,args | rg -i 'entrypoints/train.py --config configs/train-div2k/train-sc-inr.yaml'`
  - `ls -lh artifacts/checkpoints/seed{2,3}/sc-inr/epoch-{best,last}.pth`
- 修改文件：
  - `Equivariant-ASISR/memory/task_ledger.md`
  - `Equivariant-ASISR/memory/daily/2026-05-10.md`

### 当前状态

- 主线研究对象仍清楚：decoder-side scale sampling consistency；禁止 strict
  scale equivariance / whole-network scale equivariance。
- `SC-INR-NoPhi` 是当前 cite_ok 的 3-seed core 证据主体；final `SC-INR` 仍是 seed1
  preliminary，必须等待 seed2/seed3 full benchmark。
- NoSinc 和 response diagnostics 已经把 same-LR consistency 降级为需要联合解释的诊断，
  不能作为 sinc 主证据。
- seed2/seed3 live 状态：
  - `2026-05-10 06:55 UTC`
  - seed2 latest: `epoch 113/1000, train: loss=0.0314, val: psnr=29.9524`
  - seed3 latest: `epoch 114/1000, train: loss=0.0321, val: psnr=29.7367`
  - GPU 2/3 memory about `2790 MiB` each, utilization about `95%/94%`
  - `epoch-last.pth` for both seeds updated at `06:55`，seed3 `epoch-best.pth` also updated
    at `06:55`
  - 当前日志未匹配到 `nan`/`inf`、`Traceback`、`error`、`exception` 或 OOM。

### 阶段判断与下一步 gate

- 当前项目已经回到主线：等待 final `SC-INR` seed2/seed3 完成，而不是继续扩新消融。
- 现在不应改训练协议、改模型分支、扩 descriptor/metric；除非训练触发暂停条件或结果完成后
  evidence audit 暴露根本问题。
- 最小下一步 gate：
  1. 继续监控 seed2/seed3 到 `epoch 1000/1000`。
  2. 使用 `artifacts/checkpoints/seed{2,3}/sc-inr/epoch-best.pth` 运行 full benchmark。
  3. 比较 `LIIF`, `LTE`, `SC-INR-NoPhi`, `SC-INR`。
  4. 生成 per-seed 与 mean/std summary。
  5. 做 evidence audit 后更新 `claims_evidence_matrix.md` 和 `ARTIFACTS_ALLOWED.md`。

### 科学进展分类

- 本节点是“主线状态诊断 + 训练监控 + 记录维护”，不是新的实验结果。
- 科学主结论未升级；final `SC-INR` 仍不能写成最终多 seed winner。

### 专家评审

- 可靠结论：主线阻塞项明确，训练健康推进，当前没有暂停或改方向证据。
- 证据不足：seed2/seed3 只到约 11% epoch，无法判断最终 benchmark 结果。
- 下一步是否合理：保持训练、等待完成后 full benchmark，是当前最合理且最小的动作。
- 需要防止过度宣称：不要把 running 训练写成完成；不要把 seed1 preliminary 升级为最终
  结论；不要把 same-LR consistency 单独作为 sinc 或 observation correctness 证明。

## SC-INR-EQ 模型方案预研

### Node Contract

- 科学问题：是否可以参考 `LTE-EQ` 设计一个 `SC-INR-EQ`，以及哪些结构可以复用、哪些
  SC-INR 契约不能破坏。
- 主指标：保持 SC-INR 的 decoder-side sampling response 定义，即 `coef(z)`、`omega(z)`、
  `phi(z)` 只由 feature 生成，cell/scale 只进入 analytic sinc response。
- 禁止事项：不把 `LTE-EQ` 的 cell-conditioned phase 直接搬到 SC-INR-EQ；不宣称 strict
  scale equivariance 或 whole-network scale equivariance；不把新模型分支插入当前 final
  SC-INR 多 seed 闭环之前。

### subagent 使用与 synthesis

- 使用真实审查 subagent `Einstein`，输入包限定为 `src/models`、registry、taxonomy 和
  current_state，任务是检查 `LTE-EQ` 与当前 `SC-INR` 的结构契约。
- 采纳结论：`LTE-EQ` 的 Rot-E plumbing 可参考，包括 EQ encoder、`Fconv_PCA` 的
  equivariant feature map 生成、`EQ_lte_input` 的旋转坐标/Fourier 输入组织、equivariant
  imnet。
- 采纳反对意见：`LTE-EQ` 的 `EQ_linear_inter(cell)` 属于 learned cell-conditioned phase，
  不能进入 SC-INR-EQ，否则会破坏 “cell only through analytic sinc response” 的主方法边界。
- 设计边界：`SC-INR-EQ` 应被定位为 “SC-INR decoder contract + Rot-E feature/decoder
  plumbing” 的正交扩展，而不是当前 SC-INR 主线消融。

### 完成范围

- 核对 `src/models/lte_eq.py`：确认其使用 `edsr-eq` encoder、`Fconv_PCA` 生成
  `coef/freq`、`EQ_linear_inter` 由 cell 生成 phase、`EQ_lte_input` 生成 Fourier 输入。
- 核对 `src/models/e_linear.py`：确认 `EQ_lte_input` 会按 `tranNum` 旋转坐标并与频率
  做内积，再加 phase 后生成 cos/sin 特征。
- 核对 `src/models/sc_inr_adaptive.py`：确认 final `SC-INR` 的 `phi(z)` 只来自 feature，
  cell 只通过 `sinc(omega_x*c_x/2)*sinc(omega_y*c_y/2)` 进入。
- 核对 `configs/train-div2k/train-lte-eq.yaml` 与 `configs/train-div2k/train-sc-inr.yaml`：
  确认二者训练协议相近，但 encoder/imnet/decoder contract 不同。

### 设计结论

- 建议的主方案是 `SC-INR-EQ`：EQ encoder + equivariant `coef/omega/phi` heads + SC-INR
  analytic sinc response + EQ imnet。
- `phi` 必须是 feature-conditioned `phi(z)`，不能接收 cell/scale；LTE-EQ 的
  `phase(cell)` 最多作为反例或对照，不应作为 SC-INR-EQ 的默认设计。
- sinc response 的数学处理是最敏感点：需要决定旋转频率分量下 cell footprint 是否只按
  axis-aligned cell 计算，还是需要与 Rot-E 表示一致地旋转/解释 footprint；编码前必须先做
  一个明确的属性测试设计。

### 未解决风险与下一步

- 当前不建议立即训练 SC-INR-EQ；应先完成 final `SC-INR` seed2/seed3 full benchmark。
- 若后续实现，最小 gate 是：shape/device smoke、finite backward、`hidden_dim/num_freqs`
  与 `tranNum` 整除检查、`tranNum=1` 退化检查、sinc response 属性测试。
- 论文定位上应保留两个对照轴：`SC-INR` vs `SC-INR-EQ` 测 Rot-E 增量；
  `LTE-EQ` vs `SC-INR-EQ` 测 decoder contract 差异。不能把 Rot-E 收益归因到
  sampling consistency。

### 专家评审

- 可靠结论：可以参考 LTE-EQ 的 equivariant 管线，但不能照搬其 cell-conditioned phase；
  SC-INR-EQ 的核心边界应是 SC-INR decoder contract 不变。
- 证据不足：尚未实现、训练或测试 SC-INR-EQ，也未完成 sinc footprint 在 Rot-E 表示下的
  数学属性验证。
- 下一步是否合理：先把当前 final SC-INR 多 seed 主线闭环；若结果稳定，再以设计 note +
  smoke/property test 的形式启动 SC-INR-EQ。
- 需要防止过度宣称：不要把 SC-INR-EQ 写成当前论文主贡献的已验证结果；不要把 Rot-E
  equivariance 写成 scale equivariance；不要用参数量/encoder 改变带来的收益支持 sinc
  机制结论。

## SC-INR-EQ 最小原型与并行训练启动

### Node Contract

- 科学问题：在用户确认有多余 GPU 后，是否可以不等待 final `SC-INR` seed2/seed3 完成，
  先并行启动 `SC-INR-EQ` exploratory 单 seed。
- 主指标：新分支不干扰 final `SC-INR` 多 seed 主线；实现保持 SC-INR decoder contract；
  启动前必须通过 smoke/property gate。
- 禁止事项：不占用 GPU2/3；不修改 final `SC-INR` seed2/seed3 协议；不把 single-seed
  exploratory 结果写成论文主结论；不引入 `LTE-EQ` 的 `phase(cell)`。

### subagent 使用与 synthesis

- 使用真实审查 subagent `Faraday` 进行只读审查。
- 采纳结论：支持有条件并行启动，但只作为 exploratory 单 seed，并要求 GPU、保存目录和日志
  与主线训练隔离。
- 采纳 gate：结构上不得出现 `phase(cell)`、`omega(cell)` 或 `coef(cell)`；正式训练前需
  forward/backward finite gate；论文中只能作为探索性附录/未来工作，不能作为主表结论。
- 补充主流程判断：用户新确认的资源条件改变了上一节“等待多 seed 完成后再启动”的执行建议，
  但没有改变科学 claim gate。

### 完成范围

- 新增 `src/models/sc_inr_eq.py`：
  - 使用 `edsr-eq-baseline` encoder。
  - 使用 `Fconv_PCA` 生成 equivariant `coef(z)` 和 signed bounded `omega(z)`。
  - 使用 equivariant `phi(z)` head；`phi` 不接收 cell/scale。
  - 保留 analytic sinc response；cell 只在 response 中使用。
  - sinc response 使用旋转通道对应到原图坐标系的 effective omega components。
- 新增 `configs/train-div2k/train-sc-inr-eq.yaml`，训练协议对齐现有 DIV2K 1000 epoch
  配置，输出模型名为 `sc_inr_eq`。
- 新增 `scripts/analysis/smoke_check_sc_inr_eq.py`，作为训练前 gate。
- 更新 `src/models/__init__.py` 导入新模型。
- 更新 `memory/task_ledger.md` 记录后台训练协议和恢复信息。

### 运行命令与验证结果

- 语法和配置检查：
  - `python -m py_compile src/models/sc_inr_eq.py scripts/analysis/smoke_check_sc_inr_eq.py`
  - YAML parse for `configs/train-div2k/train-sc-inr-eq.yaml`
- 首次 smoke 失败：
  - `Fconv_PCA(kernel_size=1)` 在 phase head 中触发 NaN；原因是 basis 构造对 `sizeP=1`
    有除零风险。
- 修正：
  - `phase_kernel_size=1` 时改用 `Fconv_1X1`。
  - 对 no-bias zero tensor 注册 buffer，避免 `.to(device)` 后仍停留在 CPU。
- 修正后 smoke：
  - `CUDA_DEVICE_ORDER=PCI_BUS_ID CUDA_VISIBLE_DEVICES=0 python scripts/analysis/smoke_check_sc_inr_eq.py --device cuda:0`
  - omega init max abs error：`7.450581e-09`
  - forward/backward shape：`(1, 256, 3)`，loss finite。
  - x2/x4/x8/x16/x30 cell outputs finite。
  - response cell delta：`3.166991e-01`。
- 启动训练：
  - `CUDA_DEVICE_ORDER=PCI_BUS_ID CUDA_VISIBLE_DEVICES=0 python entrypoints/train.py --config configs/train-div2k/train-sc-inr-eq.yaml --name sc-inr-eq --saveFolder artifacts/checkpoints/seed1 --seed 1`
  - tmux：`scinr_eq_seed1_20260510`
  - 输出目录：`artifacts/checkpoints/seed1/sc-inr-eq/`

### 当前状态

- `2026-05-10 16:06 UTC` 检查：`SC-INR-EQ` 训练进程仍在运行，日志已写出 `seed: 1`、
  `CUDA_VISIBLE_DEVICES: 0`，尚未写出首个 epoch，判断仍处于初始化/缓存数据阶段。
- GPU0 memory about `2625 MiB`，未占用 GPU2/3。
- final `SC-INR` seed2/seed3 训练仍在 GPU2/3 运行，未被新任务中断。
- `2026-05-10 16:10 UTC` 再检查：仍未写出首个 epoch；GPU0 memory about `2127 MiB`，
  utilization `0%`，CPU high。暂判断为 in-memory dataset 初始化或 I/O/CPU 争用偏慢，
  尚未见 traceback/OOM。
- `2026-05-10 16:16 UTC` 再检查：仍未写出首个 epoch；RSS about `10.97GB`，CPU high。
  已核对 `image-folder` 的 `cache: in_memory` 会在 dataset 构造阶段同步读入图片；当前更像
  数据缓存阶段偏慢，而不是模型计算失败。

### 未解决风险与下一步

- 必须等待 `SC-INR-EQ` 首个 epoch 后确认 loss/val finite；当前只能说明进程已启动和
  smoke gate 通过。若继续长时间无 epoch，应诊断数据缓存/CPU/IO 策略，而不是把运行时间
  误认为有效训练。
- `SC-INR-EQ` 混合了 EQ encoder、EQ imnet、equivariant heads 和 SC-INR decoder contract；
  即使 PSNR 提升，也不能直接归因于 sinc 或 sampling consistency。
- Rot-E 表示下 sinc response 的 effective omega 处理仍需后续更强属性测试；当前 smoke
  只验证有限性和 cell sensitivity，不验证完整 equivariance。

### 专家评审

- 可靠结论：最小原型已实现并通过训练前 smoke；启动目录和 GPU 与 final `SC-INR`
  主线隔离；实现未引入 LTE-EQ 的 cell-conditioned phase。
- 证据不足：尚未进入首个 epoch，尚无训练曲线、benchmark 或公平对照；不能判断
  `SC-INR-EQ` 是否有效。
- 下一步是否合理：等待首个 epoch 和若干早期 epoch 后检查稳定性；若训练稳定，再把它保留为
  exploratory branch，最终完成后再决定是否 benchmark。
- 需要防止过度宣称：不要把该分支写成当前论文主方法；不要把 Rot-E equivariance 写成
  scale equivariance；不要把单 seed exploratory 结果纳入主表或主 claim。
