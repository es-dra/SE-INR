# 2026-05-08

## 实验进展只读巡检

### 完成范围

- 按用户要求查看当前所有主要实验进展，只读核对进程、GPU、日志、checkpoint 和结果 JSON。
- 未启动新的训练或评估，未修改 checkpoint 或结果文件。

### 当前运行状态

- `SC-INR-Signed` 仍在训练：
  - 命令：`python -u train.py --config configs/train-div2k/train-sc-inr-adaptive-signed.yaml --name sc-inr-adaptive-signed --saveFolder ./save --device 2 --seed 1`
  - PID：`2688313`
  - 最新日志：`epoch 437/1000, train: loss=0.0311, val: psnr=29.9200, 1.8m 16.4h/37.6h`
  - 当前 best val：epoch 407，PSNR 30.5994
- `LIIF seed3` 仍在训练：
  - PID：`2698616`
  - 最新日志：`epoch 932/1000, train: loss=0.0306, val: psnr=30.0276, 49.8s 13.5h/14.5h`
  - 当前 best val：epoch 861，PSNR 30.4974
  - 旧 pipeline 父进程仍在，训练完成后预期会自动执行 LIIF seed3 benchmark。
- `LTE seed3` 仍在训练：
  - PID：`2712170`
  - 最新日志：`epoch 949/1000, train: loss=0.0307, val: psnr=29.8127, 37.7s 9.9h/10.4h`
  - 当前 best val：epoch 676，PSNR 30.5519
  - 当前只确认训练续跑，没有确认自动评估队列。
- `SC-INR seed3` 已完成：
  - `save-seeds/seed3/sc-inr-adaptive/log.txt` 到 `epoch 1000/1000`
  - final val：30.1961
  - best val：epoch 722，PSNR 30.6670

### 已完成训练

- seed1 主线：
  - LIIF、LTE、SC-INR-Fixed、SC-INR、LTE-NoCell、LTE-FeaturePhase、LTE-EQ、LIIF-EQ 均已完成。
- seed2：
  - `SC-INR`、`LIIF`、`LTE` 均已完成。
- seed3：
  - `SC-INR` 已完成。
  - `LIIF`、`LTE` 接近完成但仍在训练。

### 已有 benchmark 结果

- `results/benchmark.json` 是 seed1 主 benchmark：
  - LTE OOD 平均：22.6917
  - SC-INR OOD 平均：22.7471
  - SC-INR 相对 LTE：+0.0554 dB
  - LIIF-EQ OOD 平均：22.7469，和 SC-INR 几乎持平
  - LTE-FeaturePhase OOD 平均：22.6910，基本不优于 LTE
- `results/seeds/benchmark_seed2.json` 已包含 `SC-INR`、`LIIF`、`LTE`：
  - LTE OOD 平均：22.7173
  - SC-INR OOD 平均：22.7680
  - SC-INR 相对 LTE：+0.0507 dB
  - LIIF OOD 平均：22.7291
- `results/seeds/benchmark_seed3.json` 目前只包含 `SC-INR`：
  - SC-INR ID 平均：31.0826
  - SC-INR OOD 平均：22.7592
  - 文件时间仍是 `2026-05-07 03:42 UTC`，说明 LIIF/LTE seed3 benchmark 尚未写入。

### 辅助指标和论文材料

- `results/analysis/seed1_aux_metrics_all8/` 已存在完整 seed1 辅助指标：
  - `quality_summary.csv`
  - `consistency_summary.csv`
  - `figures/`
  - `paper/README_zh.md`
  - `paper/key_findings.md`
  - `paper/consistency_table.tex`
  - `paper/reviewer_advice_analysis.md`
- 这些结果仍是目前最强机制证据：SC-INR 在质量小幅提升的同时显著改善 same-LR cross-scale observation consistency。
- 但这些仍是 seed1 辅助结果，不能替代多 seed 统计。

### 新增但尚未训练的实验

- 已实现并 smoke 通过 `SC-INR+PhiZ`：
  - 配置：`configs/train-div2k/train-sc-inr-signed-phiz.yaml`
  - 模型注册：`sc_inr_signed_phiz`
  - 设计：signed omega + feature-conditioned phase `phi(z)`，不让 phase 依赖 cell/scale。
- 该实验尚未启动全量训练，因为当前 GPU 都在占用。

### 资源状态

- `nvidia-smi` 显示四张 GPU 均有高负载：
  - GPU0：A800，高负载但进程表未展开，不能视为空闲；
  - GPU1：LTE seed3；
  - GPU2：SC-INR-Signed；
  - GPU3：LIIF seed3。
- 当前不建议启动 `SC-INR+PhiZ` 或其他新全量训练。

### 下一步

- 等 `LIIF seed3` 完成后，检查旧 pipeline 是否自动把 LIIF 写入 `results/seeds/benchmark_seed3.json`。
- 等 `LTE seed3` 完成后，若没有自动评估，手动补：
  - `python eval_full.py --device 1 --save_root ./save-seeds/seed3 --models LTE --output ./results/seeds/benchmark_seed3.json --skip_existing`
- 当 seed3 的 LIIF/LTE 都写入后，再更新多 seed mean/std。
- 等 `SC-INR-Signed` 完成后，单独按同协议评估，再决定 signed omega 是否替代当前 SC-INR 主模型。
- GPU 空闲后再启动 `SC-INR+PhiZ`：
  - `python -u train.py --config configs/train-div2k/train-sc-inr-signed-phiz.yaml --name sc-inr-phiz --saveFolder ./save --device <free_gpu> --seed 1`

### 专家评审

可靠部分：训练进度来自当前进程、日志尾部和 checkpoint 文件时间；seed1/seed2 benchmark 数值来自本地 JSON，可作为当前进展事实。

证据不足部分：seed3 的 LIIF/LTE benchmark 尚未完成，多 seed mean/std 还不能更新；SC-INR-Signed 仅训练到 437/1000，不能判断 signed omega 是否有效；SC-INR+PhiZ 只有 smoke 通过，没有实验结果。

下一步合理性：当前最短路径是等待 LIIF/LTE seed3 结束并补齐 benchmark，然后再做多 seed 汇总。新结构实验不应抢当前三条训练资源。

需要防止过度宣称：SC-INR 的 PSNR gain 目前稳定但幅度小，论文必须继续依赖 consistency、texture RMSE、多 seed 和机制消融共同支撑；不能把 signed omega 或 PhiZ 写成已验证改进。

## GPU0 状态复核与 SC-INR+PhiZ 未启动

### 完成范围

- 根据用户要求重点核对 GPU0 是否空闲、是否只是僵尸进程占用。
- 未启动 `SC-INR+PhiZ` 训练。

### 证据

- 连续三次 `nvidia-smi` 采样显示 GPU0：
  - 显存占用约 `16609 MiB`
  - GPU 利用率持续 `100%`
  - 功耗约 `232-245 W`
- `nvidia-smi --query-compute-apps` 显示 GPU0 上有 compute app：
  - PID `1724989`，约 `12944 MiB`
  - PID `2982335`，约 `2804 MiB`
  - PID `3254442`，约 `838 MiB`
- 这些 PID 在当前容器 `/proc` 中不可见，不能由本容器确认命令行或安全终止。
- 这更像宿主机或其他命名空间里的活跃 GPU 进程，而不是可安全忽略的僵尸进程。持续 `100% util` 和高功耗尤其不支持“空闲”判断。

### 决策

- 未在 GPU0 上启动 `SC-INR+PhiZ`。
- 当前更合理的启动时机是等待 `LTE seed3` 或 `LIIF seed3` 完成后释放 GPU1/GPU3，再启动：
  - `python -u train.py --config configs/train-div2k/train-sc-inr-signed-phiz.yaml --name sc-inr-phiz --saveFolder ./save --device <free_gpu> --seed 1`

### 专家评审

可靠部分：GPU0 不是安全空闲卡这一判断由连续采样的显存、利用率、功耗和 compute app 记录共同支持。

证据不足部分：由于 GPU0 上的 PID 不在当前容器 `/proc` 可见，无法判断具体是谁启动、是否可终止、是否属于其他用户或宿主机任务。

下一步合理性：等待 GPU1/GPU3 训练自然结束后再启动 PhiZ，比强行占用 GPU0 更稳妥。

需要防止过度操作：不能因为当前容器看不到 PID 就假定它是僵尸；在共享/容器化 GPU 环境中，`[Not Found]` 常见于命名空间不可见进程。

## 启动 SC-INR+PhiZ seed1 训练

### 完成范围

- 确认 `LTE seed3` 已训练完成：`save-seeds/seed3/lte/log.txt` 到 `epoch 1000/1000`，final val PSNR `29.7516`，原 LTE seed3 训练进程已不存在。
- 复核当前 CUDA ordinal 与 `nvidia-smi` 物理行的映射，避免误判 GPU：
  - PyTorch `cuda:0` -> RTX 4090，UUID `42f95d21-12cd-8428-a80b-82b3721abc01`
  - PyTorch `cuda:1` -> RTX 4090，UUID `4fd8e39c-ecb7-159e-b1f9-930c3066676e`
  - PyTorch `cuda:2` -> A800，UUID `675adc8d-a6ea-1c8b-f833-f8ae8a87cdaa`
  - PyTorch `cuda:3` -> A800，UUID `7a98572d-91e0-6c35-b4e5-4ac57a77e8cd`
- 在 PyTorch `--device 1` 上启动 `SC-INR+PhiZ` seed1 全量训练；该设备对应 `nvidia-smi` 物理行 3 的 RTX 4090，此前空闲，没有抢占 LIIF seed3 或 SC-INR-Signed。

### 启动命令与产物

- 启动命令：
  - `setsid python -u train.py --config configs/train-div2k/train-sc-inr-signed-phiz.yaml --name sc-inr-phiz --saveFolder ./save --device 1 --seed 1 > logs/sc-inr-phiz_seed1_gpu1.log 2>&1 < /dev/null &`
- PID：
  - `2740208`
  - PID 文件：`logs/sc-inr-phiz_seed1_gpu1.pid`
- 训练日志：
  - stdout/stderr：`logs/sc-inr-phiz_seed1_gpu1.log`
  - repo 训练日志：`save/sc-inr-phiz/log.txt`
- checkpoint/配置输出目录：
  - `save/sc-inr-phiz/`

### 验证结果

- `save/sc-inr-phiz/log.txt` 已写出数据集、模型和早期 epoch：
  - train dataset size `16000`
  - val dataset size `1600`
  - model params `1.7M`
  - `epoch 1/1000`: train loss `0.0385`, val PSNR `29.0867`
  - `epoch 2/1000`: train loss `0.0357`, val PSNR `29.2589`
  - `epoch 3/1000`: train loss `0.0349`, val PSNR `29.4321`
- 进程状态：
  - `2740208` 仍在运行，命令行为 `python -u train.py --config configs/train-div2k/train-sc-inr-signed-phiz.yaml --name sc-inr-phiz --saveFolder ./save --device 1 --seed 1`
- `nvidia-smi` 物理行 3（UUID `4fd8e39c-ecb7-159e-b1f9-930c3066676e`）显存约 `2790 MiB`、GPU 利用率约 `91%`，与 PhiZ 训练相符。

### 当前其他实验状态

- `LIIF seed3` 仍在训练，日志到 `epoch 990/1000` 左右，命令使用 `--device 3`，对应 PyTorch A800 ordinal，不应误认为 `nvidia-smi` 行 3。
- `SC-INR-Signed` 仍在训练，日志到 `epoch 469/1000` 左右，命令使用 `--device 2`。
- GPU0 和 GPU1 的 `nvidia-smi` 物理行仍有容器外或不可见高负载任务，不应主动占用。

### 下一步

- 等 `LIIF seed3` 完成后确认旧 pipeline 是否自动评估并写入 `results/seeds/benchmark_seed3.json`。
- LTE seed3 已完成训练，但 seed3 benchmark 仍需补齐；若没有自动评估，应使用明确输出路径补：
  - `python eval_full.py --device <available_cuda_ordinal> --save_root ./save-seeds/seed3 --models LTE --output ./results/seeds/benchmark_seed3.json --skip_existing`
- 持续观察 `SC-INR+PhiZ`，只在训练完成并按同协议评估后判断其是否优于 `SC-INR` 或 `SC-INR-Signed`。

### 专家评审

可靠部分：`SC-INR+PhiZ` 已经真实进入训练并完成前 3 个 epoch；设备选择基于 PyTorch CUDA ordinal 与 GPU UUID 映射，而不是仅凭 `nvidia-smi` 行号，因而避免了抢占 LIIF seed3。

证据不足部分：前几个 epoch 的 val PSNR 只能说明训练正常，不能判断 PhiZ 是否有效；signed omega 和 PhiZ 都尚未完成全量训练与统一 benchmark。

下一步合理性：当前并行资源使用是可接受的，短期重点应转向等 LIIF seed3 自动评估、补 LTE seed3 benchmark、继续跟踪 signed/PhiZ 完成。

需要防止过度宣称：`SC-INR+PhiZ` 是一个机制消融/扩展实验。即使它最终 PSNR 提升，也要结合 consistency、texture RMSE、continuous scale 和多 seed 结果判断，不能仅凭单次 seed 或训练集内验证 PSNR 做主模型结论。
