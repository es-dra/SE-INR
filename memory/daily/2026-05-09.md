# 2026-05-09

## 补齐 SC-INR-Signed / SC-INR+PhiZ benchmark 与核心多 seed 汇总

### 完成范围

- 核对 `save/sc-inr-phiz/log.txt`：训练已到 `epoch 1000/1000`；最后 val PSNR `30.0127`，日志内最佳 val PSNR `30.6425`，出现在 epoch 846。
- 核对 `save/sc-inr-adaptive-signed/log.txt`：训练已到 `epoch 1000/1000`；最后 val PSNR `30.1639`，日志内最佳 val PSNR `30.5994`，出现在 epoch 407。
- 使用现有 HR 现场下采样 benchmark 协议补齐 seed1 新变体评估，没有覆盖 `results/benchmark.json`。
- 补齐 seed3 的 `LTE` benchmark，使 `results/seeds/benchmark_seed3.json` 中 `LIIF`、`LTE`、`SC-INR` 均达到 36 个 benchmark/scale 组合。
- 生成阶段性分析目录：`results/analysis/benchmark_progress_2026-05-09/`。

### 运行命令与产物

- 新增 seed1 变体 benchmark：
  - `python eval_full.py --device 1 --save_root ./save --models 'SC-INR-Signed,SC-INR+PhiZ' --output results/benchmark_seed1_with_signed_phiz.json --skip_existing`
  - 产物：`results/benchmark_seed1_with_signed_phiz.json`
- seed3 LTE benchmark 补评：
  - `python eval_full.py --device 1 --save_root ./save-seeds/seed3 --models LTE --output ./results/seeds/benchmark_seed3.json --skip_existing`
  - 产物：`results/seeds/benchmark_seed3.json`
- 派生汇总产物：
  - `results/analysis/benchmark_progress_2026-05-09/README_zh.md`
  - `results/analysis/benchmark_progress_2026-05-09/seed1_signed_phiz_summary.csv`
  - `results/analysis/benchmark_progress_2026-05-09/seed1_scale_deltas.csv`
  - `results/analysis/benchmark_progress_2026-05-09/multiseed_core_per_seed.csv`
  - `results/analysis/benchmark_progress_2026-05-09/multiseed_core_summary.csv`

### 结果摘要

- seed1 PSNR benchmark：
  - `LTE`：ID `31.0450`，OOD `22.6917`，All `25.4761`
  - `SC-INR`：ID `31.0515`，OOD `22.7471`，All `25.5152`
  - `SC-INR-Signed`：ID `31.0567`，OOD `22.7418`，All `25.5134`
  - `SC-INR+PhiZ`：ID `31.1187`，OOD `22.7766`，All `25.5573`
- seed1 差值：
  - `SC-INR - LTE`：ID `+0.0065`，OOD `+0.0554`，All `+0.0391`
  - `SC-INR-Signed - SC-INR`：ID `+0.0053`，OOD `-0.0053`，All `-0.0018`
  - `SC-INR+PhiZ - SC-INR`：ID `+0.0673`，OOD `+0.0295`，All `+0.0421`
  - `SC-INR+PhiZ - LTE`：ID `+0.0738`，OOD `+0.0849`，All `+0.0812`
- 核心三模型 3 seed 汇总：
  - `LIIF`：ID `31.0301±0.0274`，OOD `22.7251±0.0124`，All `25.4934±0.0174`
  - `LTE`：ID `31.0786±0.0296`，OOD `22.7074±0.0137`，All `25.4978±0.0190`
  - `SC-INR`：ID `31.0746±0.0203`，OOD `22.7581±0.0105`，All `25.5302±0.0137`
  - `SC-INR - LTE` 的 3 seed 均值差：ID `-0.0040`，OOD `+0.0507`，All `+0.0325`

### 证据支持的结论

- `SC-INR` 相对 `LTE` 的 OOD PSNR 优势在 3 个 seed 中较稳定，当前更适合作为主线结果：强调 decoder-side sampling consistency 对 OOD scale robustness 的帮助。
- `SC-INR+PhiZ` 在 seed1 上明显改善 PSNR，说明 feature-conditioned phase 是值得继续验证的方向；由于 phase 不依赖 cell，它不直接回到 LTE 的 cell-conditioned phase 问题。
- `SC-INR-Signed` 在参数化上更合理，但 seed1 PSNR 结果基本中性，不能据此宣称经验上优于原 `SC-INR`。

### 未解决风险与下一步

- `SC-INR+PhiZ` 还缺少 consistency、texture RMSE、continuous-scale curve、机制诊断和多 seed 复现；不能直接作为最终主模型。
- `SC-INR-Signed` 的价值目前主要是修复非负 Cartesian omega 的表达限制，仍需看 consistency/omega distribution/sinc response 诊断。
- 多 seed 统计目前只覆盖 `LIIF`、`LTE`、`SC-INR` 核心三模型；新变体仍是 seed1 preliminary。
- 后续建议优先补：`SC-INR+PhiZ` 与 `SC-INR-Signed` 的辅助指标、连续尺度曲线和关键可视化；再决定是否训练新变体多 seed。

### 专家评审

- 可靠结论：核心 `SC-INR` 的 3 seed OOD PSNR 优势比单 seed 更稳，适合进入论文主表或附表；新增 benchmark 文件完整，且使用与现有结果一致的 HR 现场下采样协议。
- 证据不足：`SC-INR+PhiZ` 目前只有 seed1 PSNR 提升，不能推导为采样一致性更好；`SC-INR-Signed` 也不能因参数化更合理就被包装成实证优势。
- 下一步合理性：先做新变体的 consistency/texture/continuous-scale 评估，比立即启动多 seed 更能判断机制是否符合论文主张；若 PhiZ 在一致性上不退化，再考虑多 seed。
- 需要防止过度宣称：不要使用 strict scale equivariance 表述；不要把 ID 上的小波动写成稳定提升；不要把 `SC-INR+PhiZ` 的 seed1 PSNR 增益写成最终结论。

## SC-INR+PhiZ 论文可发表性审稿式判断

### 完成范围

- 复核 `results/analysis/benchmark_progress_2026-05-09/README_zh.md`、`seed1_signed_phiz_summary.csv`、`seed1_scale_deltas.csv`、`multiseed_core_summary.csv`。
- 复核 `results/analysis/seed1_aux_metrics_all8/paper/README_zh.md` 和 `key_findings.md`，将 PSNR benchmark 与已有 consistency/texture 辅助证据分开解释。
- 进行主流程内模拟独立视角审查：审稿人视角、实验设计视角、方法解释视角。未启动真实 subagent，因为当前对话未明确授权真实 subagent。

### 判断摘要

- `SC-INR+PhiZ` 在 seed1 全 benchmark PSNR 上是当前表内 All/OOD 最强模型：相对 `LTE` 的 All `+0.081 dB`、OOD `+0.085 dB`，36/36 个 benchmark-scale 组合胜出。
- 相对 `SC-INR`，`SC-INR+PhiZ` 的 All `+0.042 dB`、OOD `+0.030 dB`，33/36 个组合胜出；说明 feature-conditioned phase 是有希望的增量。
- 与强 baseline `LIIF-EQ` 相比，`SC-INR+PhiZ` 的 All 只高 `+0.019 dB`，OOD 高 `+0.030 dB`，ID 基本持平且略低；如果论文定位成“明显 SOTA PSNR”，证据偏弱。
- 当前最稳的论文主张仍是：decoder-side sampling consistency 改善 OOD scale robustness。`SC-INR+PhiZ` 可以作为增强版候选，但必须补 consistency/texture/continuous-scale 和多 seed。

### 对 PSNR 和尺度外推的解释边界

- PSNR 是 fidelity 指标，能说明在当前退化和评测协议下 MSE 更低，但不能单独证明感知质量、纹理真实性或采样一致性。
- 当前 OOD scale 评估的依据不是严格 scale equivariance，而是：训练中 decoder 学到连续局部内容表示，测试时改变 output cell/coordinate；SC-INR 类方法把 cell 约束到解析 sinc observation response 中，从而减少 learned cell-conditioned extrapolation 的自由度。
- `SC-INR+PhiZ` 的 feature-conditioned phase 在理论上比 LTE 的 cell-conditioned phase 更安全，因为 phase 不直接依赖 scale/cell；但这需要通过跨尺度一致性和 phase/omega 诊断验证。

### 下一步 gate

- Gate 1：补 `SC-INR+PhiZ` 的辅助指标，要求 OOD PSNR 不退化且 consistency 相对 `LTE` 仍显著改善，不能只看 benchmark PSNR。
- Gate 2：补 `SC-INR+PhiZ` 至少 3 seed 或关键 seed 复现，确认 `+0.03~0.08 dB` 不是 seed 偶然。
- Gate 3：做 `SC-INR+PhiZ w/o sinc` 或等价 sinc 消融，证明收益不是单纯来自额外 phase 参数。
- Gate 4：重做 qualitative crops、error maps 和 continuous-scale curve，用于解释 PSNR 小幅提升是否对应可见结构/纹理改善。

### 专家评审

- 可靠结论：`SC-INR+PhiZ` 是当前 seed1 PSNR 最优候选，值得进入下一轮验证；核心 `SC-INR` 的 3 seed OOD 优势已经比单 seed 更可靠。
- 证据不足：目前不能说 `SC-INR+PhiZ` 已经论文级显著优于所有现有模型；尤其相对 `LIIF-EQ` 的 All PSNR 差距很小，且缺少多 seed 和机制指标。
- 下一步是否合理：优先做 `SC-INR+PhiZ` 的辅助指标和消融，比立刻写“最终主模型”更稳；若 consistency 被破坏，需要考虑 phase 正则、残差幅度限制或退回无 phase 主线。
- 需要防止过度宣称：PSNR 小幅提升不能等价于视觉质量明显更好；OOD scale robustness 不能写成严格尺度等变；尺度外推的解释必须绑定解析 observation response 和当前 downsampling protocol。

## SC-INR+PhiZ qualitative figure 入口与视觉证据设计

### 完成范围

- 更新 `scripts/evaluate_seed1_aux_metrics.py` 的模型路径与配色，使辅助评估脚本能识别：
  - `SC-INR`
  - `SC-INR-Signed`
  - `SC-INR+PhiZ`
- 新增 `scripts/prepare_qualitative_figure.py`，用于生成论文式局部恢复图：
  - 使用与 benchmark 一致的 HR 现场 bicubic downsampling 协议；
  - 支持 full image + crop rectangle；
  - 支持 `GT/Bicubic/LTE/SC-INR/SC-INR+PhiZ` 等方法的局部 crop；
  - 支持 error map、局部 PSNR-Y、单独 crop PNG 导出；
  - 支持手工 crop，也支持 `--auto_texture_crop` 和透明的 `--auto_delta_crop` 候选搜索。

### 运行命令与产物

- 语法检查：
  - `python -m py_compile scripts/prepare_qualitative_figure.py scripts/evaluate_seed1_aux_metrics.py`
- 自动高纹理 smoke：
  - `python scripts/prepare_qualitative_figure.py --dataset urban100 --image img_004.png --scale 8 --models Bicubic,LTE,SC-INR,SC-INR+PhiZ --auto_texture_crop --crop_size 96 --device cuda:1 --with_error_maps --show_crop_psnr --out results/analysis/qualitative_phiz_smoke --name urban100_img004_x8_auto_texture`
  - 该 crop 上 `SC-INR+PhiZ` 局部 PSNR-Y 为 `13.3548`，低于 `LTE` 的 `13.4507`，说明“高纹理自动 crop”不等于能展示方法优势。
- 局部增益候选 smoke：
  - `python scripts/prepare_qualitative_figure.py --dataset urban100 --image img_004.png --scale 8 --models Bicubic,LTE,SC-INR,SC-INR+PhiZ --auto_delta_crop --target_model SC-INR+PhiZ --baseline_model LTE --min_texture_quantile 0.50 --crop_size 96 --device cuda:1 --with_error_maps --show_crop_psnr --out results/analysis/qualitative_phiz_smoke --name urban100_img004_x8_delta_phiz_vs_lte`
  - 产物：
    - `results/analysis/qualitative_phiz_smoke/urban100_img004_x8_delta_phiz_vs_lte.png`
    - `results/analysis/qualitative_phiz_smoke/urban100_img004_x8_delta_phiz_vs_lte.pdf`
    - `results/analysis/qualitative_phiz_smoke/urban100_img004_x8_delta_phiz_vs_lte_metrics.csv`
    - `results/analysis/qualitative_phiz_smoke/urban100_img004_x8_delta_phiz_vs_lte_crop_candidates.csv`
- 该候选 crop 坐标：`y=144, x=912, size=96`。
  - `Bicubic` crop PSNR-Y `16.2966`
  - `LTE` crop PSNR-Y `19.0235`
  - `SC-INR` crop PSNR-Y `20.8491`
  - `SC-INR+PhiZ` crop PSNR-Y `21.4873`

### 方法定位记录

- 用户对方法的核心解释是合理的：相较于 LTE，将 phase 的输入从 output cell/scale 改为 feature，使 Fourier basis 的 `coef/freq/phase` 都由 content feature 预测；scale/cell 不再作为 learned phase shortcut，而是在解析 sinc observation response 中进入。
- 论文主线应表述为：feature-conditioned content Fourier basis + analytic footprint response，而不是 rotation-equivariant 方法替代品。
- Rot-E/LIIF-EQ/LTE-EQ 更适合作为 orthogonal context 或 appendix sanity comparison；主表应优先比较同一非 EQ 体系下的 `LIIF/LTE/SC-INR/SC-INR+PhiZ`。

### 下一步

- 用 `--auto_delta_crop` 在 `Urban100`、`Set14` 中筛选若干候选，再人工审查是否视觉上确实更清晰，避免只按局部 PSNR cherry-pick。
- 为正文准备两类图：
  - 局部恢复图：展示 repetitive texture / line structure 的更清晰恢复；
  - 方法独特图：same-LR cross-scale observation consistency 的 direct-vs-downsample difference map，突出 scale-decoupled observation，而不是只说视觉更好。
- 针对最终模型的消融表建议采用 factorial 设计：
  - `LTE`: feature freq/coef + cell-conditioned phase；
  - `LTE-FeaturePhase` 或 `SC-INR+PhiZ w/o sinc`: feature-conditioned phase 但无解析 sinc response；
  - `SC-INR`: feature freq/coef + no phase + analytic sinc；
  - `SC-INR+PhiZ`: feature freq/coef/phase + analytic sinc。

### 专家评审

- 可靠结论：`SC-INR+PhiZ` 的视觉图生成流程已可复现；局部增益候选图在 Urban100 img_004 x8 上能展示比 LTE/SC-INR 更低局部误差。
- 证据不足：当前 qualitative 仍是 smoke，不是最终论文图；自动挑最大 delta crop 可能引入选择偏差，必须保留候选 CSV，并用人工视觉审查与全局指标约束。
- 下一步是否合理：先建立透明的候选筛选流程，再选 2-3 张正文图，是比直接手工挑图更稳的论文策略。
- 需要防止过度宣称：局部视觉图只能说明代表性案例，不证明平均视觉质量；需要与 texture RMSE、SSIM、consistency 和多 seed 共同支撑。

## 批量筛选 SC-INR+PhiZ qualitative 候选图与更新 subagent 工作流

### 完成范围

- 按用户要求启用真实 subagent：
  - qualitative 视觉证据审查 subagent：检查 `auto_delta_crop` 是否容易 cherry-pick，给出筛图标准和补充指标建议。
  - 全局工作流审查 subagent：检查 `/home/admin/.codex/AGENTS.md` 中 subagent 规则是否过硬，并给出弹性化修改建议。
- 搜索本地 skill 库，当前与科研/论文/subagent 最相关的是 `research-coding-workflow`，其中 `references/task-orchestration.md` 与 `references/paper-writing.md` 已覆盖多视角审查、论文证据审计和 subagent 角色建议。
- 搜索公开资料，参考 Claude Code subagents 文档中“独立上下文、专门角色、自动委派、动态选择、工具权限限制”的原则；未把外部产品细节写成项目事实。
- 更新全局工作流 `/home/admin/.codex/AGENTS.md`：将 subagent 规则改为由 Codex 按任务复杂度、科研风险、不可逆成本、可并行性和授权情况动态决定数量与角色；简单任务从简，复杂科研/论文任务加强。

### qualitative 候选图产物

- 批量生成目录：`results/analysis/qualitative_phiz_candidates/`
- 总览入口：`results/analysis/qualitative_phiz_candidates/README_zh.md`
- 快速人工审查图：`results/analysis/qualitative_phiz_candidates/contact_sheet.png`
- 汇总表：`results/analysis/qualitative_phiz_candidates/candidate_summary.csv`
- 生成方法：`scripts/prepare_qualitative_figure.py`
- 方法列：`GT`、`Bicubic`、`LIIF`、`LTE`、`SC-INR`、`SC-INR+PhiZ`
- 筛选参数：`--auto_delta_crop --target_model SC-INR+PhiZ --baseline_model LTE --min_texture_quantile 0.50 --crop_size 96`

### 候选结果摘要

- `urban100_img012_x8_delta_phiz_vs_lte`：`PhiZ-LTE +2.78 dB`，`PhiZ-SC-INR +3.61 dB`
- `urban100_img004_x8_delta_phiz_vs_lte`：`PhiZ-LTE +2.46 dB`，`PhiZ-SC-INR +0.64 dB`
- `urban100_img018_x8_delta_phiz_vs_lte`：`PhiZ-LTE +2.29 dB`，`PhiZ-SC-INR -0.00 dB`
- `set14_ppt3_x8_delta_phiz_vs_lte`：`PhiZ-LTE +2.12 dB`，`PhiZ-SC-INR +1.57 dB`
- `urban100_img004_x16_delta_phiz_vs_lte`：`PhiZ-LTE +1.94 dB`，`PhiZ-SC-INR +1.51 dB`
- `set14_barbara_x16_delta_phiz_vs_lte`：`PhiZ-LTE +1.06 dB`，`PhiZ-SC-INR +0.47 dB`
- 其余 `zebra/barbara/comic` x8 候选是中等或小增益，更适合作为中性/补充样例。

### subagent 审查要点

- qualitative 审查结论：`auto_delta_crop` 是候选探索工具，不应直接当论文筛图协议；top-1 局部增益远大于全图平均，存在 cherry-pick 风险。正式论文应冻结候选池，并保留优势、中性和失败/不明显样例。
- 工作流审查结论：用户最新偏好属于跨项目规则，适合进入全局工作流；但规则过于弹性也可能导致复杂任务审查不足，因此保留 L3/L4 任务升级审查和记录 gate。

### 下一步

- 用户人工优先检查：
  - `contact_sheet.png`
  - `urban100_img004_x8_delta_phiz_vs_lte.png`
  - `urban100_img012_x8_delta_phiz_vs_lte.png`
  - `set14_ppt3_x8_delta_phiz_vs_lte.png`
  - `urban100_img004_x16_delta_phiz_vs_lte.png`
- 若用户确认视觉效果可信，下一步应补 `SC-INR+PhiZ` 的辅助指标：quality/texture/SSIM/consistency，与视觉图保持证据一致。
- 正式论文图建议同时准备：
  - 局部恢复图，展示纹理/结构；
  - same-LR cross-scale consistency difference map，展示 scale-decoupled observation 的独特性。

### 专家评审

- 可靠结论：批量候选图、局部指标和 crop 坐标均可复现；全局 subagent 工作流已按用户要求改成动态角色/数量策略。
- 证据不足：候选图仍按 `PhiZ-LTE` 局部增益排序，不能单独证明平均视觉质量；需要用户人工确认和后续感知/纹理/一致性指标约束。
- 下一步是否合理：先由用户筛 2-3 个视觉可信案例，再补 PhiZ auxiliary metrics 和 sinc/phase 消融，比马上重构工作区更稳。
- 需要防止过度宣称：不能把局部 top-delta 图写成普遍视觉优势；不能把 subagent 工作流弹性化理解为每个简单任务都要并行审查。

## 工作区重构只读审查

### 完成范围

- 用户确认两张视觉候选图可作为初期视觉证据：
  - `results/analysis/qualitative_phiz_candidates/urban100_img012_x8_delta_phiz_vs_lte.png`
  - `results/analysis/qualitative_phiz_candidates/urban100_img004_x8_delta_phiz_vs_lte.png`
- 按 L4 论文/工作区重构任务处理，冻结本轮为只读审查和方案设计，不移动、不删除、不重命名任何文件。
- 启动三个真实 subagent 做只读审查：
  - 代码与配置审查；
  - 结果与产物组织审查；
  - 论文/记忆/叙事审查。
- 主流程同步盘点目录、结果、脚本、日志、文档和命名混乱点。

### 主要发现

- 当前最大问题不是缺少结果，而是正式 raw、派生分析、smoke/debug、候选图和历史报告混放，容易误引用。
- `SC-INR` 展示名已经替代 `SC-INR-Adaptive`，但训练配置、checkpoint 目录、部分脚本和旧文档仍使用旧名；不应硬改 checkpoint 路径，应通过 manifest/alias 层统一。
- `SC-INR+PhiZ` 命名最易混淆：配置是 `train-sc-inr-signed-phiz.yaml`，registry 是 `sc_inr_signed_phiz`，保存目录是 `save/sc-inr-phiz`。
- `results/analysis/benchmark_progress_2026-05-09/` 是当前 benchmark 最新入口；`results/analysis/benchmark_seed_mean_std.csv` 已被替代，不应作为多 seed 表引用。
- `results/analysis/seed1_aux_metrics_all8/paper/` 是当前机制/一致性论文表图入口，但仍以 `SC-INR-Adaptive` 旧名为主，需要后续统一展示名。
- `results/analysis/qualitative_phiz_candidates/` 是视觉候选池，不是正式论文图；两张用户确认图可进入候选主线，但仍需标注筛选协议和 cherry-pick 风险。
- `PHASE0_1_REPORT.md` 是历史阶段报告，部分结论与 5 月 9 日多 seed / PhiZ 进展不一致，应归档或显著标旧。
- `eval_continuous.py`、`analyze_phase_consistency.py` 等脚本存在硬编码路径或旧模型表，容易产出旧命名结果。
- `scripts/run_key_3seed_experiments.sh` 是历史后台训练 launcher，当前不宜作为通用入口。

### 重构原则

- 第一阶段先做逻辑重构：manifest、README、别名表、证据矩阵；不要先物理移动 checkpoint 或 raw JSON。
- 所有 raw 结果保留原路径，新增 `status` 标签区分 `formal_raw`、`derived`、`candidate`、`smoke`、`deprecated`。
- 所有展示表统一使用 `SC-INR`、`SC-INR-Signed`、`SC-INR+PhiZ`，但 manifest 保留原始 key、registry name、checkpoint dir 和 config path。
- 历史和 smoke 产物先通过 README 降级，不直接删除；等论文证据链稳定后再考虑归档目录。
- 重构应优先服务论文写作：先建立 `claims_evidence_matrix`，再重画表图和整理消融。

### 专家评审

- 可靠结论：三个审查视角一致认为应先建立 manifest/alias/README 入口，而不是直接搬文件；这能最大限度保护 checkpoint 和 raw 结果可追溯性。
- 证据不足：尚未实际执行重构，因此当前仍存在误引用风险；下一步需要先创建清单文件和证据矩阵。
- 下一步是否合理：建议先新增 `results/MANIFEST.json`、`results/README_zh.md`、`docs_or_paper/claims_evidence_matrix.md` 或等价入口，再逐步更新脚本模型别名。
- 需要防止过度宣称：两张视觉图是“用户确认的初期视觉证据”，不是平均视觉质量证明；PhiZ 仍需 auxiliary metrics 和消融闭环。

## 重构方案再审视：从论文收口升级为论文级研究工程

### 用户反馈

- 用户指出上一版重构更像“适合发论文的收口整理”，但不能根治项目长期可查、可读、可扩展的问题。
- 新目标应是把工作区重构成真正论文级研究工程：不仅服务当前投稿，也服务后续消融、多 seed、Rot-E 正交结合、LIIF+ 扩展、复现和长期维护。

### 新的重构判断

- 仅新增 `paper/`、`README`、`MANIFEST` 属于表层治理，能降低误引用，但不能解决多脚本硬编码模型表、配置命名不一致、实验生命周期缺失、结果状态不可机器校验等根因。
- 根因包括：
  - 缺少统一 project contract：数据协议、模型别名、checkpoint、配置、评估协议、结果状态没有统一 schema。
  - 缺少 experiment registry：训练、评估、分析、论文表图之间没有一张可追溯的实验卡。
  - 缺少 artifact lifecycle：raw / derived / candidate / smoke / deprecated 只是人工约定，还没有被脚本和目录结构约束。
  - 缺少 validation layer：指标/consistency/qualitative 筛图没有统一 gate 和属性检查。
  - 旧论文叙事、旧脚本和新主线并存，缺少 legacy boundary。

### 修正后的重构方向

- 将项目从“脚本堆叠型研究仓库”重构为“registry-driven research project”。
- 先定义稳定接口与清单，再逐步迁移脚本；不先移动 checkpoint/raw result。
- 目标状态应包含：
  - `project/` 或 `configs/registry/` 下的模型、数据、实验、结果 schema；
  - `experiments/` 下按问题组织的实验卡，而不是按临时日期散落；
  - `artifacts/` 或 `results/` 下有状态分层和 manifest；
  - `paper/` 下有 claim-evidence ledger、主表图、补充材料和禁止表述；
  - `archive/legacy/` 明确隔离历史报告、旧脚本、smoke 输出。

### 专家评审

- 可靠结论：用户指出的问题成立；上一版方案能短期收束论文证据，但不足以作为长期工程重构方案。
- 证据不足：尚未执行实际重构，仍需先做 schema/目录 RFC，再实施最小迁移。
- 下一步是否合理：先写一份 `docs/refactor_rfc_zh.md` 或等价 RFC，冻结目标架构、迁移顺序和兼容策略，再开始动文件。
- 需要防止过度工程：不要一上来引入大型框架或重写训练系统；优先用轻量 registry、experiment card 和 manifest 统一现有代码。

## 论文级研究工程重构实施

### 完成范围

- 按用户授权执行完整工作区重构，目标从“论文收口整理”升级为“长期可查、可读、可扩展的 registry-driven research project”。
- 建立 canonical 目录：
  - `src/`：模型、数据集和工具源码；
  - `entrypoints/`：训练、测试、benchmark、continuous、diagnostics 入口；
  - `scripts/analysis/`、`scripts/paper/`、`scripts/viz/`、`scripts/legacy/`：按用途拆分脚本；
  - `artifacts/`：checkpoint/raw result/derived/smoke/legacy/results compatibility；
  - `experiments/`：实验卡；
  - `paper/`：claim-evidence 与引用白名单；
  - `docs/`：项目文档和历史报告。
- 保留旧路径兼容 symlink：`train.py`、`test.py`、`eval_full.py`、`models`、`datasets`、`utils.py`、`save`、`save-seeds`、`results`、`logs` 等。
- 将结果产物分层：
  - formal raw：`artifacts/raw_results/`;
  - derived：`artifacts/derived/`;
  - smoke：`artifacts/smoke/`;
  - legacy：`artifacts/legacy/`;
  - compatibility：`artifacts/results/`。
- 新增/更新项目治理文档：
  - `README.md`
  - `MANIFEST.md`
  - `docs/refactor_rfc_zh.md`
  - `artifacts/README_zh.md`
  - `artifacts/raw_results/README_zh.md`
  - `artifacts/derived/README_zh.md`
  - `artifacts/checkpoints/README_zh.md`
  - `artifacts/results/README_zh.md`
  - `configs/registry/models.yaml`
  - `configs/registry/protocols.yaml`
  - `experiments/TEMPLATE.md`
  - `experiments/phiz/experiment_card.md`
  - `experiments/core_sc_inr_multiseed/experiment_card.md`
  - `paper/claims_evidence_matrix.md`
  - `paper/ARTIFACTS_ALLOWED.md`

### 兼容性修复

- 更新移动后的入口和脚本的 `ROOT` / `sys.path` 逻辑，使旧根目录命令和 canonical 入口均可用。
- 修复 `configs/test/*.yaml` 的数据路径：从错误/不一致的 `../../Data/...` 统一为当前项目约定的 `../Data/...`，已确认指向 `/workspace/SE-INR/Data/...`。
- 将脚本默认展示名从 `SC-INR-Adaptive` 迁移到 `SC-INR`，但保留旧别名读取：
  - `entrypoints/eval_continuous.py`
  - `scripts/analysis/evaluate_seed1_aux_metrics.py`
  - `scripts/analysis/analyze_sampling_response.py`
  - `scripts/paper/prepare_seed1_aux_paper_artifacts.py`
- 重新生成 `seed1_aux_metrics_all8/paper` 下的轻量 paper artifacts，使表格/`key_findings.md` 使用 `SC-INR` 展示名。
- 修复 `scripts/legacy/run_key_3seed_experiments.sh` 在 canonical 路径直接运行时的 project root 推断，并把 eval 模型名更新为 `SC-INR`。

### 运行过的验证

- 语法/导入检查：
  - `python -m py_compile train.py test.py eval_full.py eval_continuous.py eval_fce.py eval_phase_intervention.py analyze_phase_consistency.py scripts/analyze_results.py scripts/analyze_sampling_response.py scripts/evaluate_seed1_aux_metrics.py scripts/prepare_seed1_aux_paper_artifacts.py scripts/prepare_qualitative_figure.py scripts/smoke_check_signed_omega.py`
- CLI smoke：
  - `python eval_full.py --help`
  - `python eval_continuous.py --help`
  - `python test.py --help`
  - `python train.py --help`
  - `python scripts/prepare_qualitative_figure.py --help`
  - `python scripts/analyze_sampling_response.py --help`
- 派生分析 smoke：
  - `python scripts/analyze_results.py --out /tmp/seinr_analysis_smoke`
  - `python scripts/prepare_seed1_aux_paper_artifacts.py`
- 路径验证：
  - `find . -xtype l -print` 无断链输出；
  - `results/benchmark.json`、`results/benchmark_seed1_with_signed_phiz.json`、`results/seeds/benchmark_seed2.json`、`results/seeds/benchmark_seed3.json` 均可读；
  - `save/lte/epoch-best.pth`、`save/sc-inr-phiz/epoch-best.pth`、`save/sc-inr-adaptive-signed/epoch-best.pth`、`save-seeds/seed3/lte/epoch-best.pth` 均存在；
  - 用户确认的两张 qualitative 候选图在新旧路径下均可访问；
  - `configs/test/*.yaml` 的 `../Data/.../HR` 路径均存在。
- 进程核对：
  - 未发现仍在运行的 `train.py`、`eval_full.py`、`eval_continuous.py` 或 legacy nohup pipeline。

### 独立审查摘要

- 代码/入口审查指出：
  - legacy launcher 的 root 推断有风险，已修复；
  - config 中相对数据路径依赖从 repo root 运行，当前训练/测试工作流可接受，但长期更好方案是入口统一解析 config-relative 或 project-root-relative path；
  - repo-local `Data -> artifacts/data_local` 与正式默认数据根 `/workspace/SE-INR/Data` 不同，已在 README 中标注 repo-local `Data` 非 authoritative。
- 结果/证据审查指出：
  - `results/` 兼容树可能诱导误引用，已新增 `artifacts/results/README_zh.md` 和 `paper/ARTIFACTS_ALLOWED.md`；
  - `benchmark_seed_mean_std.csv` 等旧 overview 文件仍存在误用风险，当前已在 overview README 和白名单中明确不得作为当前多 seed 表；
  - qualitative candidate 仍是 candidate，不是 final paper figure。

### 证据支持的结论

- 当前重构后，核心旧命令没有明显断裂；主要源码、入口、结果和文档已经形成稳定分区。
- 新结构比原始工作区更适合后续消融和论文写作：raw、derived、smoke、legacy 的边界更清楚，模型别名和论文 claim 也有独立清单。
- `SC-INR` 展示名已进入主要新文档和脚本默认路径；旧 checkpoint/config 目录仍保留历史名，避免破坏已有结果追溯。

### 未解决风险与下一步

- 当前是大规模搬迁，`git status` 预期会显示大量 delete/type-change/untracked；提交前应按逻辑分批审阅，而不是混成一个不可读提交。
- 仍有部分历史文档如 `docs/project/project_story.md` 保留旧叙事和旧路径，它们应被视为历史/项目故事材料，不是当前论文事实源。
- 训练和测试 configs 仍依赖从 repo root 启动；后续可在 entrypoints 中加入统一 project-root path resolver，减少 cwd 依赖。
- `Data` 本地兼容 symlink 指向 `artifacts/data_local`，而正式数据协议使用 `/workspace/SE-INR/Data`；后续如果要公开仓库，应决定是否保留这个本地 symlink 或改成文档提示。
- 下一步科研工作应转向：
  1. `SC-INR+PhiZ` auxiliary metrics；
  2. `SC-INR+PhiZ w/o sinc` 消融；
  3. continuous-scale / sampling response / omega distribution diagnostics；
  4. 把确认过的两张 qualitative 图晋级为正式 figure draft，并补筛选协议说明。

### 专家评审

- 可靠结论：本轮重构解决了工作区最严重的混放问题，并通过语法、CLI、JSON、symlink、数据路径和关键 checkpoint 路径验证；旧入口仍可作为兼容层使用。
- 证据不足：这不是完整软件工程迁移测试；没有跑实际 benchmark/training，也没有验证所有历史脚本的全功能行为。legacy 脚本仍只适合作历史参考，不应恢复为主入口。
- 下一步是否合理：先基于新结构推进 PhiZ auxiliary metrics 和 sinc/phase 消融，比继续扩大目录治理更有科研收益；若再重构，应优先做 registry-driven evaluator，而不是继续手工搬目录。
- 需要防止过度宣称：工作区整理提高的是可追溯性和复现管理，不是新的实验结论；qualitative 候选图仍不能证明平均视觉质量；SC-INR 仍应表述为 decoder-side sampling consistency，而不是严格 scale equivariance。

## 推送后项目组织与论文冗余度复核

### 完成范围

- 在用户完成项目推送后，对当前仓库做只读复核，重点检查：
  - 顶层目录与 symlink 组织；
  - README / MANIFEST / registry / experiment card / paper claim ledger 的一致性；
  - entrypoints、scripts、configs 的可用性和路径风险；
  - artifacts 的 raw/derived/smoke/legacy 分层；
  - 论文视角下哪些内容必要、哪些内容冗余或仅适合内部保留。
- 本轮没有移动、删除或重命名文件。

### 运行过的检查

- `git status --short`：干净，无未提交改动。
- `find . -xtype l -print`：无断链。
- `du -sh`：仓库约 `4.0G`，其中 `artifacts/checkpoints` 约 `3.7G`，`artifacts/logs` 约 `270M`，`artifacts/derived` 约 `20M`。
- `python -m py_compile ...`：主要 train/test/eval/analysis/viz/paper 脚本通过。
- `python eval_full.py --help`、`python scripts/prepare_qualitative_figure.py --help`：入口 smoke 通过。
- `configs/registry/models.yaml`、`configs/registry/protocols.yaml` 可解析。
- `results/benchmark.json`、`results/benchmark_seed1_with_signed_phiz.json`、`results/seeds/benchmark_seed2.json`、`results/seeds/benchmark_seed3.json` 可读。
- `configs/test/*.yaml` 指向的 `../Data/.../HR` 路径均存在。

### 主要判断

- 当前目录结构已经比重构前清晰很多，能够支持后续论文实验推进：
  - `src/`、`entrypoints/`、`scripts/`、`configs/registry/` 的工程职责基本明确；
  - `artifacts/raw_results` 与 `artifacts/derived` 的边界清楚；
  - `paper/claims_evidence_matrix.md` 和 `paper/ARTIFACTS_ALLOWED.md` 能约束论文表述；
  - root 兼容 symlink 没有断裂。
- 从论文冗余度看，仍有几个需要后续收紧的问题：
  1. `artifacts/smoke/` 虽被标记为不可引用，但当前有文件被 Git 跟踪；它不应长期保留在主分支作为正式项目内容。
  2. `docs/project/project_story.md` 位于 project 文档区，但内容仍大量使用 `SC-INR-Adaptive` 旧名和旧结果叙事；应更新为当前 project story，或移动/降级到 archive。
  3. `artifacts/derived/analysis/overview/` 混有旧 overview 表和当前阶段性入口；虽然 README 已说明 caveat，但最好后续拆成 `current_seed1` 与 `deprecated`。
  4. `artifacts/derived/paper_candidates/qualitative_phiz_candidates/` 对内部筛图有价值，但不应整体作为论文产物；最终只应把确认后的 1-2 张图晋级到 `paper/figures/` 或白名单。
  5. `configs/registry/models.yaml` 目前是文档/别名层，`eval_full.py`、aux scripts 等仍各自维护模型表；长期会产生 drift，应逐步让脚本读取 registry。
  6. 根目录 `Data -> artifacts/data_local` 与真实数据根 `/workspace/SE-INR/Data` 不一致，README 已说明，但公开仓库时可能仍误导；后续可移除该 symlink 或替换为数据说明文档。
  7. 工作区存在 `__pycache__` 目录，未进入 Git，但视觉上可清理。

### 内容保留建议

- 必须保留：`src/`、`entrypoints/`、`configs/`、`configs/registry/`、`artifacts/raw_results/`、当前 `artifacts/derived/analysis/benchmark_progress_2026-05-09/`、`artifacts/derived/analysis/seed1_aux_metrics_all8/`、`experiments/`、`paper/`、`memory/`。
- 内部保留但不应引用：`artifacts/legacy/`、`artifacts/derived/analysis/overview/`、qualitative candidate pool。
- 建议移出 Git 或忽略：`artifacts/smoke/`、`__pycache__`。
- 后续可降级/归档：旧 `docs/project/project_story.md`，除非重写为当前正式 project story。

### 专家评审

- 可靠结论：当前项目已经达到“内部论文研究工程”的基本标准，不再是无序脚本和结果堆；核心证据入口、claim 边界和兼容路径都能被定位。
- 证据不足：还不能说已经达到“公开 release 级别”的干净程度，因为 smoke、legacy、candidate 和旧 project story 仍会增加读者认知负担。
- 下一步是否合理：不建议再做大规模搬迁；建议做一次小型降噪 PR：取消 tracked smoke、清理 `__pycache__`、重写或归档 `project_story`、为 registry-driven evaluator 建最小入口。
- 需要防止过度宣称：项目组织变清楚不等于论文证据已经闭环；PhiZ 仍需 auxiliary metrics、多 seed 或 sinc 消融支撑，视觉候选仍只能作为 selected examples。

## 命名体系收束与重构后复核修正

### 完成范围

- 按用户提出的叙事需求，进一步冻结模型展示命名：
  - 最终候选 `SC-INR-PhaseZ` / raw key `SC-INR+PhiZ` 在论文展示层命名为 `SC-INR`；
  - 旧 no-phase adaptive 主线命名为 `SC-INR-NoPhi`；
  - signed omega 但无 phase 的版本命名为 `SC-INR-NoPhi-Signed`；
  - fixed frequency 版本命名为 `SC-INR-FixedOmega`；
  - `LTE-NoCell` / `LTE-NoC` 命名为 `LTE-NoCellPhase`；
  - `LTE-FeaturePhase` 命名为 `LTE-PhaseZ`。
- 新增 `paper/model_taxonomy.md`，并更新 `configs/registry/models.yaml`，把 paper-facing display name 与 raw key / checkpoint dir / config path 分离。
- 更新 `README.md`、`MANIFEST.md`、`docs/project/project_story.md`、`paper/ARTIFACTS_ALLOWED.md`、`paper/claims_evidence_matrix.md`、experiment card、artifact README，使当前入口从阶段性 `benchmark_progress_2026-05-09/` 转向 `artifacts/derived/analysis/overview/`，但保留阶段性汇总作为历史记录。
- 修复 `entrypoints/eval_phase_intervention.py`：此前 `--help` 会直接启动完整评估，现已改成正式 `argparse` CLI，并显式支持 `--device`、`--save_root`、`--data_root`、`--output`、`--benchmarks`、`--scales`。
- 修复 `scripts/analysis/analyze_results.py` 的 `SC-INR` 歧义处理：只在已知旧结果文件或同时包含 raw key `SC-INR+PhiZ` 的文件中，把 raw key `SC-INR` 解释为 `SC-INR-NoPhi`；避免未来已改名后的最终 `SC-INR` 结果被误归类。
- 让 `scripts/analyze_results.py` 默认优先读取 `results/benchmark_seed1_with_signed_phiz.json` 作为 seed1 enriched benchmark，并重生成 `artifacts/derived/analysis/overview/` 下的表格和图。
- 将 `artifacts/smoke/` 加入项目 `.gitignore` 并取消 Git 跟踪；本地 smoke 文件仍保留，不作为论文证据。
- 更新 `/workspace/SE-INR/.gitignore`，把 `.claude/`、`.roo/`、`.hermes-venv/` 作为本地工具状态忽略，避免未来误提交。

### 运行过的命令与结果

- `python -m py_compile train.py test.py eval_full.py eval_continuous.py eval_fce.py eval_phase_intervention.py analyze_phase_consistency.py scripts/analysis/analyze_results.py scripts/analysis/analyze_sampling_response.py scripts/analysis/evaluate_seed1_aux_metrics.py scripts/paper/prepare_seed1_aux_paper_artifacts.py scripts/viz/prepare_qualitative_figure.py scripts/analysis/smoke_check_signed_omega`：通过。
- `python eval_full.py --help`、`python eval_continuous.py --help`、`python eval_fce.py --help`、`python eval_phase_intervention.py --help`、`python scripts/analysis/analyze_results.py --help`、`python scripts/viz/prepare_qualitative_figure.py --help`、`python scripts/analysis/analyze_sampling_response.py --help`：通过；其中 `eval_phase_intervention.py --help` 的误触发评估问题已修复。
- `python scripts/analyze_results.py --out artifacts/scratch/refactor_validation_analysis`：通过，验证模型规范化后包含 `SC-INR`、`SC-INR-NoPhi`、`SC-INR-NoPhi-Signed`。
- `python scripts/analyze_results.py --out artifacts/derived/analysis/overview`：通过，正式 overview 已更新。
- YAML 解析检查：`configs/registry/models.yaml`、`configs/registry/protocols.yaml` 均可解析。
- `find . -xtype l -print`：无断链。
- `git ls-files artifacts/smoke`：无输出，说明 smoke 产物已不再被 Git 跟踪。
- 进程检查未发现仍在运行的 `python eval*`、`python train*` 或 `python scripts/*` 任务。

### 证据支持的结论

- 用户提出的“最终模型叫 `SC-INR`，其他同族版本作为消融”的方向是合理的，但范围必须清楚：只有 SC-INR 结构轴上的 `NoPhi`、`NoPhi-Signed`、`FixedOmega`、未来 `w/o sinc` 属于 SC-INR 消融。
- 不建议把 `LIIF`、`LTE`、`LIIF-EQ`、`LTE-EQ` 改成 `SC-INR-*`，它们是 baseline 或 Rot-E 正交 baseline；`LTE-NoCellPhase` 和 `LTE-PhaseZ` 也应保留 LTE 前缀，因为它们诊断的是 LTE phase 机制。
- `LTE-P` 不推荐作为正式名，语义不唯一，容易被理解成 phase、parameter 或 proposed；`LTE-NoCellPhase` 更适合论文和审稿人阅读。
- 当前 overview 的 `benchmark_summary.csv` 显示最终候选 `SC-INR` seed1 OOD 平均为 `22.7766`，高于 `LTE` 的 `22.6917` 和 `SC-INR-NoPhi` 的 `22.7471`；但 `SC-INR` 仍只有 seed1，不能写成最终多 seed 赢家。
- 顶层 `.claude` 是本地助手规则，`.roo` 是本地 agent/MCP 配置，`.hermes-venv` 是本地运行时；它们不属于 `Equivariant-ASISR` 论文代码项目，不应进入论文仓库。`.roo/mcp.json` 和顶层 `.mcp.json` 命中密钥类配置，应视为本地敏感文件。

### 未解决风险与下一步

- 目前仍有多处脚本手写模型别名表，虽然已同步本轮命名，但长期会 drift；下一步应让 evaluator/analysis 从 `configs/registry/models.yaml` 读取，或至少增加 registry consistency check。
- `artifacts/derived/analysis/overview/benchmark_seed_mean_std.csv` 现在可作为“当前 overview”，但其中最终候选 `SC-INR` 只有 seed1；不能把该表误读为最终候选的 3-seed 统计。
- 旧 raw key `SC-INR` 的历史歧义仍存在；论文表格脚注必须写清楚：`SC-INR` = final PhiZ variant，`SC-INR-NoPhi` = old raw `SC-INR` / `SC-INR-Adaptive`。
- 后续科研优先级仍应是 `SC-INR` auxiliary metrics、多 seed 或至少关键 seed 补评、`SC-INR w/o sinc` 消融、以及把两张用户确认的视觉图晋级为正式 figure draft。

### 专家评审

- 可靠结论：当前展示命名比之前更适合论文叙事，且没有物理重命名 checkpoint/raw result，保留了可追溯性；`eval_phase_intervention.py` 的 CLI 风险已实际修复；overview 派生产物已由脚本重生成并通过基础验证。
- 证据不足：这轮调整主要是组织和展示层修正，不产生新的实验结论；最终候选 `SC-INR` 仍缺少多 seed 和 auxiliary metrics 证据。
- 下一步是否合理：建议停止继续扩大目录重构，转向 registry-driven evaluator 小修、PhiZ auxiliary metrics、w/o sinc 消融和论文图表晋级。
- 需要防止过度宣称：不能因为最终候选 seed1 指标和两张视觉图好，就写成“视觉质量整体更好”或“最终显著优于所有模型”；应表述为 seed1 preliminary + selected qualitative evidence，并用后续消融和多指标闭环支撑。

## 命名细节收口、SC-INR 实验记录修订与论文草稿

### 完成范围

- 删除用户明确认为无需保留的 `docs/refactor_rfc_zh.md`；重构过程已经记录在每日记忆和 `MANIFEST.md` / `README.md` 中。
- 恢复根目录兼容入口 `PROJECT_STORY.md -> docs/project/project_story.md`。
- 清理本轮编译/语法检查产生的 `__pycache__`。
- 新增推荐训练配置 alias，避免新文档继续使用旧文件名：
  - `configs/train-div2k/train-sc-inr.yaml -> train-sc-inr-signed-phiz.yaml`
  - `configs/train-div2k/train-sc-inr-nophi.yaml -> train-sc-inr-adaptive.yaml`
  - `configs/train-div2k/train-sc-inr-nophi-signed.yaml -> train-sc-inr-adaptive-signed.yaml`
  - `configs/train-div2k/train-sc-inr-fixed-omega.yaml -> train-sc-inr-fixed.yaml`
  - `configs/train-div2k/train-lte-nocellphase.yaml -> train-lte-no-cell.yaml`
  - `configs/train-div2k/train-lte-phasez.yaml -> train-lte-feature-phase.yaml`
- 更新 `configs/registry/models.yaml`，让 `train_config` 指向新展示名 alias，同时保留 `legacy_train_config`。
- 更新 `artifacts/checkpoints/README_zh.md`，明确 `save/...` 是 legacy 兼容路径，canonical checkpoint 路径在 `artifacts/checkpoints/seed1/...`。
- 更新 `paper/model_taxonomy.md`，表格改用 canonical checkpoint 路径，并说明 `save/...` 只是兼容 symlink。
- 修订 `artifacts/derived/analysis/benchmark_progress_2026-05-09/` 下 README 和 CSV 展示名：
  - 旧多 seed `SC-INR` 改为 `SC-INR-NoPhi`；
  - `SC-INR-Signed` 改为 `SC-INR-NoPhi-Signed`；
  - `SC-INR+PhiZ` 改为最终候选 `SC-INR`；
  - delta 字段改为 `sc_inr_minus_nophi`、`nophi_signed_minus_nophi`、`nophi_minus_lte`。
- 重写 `docs/project/SC-INR_Exp.md`：
  - 明确它是内部技术与实验记录；
  - 补入最终候选 `SC-INR` 的 signed omega、feature-conditioned `phi(z)`、sinc observation response、坐标/cell 单位；
  - 区分 `SC-INR-NoPhi` 的 3-seed/auxiliary 稳证据和最终候选 `SC-INR` 的 seed1 preliminary；
  - 删除旧文中把 no-phase 结果直接称为 `SC-INR` 的误导。
- 新建可编译论文草稿：
  - `paper/draft/main.tex`
  - `paper/draft/references.bib`
  - `paper/draft/README_zh.md`
  - `paper/draft/main.pdf`
  - `paper/draft/figures/urban100_img012_x8.png`
  - `paper/draft/figures/urban100_img004_x8.png`

### 运行过的命令与结果

- LaTeX 编译：
  - `cd paper/draft && pdflatex -interaction=nonstopmode main.tex && bibtex main && pdflatex -interaction=nonstopmode main.tex && pdflatex -interaction=nonstopmode main.tex`
  - 输出 `paper/draft/main.pdf`，4 页。
  - `rg "Warning|Overfull|Underfull|undefined|Error" main.log main.blg` 无输出；随后清理了 LaTeX aux/log/bbl/blg/out 中间文件。
- PDF 视觉检查：
  - 使用 `mutool draw -r 120` 渲染并检查页面；表格、公式和两张 qualitative 图没有发现明显重叠或引用漂移到参考文献后的问题。
- 结构检查：
  - YAML 解析：`configs/registry/models.yaml`、`configs/registry/protocols.yaml` 均通过。
  - 新配置 alias 均可解析到对应 legacy config。
  - `find . -xtype l -print` 无断链。
  - `python -m py_compile ...` 覆盖主要 train/test/eval/analysis/viz/paper 脚本，通过。
  - 进程检查未发现仍在运行的 `python train/eval/scripts` 任务。

### 证据支持的结论

- `SC-INR_Exp.md` 原版本存在实质性误导：其中名为 `SC-INR` 的主表和公式实际对应现在的 `SC-INR-NoPhi`，未包含最终候选 `phi(z)` 与 signed omega。已通过重写修正。
- checkpoint 目录不应物理改名；更合理做法是使用配置 alias、registry 和 README 显示层统一。物理移动/改名会破坏已有日志、checkpoint、raw JSON 和旧命令。
- 当前论文草稿能够作为“看效果”的最小学术版本，但仍是内部草稿。它严格区分：
  - `SC-INR-NoPhi`：3-seed OOD + consistency 稳证据；
  - `SC-INR`：seed1 PSNR + selected qualitative preliminary。

### 工作流复盘

- 本轮暴露出一个重要流程问题：大重构后不能只看目录是否清楚，还必须做“语义一致性审计”，尤其是同一个名字在 raw JSON、checkpoint、论文展示名和历史文档中是否指向同一个模型。
- 对科研项目，重命名不应只改 README；必须同时检查：
  - registry；
  - config 文件名或 alias；
  - checkpoint/readme；
  - raw/derived 表格；
  - paper claim ledger；
  - 实验卡；
  - 旧技术记录；
  - 代码注释和 smoke 脚本。
- 工作流中还缺一个可复用的“rename migration checklist”。建议后续全局工作流加入：凡模型展示名调整，必须生成 alias map、legacy map、禁止物理改名项、可物理改名项、脚本入口兼容检查和 claim 文档同步检查。
- 论文写作流程还应更早要求“claim-evidence ledger first, manuscript second”。这次先修 claim 边界再写草稿，避免了把 final `SC-INR` 写成已完成多 seed 结论。
- 对 PDF 草稿，必须编译后视觉检查，而不是只看 LaTeX exit code；第一次编译中 figure/table 浮动到了参考文献附近，视觉检查发现后改为 `[H]` 固定位置。

### 未解决风险与下一步

- 草稿引用目前只放了 LIIF 和 LTE 的基础引用，后续正式相关工作仍需补 Rot-E、EDSR/DIV2K、MetaSR/CiaoSR/mip-NeRF/抗混叠相关工作等，并逐条核验。
- `paper/draft/main.tex` 是内部草稿，不应直接投稿；下一步应拆成正式 `paper/src/` 或目标会议模板，并补完整 related work / implementation details / supplementary。
- 仍有脚本内 hard-coded alias map，长期应改为读取 `configs/registry/models.yaml`。
- 下一步实验仍应优先：最终候选 `SC-INR` auxiliary metrics、`SC-INR w/o sinc`、必要 multi-seed。

### 专家评审

- 可靠结论：本轮已经把最危险的命名歧义从技术记录、配置 alias、checkpoint README、阶段性 CSV 和论文草稿中压低；`SC-INR_Exp.md` 现在能作为内部事实记录使用；论文草稿已编译并经过视觉检查。
- 证据不足：论文草稿仍是证据组织草稿，不是完整论文；引用不完整，final `SC-INR` 的 auxiliary/multiseed/w-o-sinc 证据仍缺。
- 下一步是否合理：在继续新增实验前，当前命名和文档状态已经足够进入消融阶段；但消融结果进入论文前仍必须回填 claim-evidence ledger 和 experiment card。
- 需要防止过度宣称：草稿中的 final `SC-INR` 只能写 promising seed1 candidate；`SC-INR-NoPhi` 才是当前稳定多 seed/consistency 证据主体；不能把两者混写。

## checkpoint 命名别名补齐与最终工作区审查

### 完成范围

- 针对用户指出的 `save/` 下仍是旧 checkpoint 子目录名的问题，做了命名迁移审查。
- 采纳独立审查意见：不物理移动或改名已有 checkpoint 目录，避免破坏历史日志、raw result、旧命令和 checkpoint provenance。
- 在当前 checkpoint 目录内补充 canonical symlink alias：
  - `save/sc-inr -> sc-inr-phiz`
  - `save/sc-inr-nophi -> sc-inr-adaptive`
  - `save/sc-inr-nophi-signed -> sc-inr-adaptive-signed`
  - `save/sc-inr-fixed-omega -> sc-inr-fixed`
  - `save/lte-nocellphase -> lte-no-cell`
  - `save/lte-phasez -> lte-feature-phase`
  - `save-seeds/seed2/sc-inr-nophi -> sc-inr-adaptive`
  - `save-seeds/seed3/sc-inr-nophi -> sc-inr-adaptive`
- 更新 `README.md`、`MANIFEST.md`、`artifacts/checkpoints/README_zh.md`、`paper/model_taxonomy.md`、`configs/registry/models.yaml`、`experiments/phiz/experiment_card.md`、`docs/project/SC-INR_Exp.md`、`docs/project/project_story.md`，明确：
  - 新工作使用 canonical alias；
  - 旧物理目录和旧 raw key 只作为 historical provenance；
  - 最终候选展示名为 `SC-INR`，旧 `sc-inr-phiz` 只是历史 checkpoint key。
- 同步分析/评估脚本默认 checkpoint 路径：
  - `entrypoints/eval_full.py`
  - `entrypoints/eval_continuous.py`
  - `entrypoints/eval_fce.py`
  - `entrypoints/eval_phase_intervention.py`
  - `entrypoints/analyze_phase_consistency.py`
  - `scripts/analysis/evaluate_seed1_aux_metrics.py`
  - `scripts/analysis/analyze_sampling_response.py`
  - `scripts/analysis/smoke_check_signed_omega.py`
- 清理本轮重构验证留下的 `artifacts/scratch/refactor_validation_analysis`，并恢复空目录 `artifacts/scratch/save_test` 以避免根目录 tracked symlink `save_test` 断链。

### 运行过的命令与结果

- checkpoint alias 检查：
  - `save/sc-inr`、`save/sc-inr-nophi`、`save/sc-inr-nophi-signed`、`save/sc-inr-fixed-omega`、`save/lte-nocellphase`、`save/lte-phasez` 均能找到 `epoch-best.pth`。
  - `save-seeds/seed2/sc-inr-nophi` 和 `save-seeds/seed3/sc-inr-nophi` 均能找到 `epoch-best.pth`。
- `python -m py_compile` 覆盖本轮改动的 eval/analysis/viz/paper 脚本：通过。
- `python eval_full.py --help`、`python eval_continuous.py --help`：通过。
- `configs/registry/models.yaml` YAML 解析通过，且所有 `checkpoint_dir*` canonical alias 均存在。
- `find . -xtype l -print`：无断链。
- `rg` 审查旧路径：
  - 活跃默认路径已切到 canonical alias；
  - 旧名仍只在 `legacy_checkpoint_dir*`、raw key 映射、archive 文档、claim caveat 或脚本 alias map 中出现。

### 证据支持的结论

- 当前工作区的活跃入口已经不再要求使用 `sc-inr-phiz`、`sc-inr-adaptive`、`lte-no-cell` 等旧名；后续正式实验应使用 `SC-INR` / `SC-INR-NoPhi` 等展示名和 `save/sc-inr` / `save/sc-inr-nophi` 等 canonical alias。
- `save/` 因为仍指向 `artifacts/checkpoints/seed1`，目录列表中会同时看到 canonical alias 和历史物理目录。这个可见冗余是有意保留，不是未迁移完成；彻底隐藏旧名需要改变 checkpoint 根目录语义或物理搬迁，风险大于收益。
- 当前项目组织已经足够进入下一步正式消融与论文证据补全，但还不是最终公开 release 状态；发布前可再做 registry-driven evaluator 和 artifact manifest 自动校验。

### 未解决风险与下一步

- 旧 raw key 和旧 checkpoint 物理目录仍存在，并且应该存在；论文和图表必须只使用 canonical 展示名。
- 后续新增训练不要再使用 `--name sc-inr-phiz`、`--name sc-inr-adaptive` 等旧名；建议使用 `--name sc-inr`、`--name sc-inr-nosinc`、`--name sc-inr-seed2` 等规范名。
- `eval_full.py`、`eval_continuous.py`、aux scripts 仍各自维护 alias map；长期应改为读取 `configs/registry/models.yaml`，降低漂移风险。
- 正式下一步仍应优先做 `SC-INR` auxiliary metrics、`SC-INR w/o sinc` 和必要 multi-seed，而不是继续扩大目录治理。

### 专家评审

- 可靠结论：命名层、文档层、脚本默认路径和 checkpoint alias 已经基本一致；历史名残留有明确 provenance 语境；没有发现断链或脚本语法问题。
- 证据不足：这次是工程组织审查，不产生新的模型有效性证据；不能因此增强任何 PSNR、视觉质量或 consistency claim。
- 下一步是否合理：可以进入正式实验阶段。最合理的第一步是 final `SC-INR` 的 auxiliary metrics 和 `SC-INR w/o sinc`，同时把用户确认的两张视觉图晋级为受控论文 figure draft。
- 需要防止过度宣称：不要把 “checkpoint alias 已统一” 误写成 “历史命名已完全消失”；旧 raw key 必须保留在 provenance 中，最终候选 `SC-INR` 仍是 seed1 preliminary。
