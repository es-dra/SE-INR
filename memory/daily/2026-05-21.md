# 2026-05-21

## 项目健康清理

完成范围：

- 将 LTE vs SC-INR 机制对比图纳入活跃证据索引：
  - `paper/ARTIFACTS_ALLOWED.md`
  - `artifacts/derived/evidence/README_zh.md`
  - `artifacts/derived/paper_figures/README_zh.md`
  - `docs/project/current_state.md`
- 强化机制图 caveat：该图只说明 cell 进入 decoder 的结构差异，不能替代 benchmark、
  机制诊断或 exact box integral 证明。
- 修正旧兼容路径扩散：
  - `entrypoints/test.py` 默认 checkpoint 改为 `artifacts/checkpoints/seed1/liif/epoch-best.pth`。
  - `entrypoints/eval_full.py` 默认 `--save_root` 改为 `artifacts/checkpoints/seed1`。
  - `entrypoints/eval_continuous.py` 增加 `--save_root`，默认 `artifacts/checkpoints/seed1`。
  - `configs/train-div2k/train-lte-seed3-resume.yaml` resume 路径改为
    `artifacts/checkpoints/seeds/seed3/lte/epoch-last.pth`。
- 修正两个 selected qualitative config 中残留的旧绝对路径 `Equivariant-ASISR`。
- 清理 `__pycache__`。

运行过的命令与结果：

- `python scripts/viz/draw_lte_scinr_mechanism.py`：成功重新生成 PNG/PDF/SVG 和 README。
- `python -m py_compile scripts/viz/draw_lte_scinr_mechanism.py entrypoints/test.py entrypoints/eval_continuous.py entrypoints/eval_full.py`：通过。
- `python scripts/analysis/build_canonical_benchmarks.py`：通过，canonical benchmark 表可重建。
- `git diff --check`：通过。
- `find . -xtype l -print`：无断链。
- `rg -n "default='save|default=\"save|save-seeds|Warning: no models with epoch-best.pth found in save/|Equivariant-ASISR" ...`：
  只剩 README 中对兼容 symlink 和禁止新写 `save/`/`save-seeds/` 的主动说明。
- `find . -type d -name '__pycache__' -print`：清理后无输出。

subagent 审查：

- 使用了 1 个真实 explorer subagent 做只读健康审查。
- 采纳意见：索引 2026-05-18 机制图；在 evidence 入口补充 seed1/proxy/diagnostic-only
  边界；收敛旧 `save/` 默认路径；清理缓存。
- 未执行项：不删除 `save`/`save-seeds` 兼容 symlink，不删除 `SC-INR-EQ` registry/raw
  result/context benchmark，因为这些是受控探索证据或兼容入口。

证据支持的结论：

- 当前活跃入口比清理前更不容易误读：机制图已经进入白名单体系，旧路径不再作为默认正式入口。
- 本轮没有改变模型、checkpoint、raw benchmark 或科学结论。

未解决风险与下一步：

- `SC-INR-EQ` 仍应保持 exploratory/single-seed 位置，不能进入主方法叙事。
- 若后续继续清理，可审查 `entrypoints/eval_continuous.py` 是否应完全改为读
  `configs/registry/models.yaml`，而不是维护本地模型字典。
- 历史 memory 和 legacy 中仍会出现旧路径/旧命名，这是 provenance，不应为搜索干净而改写。

专家评审：

本轮属于项目健康与入口收敛，不是科研结论升级。可靠部分是：路径、索引、脚本编译和基础重建检查均已通过；机制图的可用边界更清晰。证据不足部分是：没有重新跑训练或 benchmark，因此不能借本轮清理改变任何 PSNR 或机制实验结论。后续需要防止把“文档更清楚”误写成“证据更强”。
