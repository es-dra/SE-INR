# Claim 与证据矩阵

本文档约束论文中的 claim 强度，避免表述超过已有证据。

| Claim | 证据 | 强度 | 允许表述 | 禁止过度表述 |
| --- | --- | --- | --- | --- |
| `SC-INR-NoPhi` 相对 LTE 改善 OOD scale robustness。 | active benchmark 入口是 `artifacts/derived/benchmarks/benchmark_all_models_long.csv`；阶段 provenance 在 `artifacts/legacy/derived_analysis/benchmark_provenance/benchmark_progress_2026-05-09/multiseed_core_summary.csv`，OOD 相对 LTE 为 `+0.0507 dB`。 | 当前协议/core models 下已验证。 | `SC-INR-NoPhi` 在当前 benchmark protocol 下相对 LTE 有稳定小幅 OOD PSNR 改善。 | `SC-INR` 严格 scale-equivariant；大幅 SOTA gain。 |
| `SC-INR-NoPhi` 改善 same-LR cross-scale observation consistency。 | `artifacts/derived/diagnostics/seed1_aux_metrics_all8/paper/consistency_table.tex`：BSD100 相对 LTE `+8.61 dB`，Urban100 相对 LTE `+7.77 dB`。 | 强 seed1 机制证据。 | no-phase variant 的 decoder-side sampling consistency 显著改善 same-LR cross-scale consistency。 | whole network scale equivariant。 |
| 最终候选 `SC-INR` 在当前 3 seed protocol 下相对 LIIF/LTE 改善 OOD/ALL PSNR。 | `artifacts/derived/benchmarks/paper_main_3seed_summary.csv`：`SC-INR` OOD `22.7578 ± 0.0316`，ALL `25.5298 ± 0.0403`；`paper_main_3seed_paired_delta.csv`：相对 LIIF 的 OOD/ALL 为 `+0.0328 ± 0.0373` / `+0.0364 ± 0.0499`，相对 LTE 的 OOD/ALL 为 `+0.0504 ± 0.0417` / `+0.0320 ± 0.0555`。seed1 context 在 `paper_context_seed1.csv`：最终 `SC-INR` 相对 `SC-INR-NoPhi` 的 OOD/ALL 为 `+0.0295` / `+0.0421`。 | 3 seed PSNR 相对 LIIF/LTE 已验证；辅助指标和 `SC-INR` vs `SC-INR-NoPhi` 仍是 seed1。 | 最终候选 `SC-INR` 在三 seed 下相对 LIIF/LTE 有正 OOD/ALL PSNR 增益；相对 `SC-INR-NoPhi` 的结构增量作为 seed1 context 展示。 | feature phase 相对 `SC-INR-NoPhi` 是稳定 multi-seed PSNR gain；`SC-INR` 是 consistency 最强变体；`SC-INR` 严格 scale-equivariant。 |
| feature-conditioned phase 改善 selected examples 的可见局部结构。 | 用户确认并重导出的图在 `artifacts/derived/paper_figures/qualitative_selected_seed1/`：Urban100 img004 x8 中 `SC-INR` crop PSNR-Y `21.4880`，LTE `19.0226`，`SC-INR-NoPhi` `20.8467`；img012 x8 中 `SC-INR` `23.2229`，LTE `20.4458`，`SC-INR-NoPhi` `19.6163`。 | 定性候选证据。 | selected qualitative examples 显示更清晰的局部结构。 | 未给分布证据时宣称 average visual quality 更好。 |
| analytic sinc response 对最终候选有贡献。 | `SC-INR-NoSinc` 出现在 active seed1 context table `artifacts/derived/benchmarks/paper_context_seed1.csv`；provenance 在 `artifacts/legacy/derived_analysis/benchmark_provenance/benchmark_progress_2026-05-10/seed1_signed_phiz_nosinc_summary.csv`：相对完整 `SC-INR` 的 ID/OOD/ALL 为 `-0.1397 dB` / `-0.0467 dB` / `-0.0777 dB`。`artifacts/derived/diagnostics/sc_inr_nosinc_aux_metrics_seed1/` 显示 NoSinc same-LR consistency 显著高于 `SC-INR`，所以 consistency 本身不能识别 sinc benefit。 | 初步 benchmark/fidelity 证据；当前 consistency metric 不支持 “sinc improves self-consistency” claim。 | 移除 analytic response 会削弱完整 seed1 benchmark，说明 sinc observation term 可能贡献最终候选 PSNR；但 same-LR self-consistency 不能单独识别该贡献。 | gain 只由 sinc 导致；sinc 已被证明 necessary；当前指标下 NoSinc 破坏 sampling consistency。 |
| same-LR consistency 是 diagnostic，不是 observation modeling 正确性的独立证明。 | `SC-INR-NoSinc` 去掉 analytic sinc response 后在 `artifacts/derived/diagnostics/sc_inr_nosinc_aux_metrics_seed1/consistency_summary.csv` 中取得更高 same-LR consistency：ALL 平均 `66.8555` vs `SC-INR` `49.6275`。`artifacts/derived/diagnostics/response_omega_diagnostics_2026-05-10/` 显示 NoSinc active attenuation 为 `0`、cell-only RMSE vs x4 为 `0`，而 `SC-INR` 有非零 analytic cell response。 | 强 diagnostic warning。 | same-LR self-consistency 应与 fidelity 和 response diagnostics 联合解释；cell-independent decoder 也可能刷高分。 | 更高 same-LR consistency 总是意味着更好的 sampling-consistent observation model。 |
| `SC-INR` 是 scale-decoupled，但不是 cell-independent。 | `artifacts/derived/diagnostics/response_omega_diagnostics_2026-05-10/response_distribution_summary.csv`：`SC-INR` active attenuation mean x4/x8/x16/x30 为 `0.040495/0.010570/0.002672/0.000762`。`cell_response_curve_summary.csv`：`SC-INR` cell-only RMSE vs x4 在 x8/x16/x30 为 `0.002854/0.003395/0.003515`，低于 LTE 但高于 NoSinc。 | 小样本机制诊断。 | `SC-INR` 保留有限 analytic cell response，同时避免 LTE 更大的 learned cell-phase sensitivity。 | `SC-INR` 严格 scale-equivariant；`SC-INR` 忽略 scale/cell；cell response magnitude 单独证明重建更好。 |
| `SC-INR-EQ` 是可行的 exploratory Rot-E integration，不是新主方法。 | `SC-INR-EQ` 只在 seed1 context table `artifacts/derived/benchmarks/paper_context_seed1.csv` 和 raw provenance `artifacts/raw_results/seed1/benchmark_sc_inr_eq.json` 中出现；36/36 entries finite，ID/OOD/ALL 为 `31.1513` / `22.7779` / `25.5691`。相对 `LTE-EQ` 的 ID/OOD/ALL 为 `+0.0841` / `+0.0749` / `+0.0780`；相对最终 `SC-INR` 为 `+0.0326` / `+0.0014` / `+0.0118`。机制摘要在 `artifacts/derived/diagnostics/mechanism_suite_2026-05-12/README_zh.md`。 | 初步 single-seed exploratory evidence；Rot-E property tests 仍缺失。 | `SC-INR-EQ` 显示 SC-INR decoder contract 可以与 Rot-E plumbing 结合，在 single-seed benchmark 上相对 `LTE-EQ` 有小幅增益，并与最终 `SC-INR` 基本打平。 | `SC-INR-EQ` 是新主方法；Rot-E integration 证明 strict scale equivariance；single-seed `SC-INR-EQ` 是 paper-level final claim。 |
